# dollama UI ブラッシュアップ計画 (設計ドキュメント)

> 対象: `ui/` (Blazor Server)。**P1-1〜P1-5 / P1-7 実装済 (2026-08-08 / `feat/ui-p1-tokens`)**、
> **P2-1 / P2-5 / P2-8 / P2-9 実装済 (2026-08-08 / `feat/ui-p2-gate` = P2 バッチ A)**、
> **P2-2 / P2-3 / P2-4 実装済 (2026-08-08 / `feat/ui-p2-preview` = P2 バッチ B)**、
> **P2-6 / P2-7 / P2-10 実装済 (2026-08-08 / `feat/ui-p2-polish` = P2 バッチ C) → P2 は全 10 項目クローズ**、
> **P1-6 / P3-1 / P3-3 実装済 (2026-08-08 / `feat/ui-p3-scale` = P3 バッチ D) → P1 も全 7 項目クローズ**、
> **P3-4 / P3-2 実装済 (2026-08-08 / `feat/ui-p3-layout` = P3 バッチ E)**、
> **P3-5 実装済 (2026-08-08 / `feat/ui-p3-history` = P3 バッチ F・ユーザー決裁のうえ実施)**。
> → **これで P1 (7) + P2 (10) + P3 (5) = 全 22 項目クローズ**。残りの実装バックログは無い。
> 以後の作業は §7 の**手動実施台帳 (24 件・全部「未」)** の消化と、そこで NG が出た場合の対処のみ。
> レビュー日: 2026-07-14 / レビュー対象コミット: `feat/fast-mode-g0b-g3k` ブランチ先端 (3f16d6c 時点の ui ツリー)。

---

## 1. 目的とスコープ

### 目的

- **使い勝手**: 生成フロー (タグ入力 → サイズ/ステップ → 生成 → プレビュー) の摩擦を減らす。
  特に「入力したのに生成されない」「今なにが起きているか分からない」系の無言失敗をなくす。
- **配色・コントラスト**: WCAG AA (テキスト 4.5:1 / UI 非テキスト 3:1) を基準に、境界・フォーカス・
  placeholder の視認性を底上げする。LM Studio 風ダークの雰囲気は維持。
- **dollama らしさ**: トップバーの HW テレメトリ (CPU/NPU/iGPU/RTX5080) を「全 HW が協調して
  動いている感」がひと目で伝わる見せ方へ強化する。
- **一貫性**: 散在するボタン/チップ/入力スタイルと直書き色値をトークン化し、以後の変更を
  `:root` 差し替えだけで効く構造にする。

### 非目標 (やらないこと)

- **機能追加・breadth 拡大はしない** ([[project-output-quality-over-features]])。ノードエディタ、
  ギャラリー管理、複雑なワークフロー等 ComfyUI 的機能は対象外。
  - **例外は P3-5 (直近生成のミニ履歴) の 1 件のみ**。この非目標に照らして着手前に判断を仰ぎ、
    **ユーザー決裁により実施**した (2026-08-08 / P3 バッチ F)。**認められた境界は
    「直近 4 枚・セッション内メモリのみ (非永続)・削除/並べ替え/ピン留めなし・
    サムネごとのダウンロード導線なし」**で、これを越えるものは決裁されていない。
    → **「breadth OK な前例」ではない**。境界は散文ではなく機械検査
    (`PreviewHistoryTests` の public メンバ集合 + `PreviewHistoryRazorTests` の F-5 / F-6) で
    固定してあるので、越えようとすると赤くなる。
- C++ 側 (`src/`) は無改修。UI ↔ C++ の API 契約 (`POST /v1/images/generations`, `GET /health`) も不変。
- 全面リライトはしない。**トークン差し替え + 局所改善で 8 割**を取る。各項目は独立にマージ可能・可逆。
- ライトテーマは**追加しない** (判断根拠は §4.1 末尾)。

---

## 2. 現状サマリ

### 構造

- **3 ペイン grid**: 左 320px (タグパレット `TagPalette` + プリセット一覧 `PresetSidebar`) /
  中央 minmax(340px, 400px) (プロンプト・ネガティブの `TagPresetField`、サイズ/ステップ、
  `LoraChips`、生成 2 ボタン) / 右 1fr (画像プレビュー専任)。1100px 以下で 1 カラム縦積み。
- **トップバー**: ブランド → テレメトリ・ミニメーター (SignalR push・4 デバイスのバー+%) →
  タグ表示言語トグル [日本語|EN] → 接続インジケータ。
- **スタイル集約**: 実質すべて `ui/wwwroot/app.css` (約 930 行)。CSS 変数は `:root` に 9 個
  (`--bg/--panel/--panel-2/--border/--text/--muted/--accent/--ok/--ng`)。

### 現行配色トークン

| トークン | 値 | 主用途 |
|---|---|---|
| `--bg` | `#14161a` | ページ背景・チップボタン背景 |
| `--panel` | `#1c1f26` | ペイン・トップバー |
| `--panel-2` | `#23272f` | 入力・カテゴリ・セカンダリボタン |
| `--border` | `#2c313b` | ほぼ全境界線 |
| `--text` | `#e6e9ef` | 本文 |
| `--muted` | `#8b92a0` | ラベル・補足 |
| `--accent` | `#6ea8fe` | 主ボタン・選択・hover・fill |
| `--ok` / `--ng` | `#3fb950` / `#f85149` | 接続 OK / NG・エラー |

### 良い点 (壊さない)

- **状態管理が堅い**: タグ列・追加先・LoRA 選択の真実源が `Generate.razor` に一元化され、
  新リスト再代入で子へ確実に伝播する流儀が徹底されている。プリレンダリング対策
  (`OnAfterRenderAsync`) も正しい。
- **本文コントラストは優秀**: `--text` on `--panel` ≈ **13.6:1**。`--accent` on `--panel` ≈ **6.8:1**、
  accent ボタン上の暗文字 (#0b1220) ≈ **7.8:1** で、主要な文字はすべて AA/AAA 圏。
- **下書き/本番の分離**が視覚 (塗り vs アウトライン) と文言の両方で表現済み。モードバッジ
  (「下書き 768²」) で直近プレビューの条件が分かる。
- チップ UI (Enter/カンマ確定・空 Backspace で末尾削除) は operate 感がよい。
- テレメトリのミニメーターという「dollama らしさ」の核が既に存在する。

---

## 3. 課題一覧

深刻度: **高** = 無言失敗・視認不能級 / **中** = 迷い・引っかかり / **低** = 磨き。

| # | 分類 | 課題 | 現状 | 深刻度 | 対象ファイル |
|---|---|---|---|---|---|
| 1 | UX | **未確定テキストのまま生成すると無視される**。「1girl」と打って Enter せず生成ボタンを押すと、`_draft` は `Tags` に入らずプロンプト空 → #2 と合わさり完全な無言失敗 **→ P2-1 で解消 (2026-08-08・生成前に親が `CommitPendingAsync()` を await + blur 確定を併設)** | `TagInput` は Enter/カンマでのみ確定。blur・生成時の確定なし | **高** | `TagInput.razor`, `Generate.razor` |
| 2 | UX | **プロンプト空だと生成ボタンが「効いているのに何も起きない」**。`GenerateAsync` 冒頭で `_promptTags.Count == 0` なら silent return。ボタンは有効に見える **→ P2-1 で解消 (2026-08-08・`GenerateGate` で disabled + 理由テキスト)** | disabled は `_busy` のみ。空時の理由表示なし | **高** | `Generate.razor` |
| 3 | 配色 | **チップ入力にフォーカス表示がゼロ**。`.chip-entry:focus { outline: none; }` のみで、外枠 `.chips` も光らない。キーボード利用時に現在地が分からない **→ P1-2 で解消 (2026-08-08)** | `textarea/select` は border 色替えのみ、`.chip-entry` は完全無表示 | **高** | `app.css` |
| 4 | 配色 | **入力・カード境界がほぼ見えない**。`--border #2c313b` vs `--panel-2 #23272f` ≈ **1.15:1**、vs `--panel` ≈ **1.26:1** (UI 非テキストの目安 3:1 に遠い)。入力欄とパネルの区別が輝度でつかない **→ P1-1/P1-4 で部分解消 (2026-08-08・境界 2 段化で vs panel 1.26→1.46 / 操作要素 vs panel 2.10。3:1 まで上げると縞模様化するため意図的に手前で止めている)** | 全境界が単一の `--border` | **高** | `app.css` |
| 5 | UX | **IME 変換確定の Enter でタグが誤確定しうる**。日本語 UI なのに `OnKeyDown` が composing を見ない (変換確定 Enter で未変換ローマ字や変換途中文字列がタグ化する恐れ) **→ P2-5 で対処 (2026-08-08・`key=="Process"` を無視・JS interop は不採用)** | `e.Key == "Enter"` のみ判定 | 中 | `TagInput.razor`, `TagPalette.razor` |
| 6 | UX | **生成開始で前回画像を即破棄** (`_imageData = null`)。下書き→本番の比較ができず、画面が毎回プレースホルダへフラッシュする **→ P2 で解消 (2026-08-08・破棄をやめ `preview-dim` で減光保持)** | busy 中はスピナーのみ | 中 | `Generate.razor`, `app.css` |
| 7 | UX | **生成中フィードバックがスピナーのみ**。1024² 本番は 20 秒前後かかるのに経過時間も目安もない (進捗 API は C++ に無いので進捗バーは対象外) **→ P2 で解消 (2026-08-08・`PeriodicTimer` 1s 刻みの経過秒オーバーレイ)** | `.spinner` 単体 | 中 | `Generate.razor`, `app.css` |
| 8 | UX | **生成画像の保存導線がない**。右クリック→名前を付けて保存頼み。data URI なのでファイル名も不定 **→ P2 で解消 (2026-08-08・右上「PNG 保存」+ `dollama_{日時}_{size}.png`)** | ダウンロード/コピー UI なし | 中 | `Generate.razor` |
| 9 | UX | **追加先フォーカス連動が暗黙的すぎる**。お気に入り入力欄に一度触れると以降のパレットクリックが全部 favorites 行きになるが、変化はパレット上部トグルの小さなハイライトだけ。追加先のフィールド側には何も出ない **→ P2-6 で解消 (2026-08-08・対象フィールドの `.chips` に inset 左縁 + favorites 時はパレット全面)** | トグル label の `.on` (accent 枠) のみ | 中 | `app.css` (+ 軽微に `TagPresetField.razor`) |
| 10 | UX | **LoRA 強度スライダの数値表示がドラッグに追従しない**。`@onchange` (離した時) なので操作中は古い値が見え続ける **→ P2 で解消 (2026-08-08・`@oninput` 化 + parse/表示の InvariantCulture 固定)** | `@onchange` バインド | 中 | `LoraChips.razor` |
| 11 | UX | **テレメトリの「生成中」が視覚に出ない**。`Generating` フラグは title 属性 (ホバー) のみ。全 HW 協調の見せ場が眠っている **→ P2-10 で解消 (2026-08-08・`generating` クラスで各バーにデバイス色グロー + ブランド横に「生成中」pill)** | バー幅の変化だけ | 中 | `Generate.razor`, `app.css` |
| 12 | 配色 | **placeholder が未スタイル**。ブラウザ既定色 (UA 依存・半透明) でダーク背景ではコントラスト不定。例示文言 (「例: 1girl と入力して Enter」) が導線なのに読みにくい **→ P1-3 で解消 (2026-08-08)** | `::placeholder` 宣言なし | 中 | `app.css` |
| 13 | 配色 | **accent 青一色に役割が集中**。主ボタン・選択チップ・hover・保存完了メッセージ・テレメトリ%・言語トグル on がすべて `--accent`。「操作できる」「選択中」「情報」の区別が色で付かない | 単一 `--accent` | 中 | `app.css` |
| 14 | 一貫性 | **色の直書きが散在**。`rgba(110,168,254,…)` (チップ 2 箇所)、`#0d1117`/`#0b1220`/`#0b0f17` (accent 上の暗文字が 3 種微妙に違う)、`#ffb4ae` (エラー淡色 2 箇所)、`#9d7bff` (バーのグラデ)。accent を差し替えるとチップだけ旧色で残る **→ P1-1 で解消 (2026-08-08・`:root` 外の直書き色ゼロを `AppCssTokenTests` で機械保証)** | トークン未経由 | 中 | `app.css` |
| 15 | 一貫性 | **ボタン系が 5 系統の重複定義**。`.preset-btn` / `.fav-plus` / `.lang-toggle button` / `.generate.secondary` / `.ps-del` がほぼ同じ見た目を別々に宣言。hover 作法も border 色替え/文字色替えが混在 **→ P1 で部分解消 (2026-08-08・境界とフォーカスの作法は統一。ボタン統合は P3-1 に残)** | 共通クラスなし | 中 | `app.css` |
| 16 | レイアウト | **タイポ階層がフラット**。11/12/13/14/15px がアドホックに散在し、ペイン見出し (`palette-head` 13px) と本文の差が 1px。視線が「どこから読むか」を掴めない | スケール定義なし | 中 | `app.css` |
| 17 | レイアウト | **1100px 縦積みで生成ボタンとプレビューが分断**。縦積み順が 左→中央→右 のため、生成ボタンを押した後に画面最下部までスクロールしないと結果が見えない | `@media (max-width:1100px)` 単純 1fr | 中 | `app.css` |
| 18 | 配色 | **`--muted` の小サイズ使用が余裕薄**。#8b92a0 on `--panel` ≈ **5.3:1**、on `--panel-2` ≈ **4.8:1** で AA は満たすが、11px 箇所 (`tm-item`, `palette-empty`, `lora-val`) はぎりぎり感 **→ P1-1 で解消 (2026-08-08・#9aa2b1 で on panel 6.42:1 / on panel-2 5.83:1)** | 単一 muted | 低 | `app.css` |
| 19 | UX | **プリセット同名保存が確認なし上書き**。`PresetStore.Save` は同 kind 同名を黙って置換する仕様 (store 側は正しい)。UI で一言も出ない **→ P2-7 で解消 (2026-08-08・保存前に `All(kind)` で照合し「〜を上書き保存しました」)** | 保存メッセージは成功文言のみ | 低 | `TagPresetField.razor` |
| 20 | UX | **保存バーが常時表示でノイズ**。タグ 0 個でもプリセット名入力+保存ボタンが両フィールドに出続ける **→ P2-7 で解消 (2026-08-08・`Tags.Count == 0` で保存バーごと非表示)** | 常時表示 | 低 | `TagPresetField.razor`, `app.css` |
| 21 | UX | **キーボードで生成できない**。タグ確定後に毎回マウスでボタンへ移動する。Ctrl+Enter 等のショートカットなし **→ P2-8 で解消 (2026-08-08・`section.gen` で keydown を拾い Ctrl/Cmd+Enter)** | なし | 低 | `Generate.razor`, `TagInput.razor` |
| 22 | 一貫性 | **再接続モーダル/エラーバーが英語+ライト配色**。`ReconnectModal` は "Rejoining the server..."、`MainLayout.razor.css` の `#blazor-error-ui` は `lightyellow` でダークテーマ内で異物 **→ P1-7 で解消 (2026-08-08)** | テンプレート既定のまま | 低 | `ReconnectModal.razor(.css)`, `MainLayout.razor(.css)` |
| 23 | 一貫性 | **角丸・チップの微差**。radius 4/6/8/10/999px が混在 (それ自体は可) だが体系がなく、`fav-chip` と `tag-chip` は同じ「タグチップ」なのに padding/font-size が別定義 | 個別指定 | 低 | `app.css` |
| 24 | レイアウト | **中央列の上限 400px が窮屈**。長いタグ列 (15 個超) でチップが縦に積み上がる一方、右ペイン 1fr は正方形画像の左右に大きな余白が残る | `minmax(340px,400px)` 固定 | 低 | `app.css` |

**計 24 件** (高 4 / 中 13 / 低 7)。

### コントラスト比の算定根拠 (概算・WCAG 相対輝度)

| ペア | 比 | 判定 |
|---|---|---|
| `--text` #e6e9ef / `--panel` #1c1f26 | ≈ 13.6:1 | AAA ✅ |
| `--muted` #8b92a0 / `--panel` | ≈ 5.3:1 | AA ✅ (小サイズは余裕薄) |
| `--muted` / `--panel-2` #23272f | ≈ 4.8:1 | AA ✅ ぎりぎり |
| `--accent` #6ea8fe / `--panel` | ≈ 6.8:1 | AA ✅ |
| #0b1220 (ボタン文字) / `--accent` (ボタン地) | ≈ 7.8:1 | AA ✅ |
| `--ok` #3fb950 / `--panel` | ≈ 6.5:1 | AA ✅ |
| `--ng` #f85149 / `--panel` | ≈ 4.9:1 | AA ✅ (テキスト用は淡色 #ffb4ae 併用が正解) |
| `--border` #2c313b / `--panel-2` | ≈ **1.15:1** | UI 3:1 ✗ |
| `--border` / `--panel` | ≈ **1.26:1** | UI 3:1 ✗ |

→ **文字は健全・「枠とフォーカス」が弱点**という診断。

---

## 4. 提案デザイン方針

### 4.1 配色トークン改訂案

方針: **ダーク単一継続** (LM Studio 風維持)。ベース色相は現行のまま、(a) 境界 2 段化、
(b) フォーカスリング新設、(c) 直書き色のトークン化、(d) テレメトリ用デバイス色 4 色の追加。
`:root` 差し替え + 直書き箇所の `var()` 置換だけで完結し、レイアウトへの影響ゼロ。

| トークン | 現行 | 改訂案 | 根拠 / 想定コントラスト |
|---|---|---|---|
| `--bg` | `#14161a` | **据え置き** | 問題なし |
| `--panel` | `#1c1f26` | **据え置き** | 問題なし |
| `--panel-2` | `#23272f` | **据え置き** | 問題なし |
| `--border` | `#2c313b` | `#343b47` | 非操作境界。vs panel ≈ 1.6:1 (装飾線としては十分・全枠を 3:1 にすると縞模様化して逆効果) |
| `--border-strong` | — (新設) | `#4a5261` | **操作可能要素** (入力・ボタン・カード) の境界専用。vs panel-2 ≈ 2.3:1 + hover/focus で補完 |
| `--text` | `#e6e9ef` | **据え置き** | 13.6:1 |
| `--muted` | `#8b92a0` | `#9aa2b1` | 11–12px 箇所の余裕確保。on panel ≈ 6.3:1 / on panel-2 ≈ 5.7:1 (AA 余裕) |
| `--accent` | `#6ea8fe` | **据え置き** | 6.8:1・ブランド色として維持 |
| `--on-accent` | — (新設) | `#0b1220` | accent 塗り上の文字。現在 3 種散在 (#0d1117/#0b1220/#0b0f17) を 1 本化。≈ 7.8:1 |
| `--accent-weak` | — (新設) | `rgba(110,168,254,0.15)` | 選択チップ地。`rgba(110,168,254,…)` 直書き置換 (accent 連動は P2 の color-mix 化で完成) |
| `--accent-border` | — (新設) | `rgba(110,168,254,0.4)` | 選択チップ枠。同上 |
| `--ok` | `#3fb950` | **据え置き** | 6.5:1 |
| `--ng` | `#f85149` | **据え置き** | 枠・ドット用。4.9:1 |
| `--ng-soft` | — (新設) | `#ffb4ae` | エラー本文用の淡色。#ffb4ae 直書き 2 箇所を統合。on エラー地 ≈ 9:1 超 |
| `--ng-weak` | — (新設) | `rgba(248, 81, 73, 0.12)` | **PL 裁定で追加** (`.error` 背景の直書き `rgba(248,81,73,0.12)` が本表から漏れていた。置換してトークン経由率 100% を例外なしで成立させるため) |
| `--focus-ring` | — (新設) | `rgba(110,168,254,0.45)` | `box-shadow: 0 0 0 2px var(--focus-ring)` の全フォーカス統一リング。リング自体の視認 3:1 相当 |
| `--overlay` | — (**P2-2 で追加**) | `rgba(0, 0, 0, 0.45)` | 生成中オーバーレイの暗幕 (`.preview-overlay`)。減光 (0.35) した前回画像の上でも経過秒が読めるようにする。明るい画像でも `--text` 比 ≈ 5.9:1 |
| `--dev-cpu` | — (新設) | `#6ea8fe` | テレメトリ・デバイス色 (青=CPU)。on panel ≈ 6.8:1 |
| `--dev-npu` | — (新設) | `#a78bfa` | 紫=NPU (現グラデ #9d7bff の系譜)。on panel ≈ 5.8:1 |
| `--dev-igpu` | — (新設) | `#5fd4c8` | ティール=iGPU。on panel ≈ 9:1 |
| `--dev-gpu` | — (新設) | `#f0a860` | オレンジ=RTX5080 (主役の熱量)。on panel ≈ 8:1 |

**ライト/ダーク判断**: **ダーク単一継続**。理由: ① 参照体験 (LM Studio) がダーク前提でブランド一貫、
② 生成画像 (アニメ塗り) の見えはダーク地が有利、③ ライト対応は全トークンの二重管理 + 検証コスト増で
「絵の質・使い勝手第一」の投資判断に合わない。将来必要になっても本改訂でトークン経由率を
100% にしておけば `:root` 追加だけで足せる (可逆)。

### 4.2 タイポ / スペーシングのスケール

現行のアドホット値を最小限のスケールに畳む。**px 値自体はほぼ現行踏襲**で、トークン名を通すのが主目的。

```css
/* タイポ (4 段) */
--fs-xs: 11px;   /* テレメトリ・補足 (使用箇所を最小化) */
--fs-sm: 12px;   /* ラベル・チップ・ボタン小 */
--fs-md: 14px;   /* 本文 (html 既定) */
--fs-lg: 16px;   /* ブランド・ペイン見出し */

/* スペーシング (4px グリッド) */
--sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;  --sp-4: 16px;  --sp-6: 24px;

/* 角丸 (3 段 + pill) */
--r-sm: 6px;   /* 入力・小ボタン */
--r-md: 10px;  /* ペイン・カード (現 8/10 を 10 に寄せる) */
--r-pill: 999px;
```

- ペイン見出し (`palette-head` 等) は `--fs-sm` + `letter-spacing: 0.06em` + `--muted` の
  「小さく薄く広く」型セクションヘッダへ (LM Studio 流儀)。本文と 1px 差で並ぶ現状より階層が立つ。
- 13px / 15px の中間値は最寄りへ寄せる (13→12 or 14、15→14 or 16)。視覚差は軽微で棚卸し効果が大きい。

### 4.3 コンポーネント別の改善方針

**トップバー**
- 接続インジケータに **未接続時の再試行ボタン** (「再接続」小ボタン) を添える。現在は未接続だと
  ページリロードしか回復手段がない。
- 未接続時は生成 2 ボタンを disabled + tooltip「C++ サーバー未接続」にし、押してからエラーで知るのをやめる。

**テレメトリ (§4.4 に詳細)**

**タグパレット (左)**
- 追加先トグルのラジオ丸ポチを隠し、**セグメントコントロール見た目** (言語トグルと同型・`.on` は
  accent 塗り) に統一。現在「◉ラジオ + accent 枠」の二重表現で言語トグルと作法が違う。
- パレットのタグボタン hover は現行 (accent 枠) 維持。favorites ターゲット中はパレット全体に
  うっすら地色 (例: `--accent-weak` の縁) を敷き「今クリックするとお気に入りに入る」を面で示す (#9)。

**生成コントロール (中央)**
- **追加先ハイライト**: `_target` に一致するフィールドの `.chips` に accent の左縁 (3px) を付け、
  「パレットのタグはここに入る」を入力欄側でも示す (#9)。
- **保存バーの整理**: タグ 0 個のときは保存バーを非表示 (または「保存…」の折りたたみ) にして
  ノイズを減らす (#20)。同名保存時はメッセージを「〜を上書き保存しました」に変える (#19)。
- **生成ボタン**: プロンプト空なら disabled + ボタン下に muted で「プロンプトにタグを 1 つ以上
  追加してください」。未確定 draft がある場合は生成時に自動確定してから送る (#1, #2)。
- **ステップスライダ**: 現行値表示は良い。既定 20 の位置に datalist 目盛り (1 行) を足す程度で十分。
- **LoRA**: スライダを `@oninput` (即時) バインドへ (#10)。LoRA 行はタイトル行「LoRA」を
  他フィールドの `field > span` と同スタイルに揃える。

**プレビュー (右)**
- **前回画像を保持**: busy 中は前回画像を `opacity: 0.35` で残し、中央にスピナー + 経過秒
  (`00:12`) をオーバーレイ (#6, #7)。完了時に差し替え。
- **アクションバー**: 画像表示中のみ右上に「PNG 保存」ボタン (a[download] + data URI で JS 追加不要)。
  ファイル名は `dollama_{yyyyMMdd_HHmmss}_{size}.png` (#8)。
- モードバッジは現行維持。バッジに steps を追記 (「本番 1024² / 20 steps」) すると再現に効く (#24 の軽減にも)。

**プリセット (左下)**
- カード hover 時に **タグ列のプレビューを title でなくポップ表示は不要** (breadth 抑制)。
  現行 title (名前のみ) を `名前 — タグ列先頭 5 個` に変えるだけで十分。

### 4.4 「全 HW 協調」の可視化強化 (dollama らしさ)

テレメトリは dollama の看板。ただし派手にはしない — **色と 1 つのアニメーションだけで語る**。

1. **デバイス固有色** (P1・CSS のみ): バーの fill を全デバイス同一グラデ → デバイス別単色へ
   (`--dev-cpu/npu/igpu/gpu`)。`tm-item` に `data-dev` 属性 (または device 名の CSS クラス) を付けて
   fill と `tm-pct` を各色に。**4 色が並んで動く = 4 HW が協調して動いている**が一瞥で伝わる。
   **実装済 (2026-08-08)**: デバイス名 → クラス名 (`tm-cpu`/`tm-npu`/`tm-igpu`/`tm-gpu`) の写像は
   razor の三項演算子ではなく純クラス `Services/DeviceStyle.cs` に切り出した (`DraftPreview` と同じ流儀・
   `ui.Tests/DeviceStyleTests.cs` で検証)。未知デバイスは `""` を返し既定グラデへフォールバックする。
   なお既定グラデに残っていた直書き `#9d7bff` は `--dev-npu` (`#a78bfa`) に置換した — **意図的な微変更**
   (トークン経由率 100% を優先し、ほぼ同系の紫へ寄せた)。
2. **生成中の強調** (P1–P2): `_sample.Generating == true` のとき `telemetry-mini` にクラス `generating`
   を付け、(a) 各バーに `box-shadow: 0 0 6px <デバイス色 40%>` の淡いグロー、(b) ブランド横に
   小さな「生成中」pill (accent 塗り) を出す。title 頼みをやめる (#11)。
3. **役割ラベルの露出** (P2): 現在 `Role` ("CLIP enc" / "SDXL UNet" 等) は title のみ。ホバー時に
   バー下へ 10px で表示、または幅に余裕がある時だけ常時表示 (`min-width` メディアクエリ)。
   「NPU が CLIP を、RTX5080 が UNet をやっている」というパイプライン理解が UI から得られる。
   **実装済 (2026-08-08 / P3-3)**: 2 案のうち**後者 (広幅時常時表示)** を採用。`.tm-role` は
   既定 `display: none` (通常フローに入らない) で、`@media (min-width: 1500px)` のときだけ
   バー行の**末尾**に `--muted` で出す。**ホバー案は不採用** — `.telemetry-mini` が
   `overflow: hidden` なので、絶対配置でバーの外へ出したラベルはクリップされて見えない
   (P2-10 のグローと同じクリップの罠)。表示制御は CSS だけで完結し razor に状態を増やさない。
   狭い幅では従来どおり `title` 属性が受け皿。
4. **やらないこと**: パイプライン DAG 図・履歴グラフ・スパークライン等は breadth。テレメトリは
   現状スタブ波形なので、実測が入るまで見せ方への過剰投資はしない。

---

## 5. 実装バックログ

工数目安: S = 〜1h / M = 半日 / L = 1 日超。各項目は独立にマージ可能な単位に分割済み。

### P1 — トークンと CSS だけで効く (app.css 中心・挙動無変更・完全可逆)

| ID | 課題# | 変更内容 | 対象 | 工数 | リスク・可逆性 | 依存 |
|---|---|---|---|---|---|---|
| P1-1 ✅ 2026-08-08 | 4, 18, 14 | `:root` トークン改訂 (§4.1 の表どおり): `--border` 明度上げ・`--border-strong`/`--on-accent`/`--accent-weak`/`--accent-border`/`--ng-soft`/`--focus-ring`/`--dev-*` 新設・`--muted` 更新。既存の直書き色 (`rgba(110,168,254,…)`, `#0d1117`, `#0b1220`, `#0b0f17`, `#ffb4ae`, `#9d7bff`) を `var()` へ置換 | `app.css` | S | ほぼゼロ。git revert 1 発で戻る | なし |
| P1-2 ✅ 2026-08-08 | 3 | フォーカス統一リング: `textarea:focus, select:focus, input:focus-visible, button:focus-visible, .chips:focus-within` に `box-shadow: 0 0 0 2px var(--focus-ring)` + border-color accent。`.chip-entry:focus { outline:none }` は残すが親 `.chips:focus-within` で必ず光らせる | `app.css` | S | ゼロ | P1-1 |
| P1-3 ✅ 2026-08-08 | 12 | `::placeholder { color: var(--muted); opacity: 1; }` を明示 | `app.css` | S | ゼロ | P1-1 |
| P1-4 ✅ 2026-08-08 | 4, 15 | 操作可能要素 (`textarea/select/.preset-name/.fav-entry/.chips/.preset-btn/.fav-plus/.ps-card/.palette-tag/.lora-chip`) の境界を `--border-strong` へ。非操作 (ペイン枠・区切り) は `--border` のまま | `app.css` | S | 低。見た目のみ | P1-1 |
| P1-5 ✅ 2026-08-08 | — (§4.4-1) | テレメトリのデバイス別色: `tm-fill`/`tm-pct` をデバイス色に。razor 側は `tm-item` にデバイス名クラス (`tm-cpu` 等) を 1 属性足すだけ | `app.css`, `Generate.razor` (1 行), `Services/DeviceStyle.cs` | S | 低 | P1-1 |
| P1-6 ✅ 2026-08-08 (`feat/ui-p3-scale`) | 16, 23 | タイポ/スペーシング/角丸トークン (§4.2) を `:root` に追加し、既存宣言を段階置換。ペイン見出しをセクションヘッダ型へ | `app.css` | M | 低。px 実値ほぼ不変 | P1-1 |
| P1-7 ✅ 2026-08-08 | 22 | `#blazor-error-ui` (`MainLayout.razor.css`) をダーク配色へ (app.css 側の同名定義と重複しているので css 側を整理)。`ReconnectModal` の文言を日本語化 + ダーク配色 | `MainLayout.razor(.css)`, `ReconnectModal.razor(.css)` | S | 低 | なし |

> **P1 実装メモ (2026-08-08)**: 重複整理の実体は「app.css 側が死にコードだった」。scoped CSS は
> `#blazor-error-ui[b-xxxx]` へ書き換わり裸のセレクタより詳細度で必ず勝つため、実際に効いていたのは
> `MainLayout.razor.css` の `lightyellow` 側。app.css 側のブロックを削除し scoped 側をダーク化して一本化した。
> P1-4 の `--border-strong` は 13 箇所 (上記 + `.generate.secondary`/`.ps-del`/`.palette-target label`/`.lang-toggle` 外枠)、
> `--border` 据え置きは 10 箇所 (`.topbar`/`.gen`/`.canvas`/`.palette,.preset-sidebar`/`.preview`/`.ps-noimg`/
> `.gen-mode`/`.palette-cat`/`.lang-toggle` 内側区切り/`.spinner`)。規準は「クリック/フォーカスできる要素の外周 = strong」。

### P2 — 挙動を伴う UX 改善 (razor/cs 変更・各項目独立)

| ID | 課題# | 変更内容 | 対象 | 工数 | リスク・可逆性 | 依存 |
|---|---|---|---|---|---|---|
| P2-1 ✅ 2026-08-08 (`feat/ui-p2-gate`) | 1, 2 | **無言失敗の根絶**: ① `TagInput` に「未確定 draft を確定して返す」公開メソッド or blur 確定を追加 ② `Generate` は生成前に確定を要求 ③ プロンプト空なら生成ボタン disabled + 理由テキスト。**テスト**: draft 確定ロジックを ui.Tests で (正規化・カンマ展開・重複) | `TagInput.razor`, `TagPresetField.razor`, `Generate.razor`, `Services/TagCommit.cs`, `Services/GenerateGate.cs` | M | 中 (バインド伝播に注意・既存の新リスト再代入流儀を踏襲) | なし |
| P2-2 ✅ 2026-08-08 (`feat/ui-p2-preview`) | 6, 7 | busy 中は前回画像を減光保持 + スピナー/経過秒オーバーレイ。経過秒は **`PeriodicTimer`** で 1s 刻み `StateHasChanged` (表示整形は純クラス `ElapsedFormat`) | `Generate.razor`, `app.css`, `Services/ElapsedFormat.cs` | M | 低 | なし |
| P2-3 ✅ 2026-08-08 (`feat/ui-p2-preview`) | 8 | プレビュー右上に「PNG 保存」(`<a download="dollama_….png" href="data:image/png;base64,…">`)。JS interop 不要 (ファイル名は純クラス `DownloadName`) | `Generate.razor`, `app.css`, `Services/DownloadName.cs` | S | 低 | なし |
| P2-4 ✅ 2026-08-08 (`feat/ui-p2-preview`) | 10 | LoRA スライダを `@oninput` 即時反映へ (`@onchange` → `@oninput`)。SignalR 往復頻度が上がるが値は軽量。**併せて parse/表示を InvariantCulture 固定** (潜在バグの同時修正) | `LoraChips.razor` | S | 低 | なし |
| P2-5 ✅ 2026-08-08 (`feat/ui-p2-gate`) | 5 | IME ガード: `e.Key == "Process"` を無視 + 可能なら `KeyboardEventArgs.IsComposing` 相当の判定 (Blazor 標準に無ければ `@onkeydown` の代わりに keypress 系 or 小さな JS interop を検討。**JS を足すなら最小 1 関数**) | `TagInput.razor`, `TagPalette.razor` | M | 中 (ブラウザ差・要手動確認) | P2-1 |
| P2-6 ✅ 2026-08-08 (`feat/ui-p2-polish`) | 9 | 追加先の可視化: `_target` 一致フィールドの `.chips` に accent 左縁 + favorites ターゲット中のパレット縁色 | `Generate.razor`, `TagPresetField.razor`, `TagPalette.razor`, `app.css` | M | 低 | P1-1 |
| P2-7 ✅ 2026-08-08 (`feat/ui-p2-polish`) | 19, 20 | 保存バー整理: タグ 0 で非表示・同名時の文言を「上書き保存」へ (`PresetStore.All(kind)` で既存名照合)。**テスト**: 文言生成を純クラス `PresetSaveMessage` へ切り出して全数 | `TagPresetField.razor`, `Services/PresetSaveMessage.cs` | S | 低 | なし |
| P2-8 ✅ 2026-08-08 (`feat/ui-p2-gate`) | 21 | Ctrl+Enter で生成 (チップ入力フォーカス中でも発火)。`@onkeydown` を `.gen` セクションで拾う | `Generate.razor`, `TagInput.razor` | S | 低 | P2-1 |
| P2-9 ✅ 2026-08-08 (`feat/ui-p2-gate`) | — (§4.3) | 未接続時: 生成ボタン disabled + 接続インジケータに「再接続」ボタン (HealthAsync 再試行) | `Generate.razor`, `app.css` | S | 低 | なし |
| P2-10 ✅ 2026-08-08 (`feat/ui-p2-polish`) | 11 (§4.4-2) | 生成中のテレメトリ強調 (`generating` クラス + グロー + 「生成中」pill) | `Generate.razor`, `app.css` | S | 低 | P1-5 |

> **P2 バッチ A 実装メモ (2026-08-08 / `feat/ui-p2-gate` / P2-1・P2-9・P2-8・P2-5)**
>
> - **確定方式は「公開メソッドを親から呼ぶ」が主・blur が従**。`Generate.GenerateAsync` が
>   `@ref` 経由で `TagPresetField.CommitPendingAsync()` → `TagInput.CommitPendingAsync()` を
>   **両フィールド分 await** してからタグ列を読む。blur 単独に頼らないのは、Blazor Server では
>   blur と click が**別の SignalR ラウンドトリップ**で飛び順序が保証されないため。
>   blur (`@onblur`) は Tab 移動時の取りこぼし救済として併設する。
> - **draft の文字列は親へ渡さない**。親が draft を持つと真実源の二重管理になるので、
>   `TagInput` は「空かどうか」だけ `DraftEmptyChanged` (真偽) で通知し、親はそれを
>   ボタンの活性判定にのみ使う。**これがないと「draft に文字があるのにタグ 0 なので disabled」→
>   確定の機会が永久に来ない**という別の無言失敗になる (実装上いちばん踏みやすい罠)。
> - **`Services/GenerateGate.cs` が P2-1 / P2-8 / P2-9 の唯一の合流点**。
>   `Evaluate(busy, connected, promptTagCount, draftEmpty) → (CanGenerate, Reason?)` で
>   2 ボタンの disabled・理由テキスト・Ctrl+Enter が同じ判定を通る。入力が真偽 4 つなので
>   **16 組合せを xUnit で全数検査**でき、razor に条件式が散らない。
> - **確定規則は `Services/TagCommit.cs`** (`Normalize` / `Split` / `Merge`) に集約。名前は
>   生成モードの `DraftPreview` と紛らわしくないよう `TagDraft` ではなく `TagCommit`。
>   ついでに `TagInput` の確定・削除・Backspace を**新リスト再代入**へ是正した
>   (旧 `AddOne` は `Tags.Add` でその場 Mutate = 本プロジェクトの伝播流儀違反だった)。
> - **P2-5 は JS interop を不採用**とした (PL 裁定)。Blazor の `KeyboardEventArgs` に
>   `IsComposing` が無く完全解には JS が要るが、① このチップ入力に入るのは danbooru タグ =
>   英数字で IME を使う場面が乏しい ② `wwwroot/*.js` + `IJSRuntime` + Dispose 管理 +
>   テスト不能領域が増え「配管を増やさない」方針に反する ③ 実害である「未変換ローマ字のタグ化」は
>   Windows の Chrome/Edge/Firefox が変換中/確定の Enter を `key="Process"` (keyCode 229) で
>   送るため `Process` 無視だけで防げる。実装は `TagInput.OnKeyDown` と
>   `TagPalette.OnFavKeyDown` の 2 箇所で即 return。
>   **残存リスク**: composition 終了と同一 keydown で `Enter` が来るブラウザでは 1 回だけ
>   確定が漏れうる。手動確認で再現したら実装で粘らず PL へ上げる (JS を勝手に足さない)。
> - **P2-9 は定期ポーリングを足さない**。未接続判定は既存 `_connected` の再利用で、再判定経路は
>   「初回描画」「生成の成否」「再接続ボタン押下時の `HealthAsync()` 1 回」の 3 つだけ
>   (`_reconnecting` で二度押し防止)。
> - テスト: `TagCommitTests` 20 件 + `GenerateGateTests` 24 件を追加し **91 → 135 件**。
>   bUnit は入れない方針を維持 (依存追加ゼロ)。

> **P2 バッチ B 実装メモ (2026-08-08 / `feat/ui-p2-preview` / P2-2・P2-3・P2-4)**
>
> - **`_imageData = null` を消した影響は 4 点に閉じる**。① **サムネ**: `_imageBytes` は
>   `TagPresetField.CurrentImage` に渡るプリセット保存のサムネ元なので、破棄をやめると
>   「生成失敗後に保存 → 直近の成功画像がサムネになる」。**画面に見えている画像がサムネになる方が
>   直感的**なので許容する (`docs/ui-preset-thumbnail-spec.md` に明記)。② **モードバッジ**:
>   `_lastMode` は成功時のみ更新の現状維持 — バッジと表示画像が常に同じ生成を指す一貫性を壊さない。
>   ③ **下書き/本番**: `_size` 非破壊・`sendSize` ローカル上書き・`_runningDraft` の文言出し分けは不変
>   (`DraftPreview` も無改修)。④ **エラー**: `_error` の生成開始時クリアは現状維持。
> - **タイマーは fire-and-forget にしない**。`GenerateAsync` 内で `CancellationTokenSource` を作り、
>   `finally` で「参照を外す → `Cancel()` → `await tickTask` → `Dispose()`」の順に確実に畳む。
>   `DisposeAsync` からも `Cancel()` するが、この順序なので破棄済み cts が見えることはない。
>   停止経路は **完了 / エラー / 離脱の 3 つ**。刻みは加算ではなく開始時刻との差分
>   (`DateTimeOffset.UtcNow - startedAt`) で取り、刻みがずれても表示が遅れない。
> - **`System.Timers.Timer` を使わない理由**: コールバックが別スレッドで来るので毎回
>   `InvokeAsync` が要るうえ、Dispose と発火のレースを自前で塞ぐ必要がある。
>   `PeriodicTimer` + `await` ならループが 1 本の Task に閉じ、キャンセルで必ず終わる。
> - **`.preview-save` はフォーカスリングの統一ブロックへ追記**した。`<a download>` なので
>   `button:focus-visible` に当たらないが、別ブロックを作ると「`var(--focus-ring)` を使う宣言は
>   1 つ」という `AppCssTokenTests` の機械検査 (P1-2 の回帰止め) に引っかかるため。
>   **※ P3-1 (バッチ D) で追記先が変わった**: このボタンが `.btn .btn-ghost .canvas-save` へ
>   統合されたのに伴い、統一ブロック内の `.preview-save:focus-visible` を
>   **`.btn:focus-visible` へ張り替えた** (1 ブロック契約は不変)。a 要素がボタンの疑似クラスに
>   乗らないという罠は同じで、受け皿の名前だけが `.preview-save` → `.btn` になった形。
> - **P2-4 は `@oninput` 化とセットで `float.TryParse` / `ToString` を `InvariantCulture` へ固定**した。
>   `input[type=range]` の値は常に `"0.85"` 形式で来るのに parse が現在カルチャ依存だったため、
>   小数点がカンマのロケール (de-DE 等) では**強度がまったく動かない**潜在バグだった。
>   発火頻度が上がる変更と同時に入れるのは範囲拡大ではなく回帰予防。
> - テスト: `ElapsedFormatTests` 31 件 + `DownloadNameTests` 38 件を追加し **135 → 204 件**。
>   タイマー本体は自動テスト対象外 (制御フローだけを使い捨てハーネスで確認)、
>   検証するのは切り出した純ロジック 2 つだけ。

> **P2 バッチ C 実装メモ (2026-08-08 / `feat/ui-p2-polish` / P2-6・P2-10・P2-7)**
>
> - **P2-6 の左縁は `border-left` ではなく `box-shadow: inset 3px 0 0`**。border 幅を変えると
>   追加先が移るたびに入力欄が 3px 伸縮してレイアウトが揺れる。
> - **左縁とフォーカスリングは box-shadow を奪い合う**。`.field.is-target .taginput .chips`
>   (クラス 4 個) は P1-2 のリング `.taginput .chips:focus-within` (クラス 3 個) に詳細度で勝つため、
>   素直に書くと**フォーカス中のプロンプト欄でリングが消える** (P1-2 の退行)。かといってリングを
>   2 ブロック目に書くと「`var(--focus-ring)` の宣言は 1 つ」の機械検査に引っかかる。
>   解決は**影の値を CSS 変数に載せて合成する**こと:
>   `:root` に `--edge`(既定) / `--edge-off`(no-op) / `--edge-target`(inset 3px) を置き、
>   `.chips` は `box-shadow: var(--edge)`、追加先の規則は `--edge: var(--edge-target)` を**代入するだけ**、
>   リング側は `box-shadow: var(--edge), 0 0 0 2px var(--focus-ring)`。詳細度の争いが消え、
>   追加先かつフォーカス中は「左縁 + リング」が同時に出る。
>   `--edge` は**継承する**ので、チップの `×` (`.chip-x`) では `--edge-off` に戻して
>   小さなボタンのフォーカスリングに 3px の縁が載らないようにする (踏みやすい罠)。
> - **`IsTarget` は親から降ろすパラメータ**。`TagPresetField` は `Target` を見て自前で判定しない —
>   追加先の真実源は `Generate._target` のままに保つ (子が判定を持つと二重管理になる)。
> - **P2-10 のグローは `.tm-fill` ではなく `.tm-bar` に出す**。`.tm-bar { overflow: hidden }` なので
>   子 (`.tm-fill`) に付けた影は**クリップされて 1px も見えない**。`color-mix()` は使わず
>   `box-shadow: 0 0 6px var(--dev-*)` (P1-5 のデバイス色そのまま) で、**新トークン追加ゼロ**。
> - **「生成中」pill の真実源はテレメトリの `_sample.Generating`** (`_busy` ではない)。
>   `TelemetryBroadcaster` は `GenerationActivity` を見ているので、C++ サーバーが無くても
>   `Activity.Enter` 中は pill とグローが出る (手動確認が C++ 無しでできる)。
> - **P2-7 で `PresetStore` は無改修**。同名判定は `TagPresetField` が保存**前**に
>   `Presets.All(Kind)` を読んで行う (照合は store と同じ `OrdinalIgnoreCase`)。
>   文言は純クラス `Services/PresetSaveMessage.Build(name, overwrite, hasThumb)` に切り出し、
>   4 通りを全数テストする (razor に三項演算子を積み増さない)。
> - 保存バーは `Tags.Count == 0` で非表示にするが、**保存後メッセージも同じ条件に載せた** —
>   バーだけ消えて文言が宙に浮くのを避けるため (裁定の「非表示条件はタグ 0」の範囲内)。
> - テスト: `PresetSaveMessageTests` 28 件 + `AppCssTokenTests` へ 9 件追加し **204 → 241 件**。
>   追加した機械検査は「左縁が inset で border 幅を動かさない」「リングとの合成形」
>   「`.chip-x` の打ち消し」「親からの `IsTarget` 伝播」「パレットの面表示」
>   「グローが `.tm-bar` 側 (クリップ回帰止め)」「pill が accent 塗り」。

### P3 — 構造に触れる・効果はあるが急がない

| ID | 課題# | 変更内容 | 対象 | 工数 | リスク・可逆性 | 依存 |
|---|---|---|---|---|---|---|
| P3-1 ✅ 2026-08-08 (`feat/ui-p3-scale`) | 15, 23 | ボタン共通化: `.btn` / `.btn-primary` / `.btn-ghost` / `.btn-icon` の 4 クラスへ既存 5 系統 (+P2 で増えた 2 系統) を統合。razor 側のクラス名張り替えを伴うため回帰確認範囲が広い | `app.css`, 各 razor | M | 中 (見た目回帰は目視のみ) | P1-6 |
| P3-2 ✅ 2026-08-08 (`feat/ui-p3-layout`) | 17 | レスポンシブ再構成: 縦積み時の order を 中央→右→左 に変更 (生成→結果→パレットの導線) + 生成ボタン行を `position: sticky; bottom: 0` に | `app.css` | M | 中 (ブレークポイント目視必須) | なし |
| P3-3 ✅ 2026-08-08 (`feat/ui-p3-scale`) | — (§4.4-3) | テレメトリ役割ラベル露出 (**広幅時常時表示**を採用・ホバー案は不採用) | `Generate.razor`, `app.css` | S | 低 | P1-5 |
| P3-4 ✅ 2026-08-08 (`feat/ui-p3-layout`) | 24 | 中央列上限を 400→460px + 右ペイン余白調整。長タグ列での窮屈さ緩和 | `app.css` | S | 低 | なし |
| P3-5 ✅ 2026-08-08 (`feat/ui-p3-history`) | — | 直近生成のミニ履歴 (右ペイン下に直近 4 枚のサムネ・クリックで再表示・**セッション内メモリのみ・永続化しない**)。breadth 境界ぎりぎりなので、P1/P2 完了後にユーザー判断を仰いでから → **仰いだうえで実施 (§1 非目標の唯一の例外)** | `Generate.razor`, `app.css`, `Services/PreviewHistory.cs`, `Services/PreviewLabel.cs` | L | 中 (メモリ保持量・保留可) | P2-2 |

> **P3 バッチ D 実装メモ (2026-08-08 / `feat/ui-p3-scale` / P1-6・P3-1・P3-3)**
>
> **★ 順序が急所: ベースラインを実装より先に書く。**
> P1-6 も P3-1 も「見た目は変えずに宣言を畳む」リファクタなので、**後からテストを書くと
> 期待値が実装のコピーになり検査価値がゼロ**になる。そこで実装前に現行 `app.css` の実効値を
> 凍結し、**いずれもリファクタ前に緑を確認してから**着手した:
> (A) `font-size` / `border-radius` の「セレクタ → 実効 px」表 **67 件** →
> (A2) `padding` / `margin` / `gap` の同型の表 **65 件** (P1-6 着手の直前に追加) →
> (B) ボタン **8 群 × 4 状態** (base / :hover / :disabled / :focus-visible) の宣言集合。
> 併せて (C) 旧クラス名の在庫と (D) 抽出器自身の自己検査を置き、**「まだ無いこと」を見る検査は
> 実装時に「ちょうどこの形であること」へ反転させる**運用にした (件数を定数で固定してあるので、
> 黙って 1 件足すと赤くなる = 増やすときにレビューが入る)。
>
> **P1-6 (タイポ/スペーシング/角丸トークン)**
> - **余白の置換規則は「全成分が 4px グリッド (`0` は中立) の宣言だけ畳む」**。
>   `padding: 4px 0 8px` は畳むが、`padding: 2px 8px` や `6px 10px` は **1 成分も触らない**。
>   混在置換 (`var(--sp-1) 0 10px`) をしないのは、読み手が「トークン化済み = グリッドに乗っている」と
>   誤読する余地を残さないため。半端値はトークン化せず**残っていることが分かる形**で残す。
> - **意図的な「寄せ」は 10 件**で、§4.2 が明示的に認めた形 (font-size 13→12/14・15→14/16・
>   border-radius 8→10) のみ。**ベースライン表の literal は 1 文字も書き換えていない** —
>   表を書き換えると「凍結」の意味が消えるので、寄せは**理由付き許可リスト + 件数固定**の側に置き、
>   連鎖セレクタへ適用する形にした。寄せの正当性が 1 箇所に閉じ、表は常に「着手前の実測値」を指す。
> - **見送り**: `.ps-noimg` の `9px` (スケール外の最小値。`--fs-xs` = 11px にすると 36px 角の
>   サムネ枠に「no img」が収まらない → **PL 申し送り**)、`border-radius` の `3px` / `4px` / `50%`
>   (段ではなく**形**が変わる。6px バーが完全な pill 化する等)。
> - `--sp-6` (24px) は §4.2 の定義どおり `:root` に置いたが、**現状の app.css に該当する余白が無く
>   未使用**。段としての欠番を作らないためにあえて残している (使う場所ができたら素直に使える)。
> - razor は無変更。374 → 447 件・全緑。
>
> **P3-1 (ボタン 8 群 → 4 クラス + 器)**
> - **設計は「4 クラス + 器」**。`.btn` (押せるものの土台・`:disabled` の作法) /
>   `.btn-primary` (accent 塗り) / `.btn-ghost` (面 + 枠) / `.btn-icon` (透明地のグリフ) が
>   **ボタンらしさ**を持ち、**置かれた場所で決まるもの** (padding・位置・並び・font) は
>   器 (`.gen-actions .btn` / `.presetbar .btn` / `.lang-toggle .btn` …) が持つ。
>   器は 2 クラス = 常に部品より詳細度が高いので、順序に依存せず上書きできる。
> - **不揃い 3 つは「差分 0」で畳んだ** (揃えると実際に見た目が変わるので統一しない):
>   ① `:disabled` の `opacity` **2 種** (0.45 / 0.6) → 共通を 0.6 にし、0.45 の 2 群は器側に残す
>   ② hover 作法 **4 種** (枠のみ / 枠 + 文字 accent / 文字のみ / 枠 + 文字 ng) →
>   共通 (`.btn-ghost:hover`) は**枠色だけ**に集約し、文字色は要る群の器側で足す。破壊操作の
>   削除ボタンだけ ng 系なので `.btn-icon:hover` に分離 ③ `font: inherit` → 器側に据え置き
>   (`.btn` に載せると `button` 既定フォントの扱いが全ボタンで変わる)。
> - **同詳細度 (0,3,0) の順序が勝敗を決める箇所が 2 つ**あった。
>   `.btn-ghost:hover` の `border-color` vs 言語トグルの区切り線 (`.lang-toggle .btn + .btn` の
>   `border-left`) / `.lang-toggle .btn:hover` の `color` vs `.on` の `color`。
>   **`.btn` 一族をファイル先頭側に置く**ことで器側と `.on` を後勝ちにして解決し、
>   **順序そのものをテストで固定**した (後ろへ動かすと区切り線が hover で accent に化ける)。
> - **`.btn:disabled` が構文上あたる 3 群** (言語トグル / 削除 / 画像保存) は元々 `:disabled` 規則を
>   持たなかった。「disabled にならないから見た目は不変」という根拠を散文でなく razor 解析で
>   機械保証したところ、**数え間違いが実際に 1 件検出された** (3 群 = 4 要素・disabled を出しうるのは
>   5 要素)。散文のままなら見逃していた。
> - **`.preview-save` は `<a download>`** なので `button:focus-visible` に当たらない。
>   `.btn` を付ける以上リング側にも受け皿が要るので、統一ブロックの `.preview-save:focus-visible` を
>   **`.btn:focus-visible` へ張り替えた** (P2-3 で踏んだのと同じ罠 = a 要素はボタンの疑似クラスに乗らない)。
>   リングの宣言を 1 ブロックに保つ契約 (P1-2) と `--edge` 合成 (P2-6) はそのまま維持。
> - **意図的差分の許可リストは 2 件** (上限 6・残枠 4)。どちらも言語トグルのセグメントに ghost が
>   載って**宣言が 1 つ増えただけ**で描画結果は同一 (角丸 0 の打ち消し / `border: none` ゆえ描かれない枠色)。
> - 447 → 466 件・全緑。
>
> **P3-3 (テレメトリ役割ラベル)**
> - **採ったのは「幅に余裕がある時だけ常時表示」** (§4.4-3 の 2 案のうち後者)。
>   `.tm-role` の既定は `display: none` で通常フローに 1px も入らず、`@media (min-width: 1500px)` で
>   だけ出す。1500px は「4 デバイス分のラベル (計 ~250px) を足してもトップバーの他の要素を
>   押し出さない」目安。これ未満の幅では従来どおり `title` 属性 (ホバー) が受け皿。
> - **採らなかったのは「ホバーでバー下へ出す」案**。`.telemetry-mini` は `overflow: hidden`
>   (トップバーからの溢れ止め) なので、**絶対配置でバーの外へ出したラベルはクリップされて 1px も
>   見えない**。`overflow` を外すと今度は狭い幅でトップバーが溢れる。P2-10 で「グローは
>   `.tm-fill` ではなく `.tm-bar` に出す」と踏んだのと**同じクリップの罠**が、外向きにもう一度出た形。
>   in-flow のまま hover で伸ばす案 (`max-width` 0 → 80px 等) も、hover のたびにバーが横に
>   ずれるので却下。
> - **`.tm-role` は寸法を 1 つも宣言しない** (`display` / `color` / `white-space` のみ)。
>   字は `.tm-item` の `--fs-xs`、間隔は `.tm-item` の `gap` を継承させる。おかげで
>   **新トークンも P1-6 のベースライン表の書き換えも不要**になった (新機能なのに (A)(A2) が
>   1 件も動かないのは、この制約を先に決めたため)。
> - **ラベルは行の末尾** (dev → bar → pct → role)。役割名は長さがまちまちなので、先頭側に置くと
>   4 つ並んだバーの開始位置が item ごとにずれて「4 HW が並んで動く」絵が崩れる。
> - **razor に状態を増やさない**。出す/出さないは CSS だけで決まり、テレメトリの真実源は
>   `_sample` のまま。色は `--muted` (`--dev-*` にすると % と同格になり「データ = 数値 /
>   説明 = ラベル」の階層が消える)。
> - 466 → 486 件・全緑。

> **P3 バッチ E 実装メモ (2026-08-08 / `feat/ui-p3-layout` / P3-4・P3-2)**
>
> **★ 実装順は P3-4 → P3-2。** どちらも `.main` グリッドに触るが、P3-4 は
> **3 ペイン側 (トラック定義)**、P3-2 は**縦積み側 (メディアクエリの中)** の変更で、
> 先に 3 ペインを確定させてから縦積みを重ねると、後段の検査を「メディアクエリの外に
> 縦積み専用の道具が 1 つも無いこと」という**位置の契約**として書ける。
>
> **P3-4 (中央列 400 → 460px + 右ペインの余白調整)**
> - 触ったのは `minmax(340px, 400px)` → `minmax(340px, **460px**)` の**1 数値だけ**。
>   3 トラック構成 (左 320px 固定 / 中央 minmax / 右 1fr) も中央の下限 340px も据え置きで、
>   狭い 3 ペイン時の挙動は完全に不変。検査は「左が 320px」「中央の下限が 340px」
>   「上限が 460px」「右が fr」を**別々の失敗メッセージ**として持つので、どこが動いたか即分かる。
> - **右ペインの余白調整は「枠の幅の頭打ち」**で入れた。右ペインは 1fr =
>   画面が広いほど横に伸びるが、生成画像は正方形 (512/768/1024²) で**高さ律速**なので、
>   伸びるのは**破線枠の中の空白だけ**だった (課題 #24 後半の実体)。
>   `@media (min-width: 1101px) { .preview { max-width: calc(100vh - 104px) } }` で
>   枠の幅を「枠の高さの概算」で止め、余った幅はペインの地 (`--panel`) へ逃がす。
>   画像は `.canvas` の `justify-content: center` で中央に留まったまま (位置は動かさない)。
>   ブレークポイントは縦積みの 1100 + 1 で**隙間なく接する** (どちらにも入らない幅を作らない)。
> - **104px は概算だが、外れても壊れない**のが採用理由。トップバー (~39) + `.main` の
>   上下 padding (32) + `.canvas` の枠と上下 padding (34) の合計で、少し小さければ枠が
>   数 px 縦長に、大きければ空白が数 px 残るだけ。画像は `object-fit: contain` のままなので
>   **どちらに外れても画像は正しく中央に収まる** (退行が起こりえない形を選んだ)。
> - **不採用①: `aspect-ratio: 1 / 1` で枠を正方形にする案**。数値が厳密で概算値も要らないが、
>   成立には `flex: 1; width: 100%` を `flex: 0 1 auto; width: auto; min-width: 0` へ
>   組み替える必要があり、枠の基準寸法が **flex-basis の解決** (definite height からの
>   ratio 転写) に依存する。外すと**デスクトップの主画面**で枠が内容幅まで潰れる。
>   PL 裁定の第一条が「デスクトップ非退行が最重要」で、かつ手動確認は未実施 (§7 台帳が全部「未」) の
>   状況で、目視できない賭けはしない — という判断。`max-width` だけなら flex の計算に
>   1 つも触らない。
> - **不採用②: `.canvas` の内側余白 (16px) を詰める案**。いちばん素直な「余白調整」だが、
>   バッチ D の (A2) 余白ベースライン表は **「余白に寄せの許可リストは無い・1px でも動かさない」**
>   と明記して凍結してある。1 行書き換えれば通るが、それは凍結の意味を自分で薄める行為なので、
>   **表を触らずに済む形 (max-width) を選んだ**。実際、本バッチは (A)(A2) を 1 行も動かしていない。
>
> **P3-2 (レスポンシブ再構成)**
> - 縦積みの並びを `order` で **中央 (1) → 右 (2) → 左 (3)** = 生成 → 結果 → パレットへ。
>   DOM は `aside.sidebar → section.gen → section.canvas` のままで **razor は無変更**。
> - **検査は値ではなく大小関係で書く** (`gen < canvas < sidebar`)。番号を詰め直しても
>   意味が変わらない限り緑のまま = テストが実装のコピーにならない。
> - **★ 踏んだ罠: `position: sticky` は「いちばん近いスクロール容器」に貼り付く。**
>   `.gen` は `overflow-y: auto` を持つ = それ自体がスクロール容器なので、素直に
>   `.gen-actions { position: sticky; bottom: 0 }` と書いても**永久に発動しない**。
>   縦積みの `.gen` は高さが内容ぴったり (自分ではスクロールせず、スクロールするのはページ) で、
>   sticky の基準になるスクロールポートが動かないため。**CSS 的には完全に正しいのに効かない**
>   という、目視でしか気づけない類の失敗。解決は縦積みブロックで `.gen { overflow-y: visible }` に
>   戻し、スクロール容器をビューポートへ返すこと (`.sidebar` が既にやっていたのと同じ処置)。
>   この 1 行が消えたら赤くなる検査 (`[scroll]`) を独立で置いた。
> - **sticky 行には地と上境界が要る** (`background: var(--panel)` + `border-top`)。
>   透明のままだと裏を流れるチップ列と重なって判読できない。地は器 (`.gen`) と同じ
>   `--panel` なので、貼り付いていても継ぎ目が見えない。**新トークンはゼロ**。
> - **余白は 1 つも足さない**。sticky 行に `padding` を足したくなる (上の内容と密着するため) が、
>   足すと (A2) の表の書き換えが要る = 裁定事項。行の外周は `.gen` の `padding: 16px` が
>   そのまま担い、上境界の 1px で切れ目を作る形にした。
> - **代償として記録**: ① 視覚順と DOM 順 (= Tab 順) がずれる。DOM を組み替えれば揃うが、
>   それは 3 ペイン時の Tab 順 (左 → 中央 → 右) を壊す取引なので採らない。
>   ② 貼り付いている間、ボタン行の**直下**にある「押せない理由」(`.gen-reason`) と
>   エラー (`.error`) は画面外へ出る。理由テキストは押せないときだけ出る 1 行で、
>   少しスクロールすれば自然位置で読める。行を包む器を新設して理由ごと貼り付ける案は
>   razor 構造の変更 + 新クラス + 余白が要るので breadth として見送った。
> - **不採用: `.main` 自体をスクロール容器にする案** (`.main { overflow-y: auto }`)。
>   sticky の基準は安定する代わりに、縦積みでページ全体ではなく内側だけがスクロールする
>   二重スクロールになり、モバイル系ブラウザのアドレスバー挙動とも噛み合わない。
>
> **検査 (486 → 529 件 = +43)**
> - `ui.Tests/AppCssLayoutTests.cs` (`AppCssTokenTests` の partial として既存 CSS パーサを共有)。
>   **デスクトップ非退行を「位置の契約」で機械保証**する: `order` も `position: sticky` も
>   **縦積みメディアクエリの外に 1 つも無い**こと (トップレベルと他クエリを全走査)、
>   縦積みブロックが触るセレクタは 5 つちょうど、トップレベルの `.gen-actions` は
>   `display: flex` + `gap` だけ、`.gen`/`.sidebar` はデスクトップでは自前スクロールのまま。
> - **変異検査は 2 段構え**。① 出荷形を写した合成 CSS を 1 箇所ずつ壊す **19 ケース**
>   (sticky を外へ / order を外へ / DOM 順のまま / 右が中央より前 / order 欠落 /
>   sticky 消失 / 貼り付く辺なし / 上端貼り付き / 地なし / 上境界なし / スクロール容器を
>   解除しない / 上限 400px のまま / トラック 2 本 / 右が固定幅 / 縦積みが 1 カラムでない /
>   頭打ち消失 / 境界が重なる / 素の伸びを殺した / 余白が生えた)。
>   ② **本物の `app.css` に 1 箇所だけ変異を入れる 6 ケース** — 合成 CSS だけだと
>   「出荷ファイルを読めていないのに緑」がありうるため。
> - 実地確認: 出荷 `app.css` の sticky 宣言を**物理的にメディアクエリの外へ移して**
>   `dotnet test` すると **7 件が赤** (`[sticky-scope]` を含む) になることを確認済み。

> **P3 バッチ F 実装メモ (2026-08-08 / `feat/ui-p3-history` / P3-5)**
>
> **★ 位置づけ: §1 非目標「機能追加はしない」の唯一の例外で、ユーザー決裁のうえ実施した。**
> 認められた境界は **4 枚 / 非永続 / 削除・並べ替えなし / サムネごとの保存導線なし**。
> 境界は散文ではなく**機械検査**で保っている (下の「breadth の錠前」)。
>
> **① 履歴が持つのは base64 だけ — byte[] を持たせない (実測ベースのメモリ見積)**
> - `PreviewItem` は `(Id, Base64, Badge, DownloadName, CreatedAt)` の 5 値で、**配列を 1 つも持たない**
>   (`PreviewItem_HoldsNoRawBytes` が型レベルで縛る)。
> - **見積 (実測ベース)**: PNG 1 枚は 1024² で概ね **1.2–1.4MB** (重い絵で 2MB 前後)。
>   .NET の `string` は **UTF-16 = 1 文字 2 バイト**なので、base64 文字列は元 PNG の
>   4/3 × 2 = **約 2.67 倍**。よって 1 枚あたり base64 は **3.3–3.8MiB**、
>   **4 枚で 13.1–15.2MiB** (重い側 2MB × 4 で **21.3MiB**)。
>   ※ PL メモにあった「両方持つと 2.33 倍」は **2.67 倍の誤り**で、前半の報告で訂正済み
>   (base64 の 4/3 に UTF-16 の 2 倍を掛け忘れていた)。
> - **常駐量だけを見るなら `byte[]` のみが最小**なのは事実 (4 枚で ~5MiB)。それでも base64 を選んだのは
>   **毎描画コスト**のため: 表示は `<img src="data:image/png;base64,…">` なので、`byte[]` だけ持つと
>   **描画のたびに 4 枚分を base64 化**することになる。生成中は経過秒タイマーで **1 秒ごとに再描画**が
>   走るので、これは **13–15MiB/秒の新規確保**を意味する。しかも 1 枚 3.3MiB の文字列は
>   **LOH 閾値 85,000 バイトを大きく超える**ので**全部 LOH 直行** = 圧縮されない領域が毎秒荒れる。
>   常駐 15MiB を許容して**毎秒のアロケーションをゼロにする**方が明確に安い。
> - 逆に「両方持つ」(base64 + byte[]) は常駐が ~20MiB になるうえ、**生バイトが要るのは
>   プリセット保存の瞬間の 1 枚だけ**なので割に合わない。**decode は選択時に 1 回**
>   (`ShowResult` の中・`Decode_HappensOnlyInsideShowResult` が 1 箇所であることを縛る)。
> - 代償として、生成成功時に「`png` → base64 → もう一度 `byte[]`」の**往復が 1 回**入る。
>   1 生成あたり 1 回・数 ms で、20 秒かかる生成に対して無視できる。**経路を 1 本に保つ対価**として払う。
>
> **② `ShowResult(PreviewItem)` funnel — 分離代入を構文レベルで禁じる**
> - 表示中の絵に紐づく 4 フィールド `_imageData` (絵) / `_imageBytes` (プリセットのサムネ元) /
>   `_lastMode` (モードバッジ) / `_downloadName` (PNG 保存名) は、**常に同じ 1 回の生成を指す 1 組**。
>   バラバラに代入できる形のままだと **F 最大の事故**「古いサムネを押したら**絵だけ**戻って
>   バッジは本番のまま・PNG 保存が別の生成のファイル名」が起きる。しかも**絵は正しく戻るので
>   目視では成功に見える** — 気づけない類の壊れ方なので、散文の約束ではなく
>   「4 つへの代入は private メソッド `ShowResult` の中にちょうど 1 つずつ」という
>   **構文の制約**にした (F-1)。呼び出し元は **生成成功時**と**サムネクリック時**の 2 箇所だけ。
> - **不採用: `_imageData` を `_selected` の投影に変える全面リファクタ** (PL 裁定)。
>   確かに「1 組」は型で表現できるが、P2-2 の**減光保持** (生成中も前回画像を消さない)・
>   **未生成 null 状態**・`_lastMode` の**成功時のみ更新**という契約が 4 フィールドに乗っており、
>   L 項目の最後で土台を入れ替えるのは事故率に見合わない。**同じ不変条件は funnel + 機械検査で
>   等価に得られる**ので、得るものが同じなら安い方を採る。
>
> **③ 罠: バッチ E の縦寸法契約と正面衝突する (104 → 176px)**
> - E は `@media (min-width: 1101px) { .preview { max-width: calc(100vh - 104px) } }` で
>   右ペインの余白を解いていた。**104 は「.preview の実高さの概算」**なので、
>   `.canvas` の中にサムネ列を 1 行足すと**その分だけ枠が縦にはみ出す**。
> - 裁定どおりサムネ列は **`.canvas` の中・`.preview` の外**に置き、`.canvas` を
>   `flex-direction: column` にして枠の下へ敷いた。そのうえで **N を 104 → 176 (= 104 + 72)** へ更新。
>   72 は `.thumb` の外寸 (`box-sizing: border-box` なので `height` がそのまま列の高さ)。
> - **E の検査意図は保たれている**。E が守っていたのは「デスクトップでプレビュー枠が
>   **縦にはみ出さない**」ことで、`104` という数値そのものではない (実際、E のテストは
>   **どこにも 104 をハードコードしていなかった** — 合成 CSS の写しに literal があるだけで、
>   判定器が見るのは「頭打ちが `min-width` クエリ 1 つにある / 境界が縦積みと隙間なく接する /
>   寸法を持たない」の 3 点)。今回は**その意図を数式として明文化**し、
>   `N == 104 + .thumb の height` を機械で固定した (F-8)。サムネの寸法を変えたのに N を
>   据え置くと赤くなる = **意図の方が前より強く守られている**。
> - `.canvas` を縦並びにしても見た目は不変。`.preview` は `flex: 1` + `width: 100%` なので、
>   横中央は `align-items: center` が、縦の占有は `flex: 1` が引き継ぐ。履歴が空ならサムネ列ごと
>   出ないので、**未生成状態の絵は完全に従来と同一**。
> - **不採用: `position: absolute` で画像に重ねる案** (PL 裁定)。**生成物を隠す**ので論外。
>   `.thumbs` / `.thumb` が `position` を持たないことを機械で縛った (`[overlay]`)。
> - **不採用: `:has()` で「履歴がある時だけ 176」と出し分ける案**。`.preview` の頭打ちが
>   2 ブロックになり、E の「頭打ちは 1 つだけ」契約 (`Single`) を壊す。履歴が空のときは枠が
>   72px ぶん縦に余るが、E が書いたとおり **概算が外れる方向は「壊れない」側**なので許容した。
>
> **④ 寸法トークンを 1 つも使わない = P1-6 のベースライン表が 1 行も動かない**
> - サムネ列は `font-size` / `border-radius` / `padding` / `margin` / `gap` を**1 つも宣言しない**。
>   結果として (A) 字送り・角丸表 (36 + 29) も (A2) 余白表 (65) も**件数・値ともに完全に不変**。
>   P3-3 の `.tm-role` と同じ通し方で、これは**制約を先に決めた**から成立している
>   (後から詰めると必ず表が動き、凍結の意味が薄れる)。
> - そのため**サムネ間の間隔は `gap` ではなく枠 (`border`) で作る**。`.thumb` に
>   `border: 4px solid var(--panel)` (右ペインの地と同色) を敷くと、隣り合うサムネの間に
>   8px ぶんの「地」が見え、プレビュー枠との間にも 4px の切れ目ができる。
>   選択中はこの枠の**色だけ**を `--accent` に差し替える → **新トークンはゼロ**
>   (`:root` が 36 トークンちょうどという検査も無傷)。
> - **`all: unset` を使った理由** (ここは自分で説明しておく必要がある): サムネは `<button>` なので
>   UA 既定の `padding: 1px 6px` / `border: 2px outset` / `appearance` が付く。放置すると
>   **中身の寸法がブラウザごとに数 px ずれ**、上の「外寸 72px」の足し算 (F-8) が崩れる。
>   かといって `padding: 0` と**書くとそれだけで余白表に 1 行増える**。
>   `all: unset` は**余白表に現れない 1 宣言**で UA 既定だけを落とせるので、これを採った
>   (`*` の `box-sizing` も一緒に落ちるため `border-box` は明示で戻す)。
>   **表を回避するための小細工ではない** — `padding: 0` と書いても実効値は同じ 0 で、
>   違うのは「アプリ側の余白設計として表に載せるか / UA 既定のリセットとして扱うか」だけ。
>   後者が実体なので後者の書き方を選んだ、という判断。
> - `--sp-6` (定義のみ未使用) をサムネ列の余白に使う案は、上記のとおり**そもそも余白を
>   宣言しない**設計になったので出番が無かった (使うために設計を曲げない、の裁定どおり)。
>
> **⑤ 挙動の裁定 (実装したとおり)**
> - **生成中** (`_busy`): 履歴に追加しない・**クリック無効** (`disabled` + ハンドラ冒頭の
>   `_busy` ガードの二重)。完了時に上書きされるだけの操作を受け付けると、P2-2 の減光対象
>   (今なにと見比べているのか) が入れ替わってしまう。
> - **生成失敗**: 履歴に積むのは `try` の**成功経路 1 箇所だけ** (F-2)。表示中の画像も履歴も無変化
>   = P2-2 の「失敗しても直前の絵が消えない」が履歴側にも一貫する。
> - **ページ離脱 / 回線切断**: circuit と一緒に消える。`DisposeAsync` での明示クリアは**書かない**
>   (フィールドは circuit の寿命そのもので、明示クリアは何も早めない)。
>   **再接続 (circuit 再作成) で履歴が消えるのは仕様**。
> - **空**: サムネ列ごと非表示 (プレースホルダも件数バッジも出さない)。
>
> **⑥ breadth の錠前 (この節がいちばん効く)**
> - `PreviewHistory` の **public メンバ集合の完全一致**検査 (`Capacity` / `Add` / `Find` / `IsSelected`)。
>   `Clear` / `Remove` / `Pin` / `Save` を 1 本足した瞬間に赤くなる。入れ子型の抜け道も塞いである。
> - razor 側は **F-5 (永続化ゼロ)** と **F-6 (削除ボタン相当・`draggable`・サムネごとの
>   `download` が 0 件)**。とくに `download` は「PNG 保存は画面に 1 つのまま」という境界の実体で、
>   バッチ D の既存検査 (`Generate.razor` 中の `download=` を持つ開始タグは 1 つ) と**二重の錠**になっている。
> - 実装中に 1 度踏んだ: **`PreviewHistory.cs` のコメントに「localStorage はやらない」と
>   書いてある**のを永続化として検出して赤くなった。以後、永続化の走査は**コメントをマスクしてから**
>   行う (razor 側も同じ理由で `@code` をマスクしてから読む)。
>
> **検査 (618 → 687 件 = +69。バッチ F 全体では 529 → 687 = +158)**
> - 前半 (純ロジック・529 → 618): `PreviewHistoryTests` **57 件** + `PreviewLabelTests` **32 件**。
> - 後半 (結線・618 → 687): `ui.Tests/PreviewHistoryRazorTests.cs` **69 件**
>   (`AppCssTokenTests` の partial として既存の CSS / razor パーサを共有)。
>   razor の `@code` を**文字列・文字・コメントをマスクしてから**波括弧で切り出す小さな
>   C# スキャナを足し、代入 (`=` / `??=` / `+=`) と比較 (`==` / `!=`) を区別して数える。
> - **変異検査は 2 段構え**。① 出荷形を写した合成 razor / CSS を 1 箇所ずつ壊す **24 ケース**
>   ② **本物の `Generate.razor` に 1 箇所だけ変異を入れる 7 ケース**。
> - 実地確認: 出荷 `Generate.razor` に `_lastMode = PreviewLabel.Build(draft, sendSize);` を
>   **物理的に ShowResult の外へ 1 行足して** `dotnet test` すると **3 件が赤**
>   (`[funnel] _lastMode への代入が 2 箇所` / `ShowResult の外に 1 箇所ある` を含む)。
>   同様に出荷 `app.css` の頭打ちを **176 → 104 へ戻す**と **2 件が赤** (`[cap]`)。
> - **既存 618 件のうち書き換えたのは 1 件だけ** (`Generate_BuildsTheModeLabelThroughPreviewLabel`)。
>   `_lastMode = PreviewLabel.Build(…)` という literal を見ていた検査で、funnel 化で
>   代入の場所が変わったため。**弱めず強めて引き継いだ** (呼び出しが razor 全体で
>   ちょうど 1 箇所であることを追加)。E の `.preview` 期待値は**出荷ファイル側には
>   ハードコードが無く**、合成 CSS の写しの literal を 104 → 176 に揃えただけ (判定器は数値を見ない)。

---

## 6. 段階適用の順序

1. ✅ **P1-1 (トークン改訂) が筆頭** (2026-08-08 完了)。`:root` 差し替え + 直書き置換だけで境界・muted・チップ・
   エラー色が全画面に効く。挙動無変更・1 コミット・revert 容易。
2. ✅ **P1-2〜P1-4 (フォーカス/placeholder/境界)** (2026-08-08 完了) — 高深刻度 #3/#4 をコードロジックに触れず解消。
3. ✅ **P1-5 (テレメトリのデバイス色)** (2026-08-08 完了) / ✅ P2-10 (生成中強調・2026-08-08 完了・
   `feat/ui-p2-polish`) — dollama らしさの即効改善。
   見た目の変化が最も分かりやすく、ユーザーの体感リターンが早い。
   ※ P1-7 (エラー UI ダーク化・再接続モーダル日本語化) も同時に完了させた (依存なし・独立)。
4. ✅ **P2-1 (無言失敗の根絶)** (2026-08-08 完了・`feat/ui-p2-gate`) — 挙動変更の第一弾。
   UX 深刻度は最高だが razor ロジックに触れるため P1 の安定後に。ui.Tests を同時に追加。
   ※ 同じ「生成ボタンの活性」に触れる P2-9 (未接続 disabled + 再接続) / P2-8 (Ctrl+Enter) と、
   同じ keydown を触る P2-5 (IME ガード) を**同一バッチ (P2 バッチ A)** でまとめて実装した
   — 判定を `GenerateGate` 1 箇所に集約するには同時に入れるのが安全なため。
5. ✅ **P2-2/P2-3 (プレビュー保持・保存)** (2026-08-08 完了・`feat/ui-p2-preview` = P2 バッチ B) →
   残りの P2 を小さく順次。※ 同じプレビュー節に触る P2-3 と、独立だが 1 行で済む P2-4
   (LoRA スライダ即時反映) を同一バッチでまとめた。
   ✅ **P2-6/P2-10/P2-7 (磨き 3 点)** も完了 (2026-08-08・`feat/ui-p2-polish` = P2 バッチ C)。
   ※ P2-6 と P2-10 は「どちらも状態を色で面に出す」CSS 系で、`box-shadow` の扱い
   (詳細度・クリップ) を一度に見切る方が安全なため同一バッチにした。
   → **これで P2 は全 10 項目クローズ。残るは P1-6 と P3 のみ。**
6. ✅ **P1-6/P3-1 (タイポトークン→ボタン統合)** (2026-08-08 完了・`feat/ui-p3-scale` = P3 バッチ D)。
   リファクタ性格なので、機能系 P2 が落ち着いてから**まとめて**実施した (P1 実装時も P1-6 は
   意図的に見送り、P3-1 と一緒に扱う方針を維持 — タイポ/スペーシングを先に畳んでもボタン 5 系統の
   重複が残ると二度手間になるため)。**着手前にリファクタ前ベースラインを凍結する**手順を挟んだ
   (詳細は上の「P3 バッチ D 実装メモ」)。
   ※ 同バッチで ✅ **P3-3 (テレメトリ役割ラベル)** も入れた — P1-5 のデバイス色に乗るだけの
   小粒 CSS で、テレメトリの見た目に触る変更をボタン統合と一度に目視できるため。
   → **これで P1 は全 7 項目・P3 は 3/5 クローズ。残りは P3-2 (レスポンシブ) / P3-4 (中央列幅) /
   P3-5 (履歴) で、P3-5 は着手前にユーザー確認。**
7. ✅ **P3-4/P3-2 (中央列幅 → レスポンシブ再構成)** (2026-08-08 完了・`feat/ui-p3-layout` = P3 バッチ E)。
   どちらも `.main` グリッドに触るので**同一バッチ**にし、**P3-4 → P3-2 の順**で入れた
   (3 ペイン側のトラック定義を先に確定させると、縦積み側は「メディアクエリの中だけに閉じる」
   という位置の契約として検査できる)。詳細は上の「P3 バッチ E 実装メモ」。
   → **残るは P3-5 (ミニ履歴) の 1 件のみ。breadth 境界ぎりぎりなので着手前にユーザー判断を仰ぐ。**
8. ✅ **P3-5 (直近生成のミニ履歴)** (2026-08-08 完了・`feat/ui-p3-history` = P3 バッチ F)。
   §1 非目標の唯一の例外として**ユーザー決裁のうえ**実施した。**前半 (純ロジック) → 後半 (結線)** の
   2 段で入れており、前半で `Services/PreviewHistory.cs` (並び替え・容量・選択判定) と
   `Services/PreviewLabel.cs` (モードバッジ文言・razor の `ModeBadge` から昇格) を**先に**
   純クラス化して全数検査を緑にし、後半で razor / CSS へ結線した。
   razor に持ち込むロジックを先に外へ出しておくと、結線側の検査が
   「**どこで代入するか**」という位置の契約だけに集中できる (バッチ D/E と同じ手)。
   詳細は上の「P3 バッチ F 実装メモ」。
   → **これで P1 (7) + P2 (10) + P3 (5) = 全 22 項目クローズ。実装バックログは空。**

各段階で `dotnet build ui/Dollama.Ui.csproj` + `dotnet test ui.Tests` 緑を確認してからマージする。

---

## 7. 検証方法

### 自動 (ui.Tests / xUnit)

- **P1 (実装済・依存追加ゼロ / bUnit 不使用)** — 2 ファイル追加。CSS/razor をテキストとして読み、
  壊れると気づきにくい性質だけを機械検査する。
  - `ui.Tests/AppCssTokenTests.cs` (28 件): ① `:root` に既存 7 + 新設 13 のトークンが定義済で値が空でない
    ② **コントラスト比を hex 完全一致ではなく WCAG 相対輝度で計算**して下限を守る (`--text`/`--panel` ≥ 7.0、
    `--muted` ≥ 4.5、`--on-accent`/`--accent` ≥ 4.5、`--ng-soft` ≥ 4.5、`--dev-*` 4 色 ≥ 4.5、
    `--border-strong`/`--panel` ≥ 2.0、`--border`/`--panel` ≥ 1.4)。値一致は単なる変更検知器になるため採らない
    ③ 半透明トークン 4 種が `rgba()` としてパースでき α が (0,1) ④ **`:root` 外に直書き色ゼロ** (hex / rgba とも・許可リストなし)
    ⑤ `app.css` + scoped CSS 2 本の `var(--x)` が全て `:root` 定義済 (かつ scoped に `:root` を書いていない)
    ⑥ フォーカス統一リングが 1 ブロックで 5 セレクタ全部を覆い `.chip-entry` の二重リング打ち消しがある
    ⑦ `::placeholder` が `color: var(--muted)` + `opacity: 1` ⑧ `.tm-cpu/npu/igpu/gpu` が各 `--dev-*` を参照し
    `Generate.razor` が `DeviceStyle.CssClass` を呼ぶ ⑨ P1-7 の回帰止め (app.css に `#blazor-error-ui` が無い・
    scoped が `lightyellow`/`color-scheme: light only` でない・`ReconnectModal` の Blazor JS 契約 id/class が全部残っている)。
  - `ui.Tests/DeviceStyleTests.cs` (17 件): `DeviceStyle.CssClass` の 4 デバイス正常系・大小文字非依存・
    未知/null/空は `""` (既定グラデへフォールバック)。
- **P2-1 / P2-8 / P2-9 (実装済 2026-08-08 / 依存追加ゼロ・bUnit 不使用)** — 2 ファイル追加。
  razor から純クラスへ切り出した「確定規則」と「ゲート判定」を検証する。
  - `ui.Tests/TagCommitTests.cs` (20 件): `Normalize` (trim/lowercase・null 安全・語中の空白は保持)、
    `Split` (カンマ展開・空除去・入力内重複を先勝ちで除去・順序保持)、`Merge` (既存列への連結・
    **常に新インスタンス**を返し `current` を Mutate しない・重複判定は正規化後だが既存表記は温存・
    Count 差で「増えたか」が判る)。
  - `ui.Tests/GenerateGateTests.cs` (24 件): `Evaluate` の **16 組合せ全数** (busy × connected ×
    タグ 0/1 × draft 空) + 理由の優先順位 (busy > 未接続 > プロンプト空) + 「draft のみでも
    enabled」の単独固定 + タグ数の境界 (負値は 0 扱い) + 理由文言が空でなく互いに異なること。
- **P2-2 / P2-3 (実装済 2026-08-08 / 依存追加ゼロ・bUnit 不使用)** — 2 ファイル追加。
  razor に残るのはタイマーと markup だけで、テストするのは切り出した純ロジック 2 つ。
  - `ui.Tests/ElapsedFormatTests.cs` (31 件): `Mmss` の通常系 (0/1/59/60/3599 秒)・60 分超も
    分として伸びる (3661 → `61:01`)・**100 分以上は `99:59` で頭打ち** (`TimeSpan.MaxValue` でも
    落ちない)・負値は `00:00` (`TimeSpan.MinValue` 含む)・端数切り捨て (12.9 秒 → `00:12`)・
    形式の不変条件 (常に 5 文字・数字とコロンのみ・秒欄は 0–59)・カルチャ非依存・単調非減少。
  - `ui.Tests/DownloadNameTests.cs` (38 件): `ForPng` が `dollama_{yyyyMMdd_HHmmss}_{size}.png` に
    なること・size の正規化 (`1024X1024` / 空白入り)・読めない size は `unknown`・
    **パス区切りと `Path.GetInvalidFileNameChars()` が絶対に混入しない** (`../../etc/passwd` 等 12 種)・
    出力は常に `[A-Za-z0-9_.]+`・日時のゼロ埋めとカルチャ非依存 (仏暦/ヒジュラ暦ロケールでも不変)・
    秒違いで名前が変わる・例外を投げない。
- **P2-6 / P2-10 / P2-7 (実装済 2026-08-08 / 依存追加ゼロ・bUnit 不使用)** — 1 ファイル追加 +
  `AppCssTokenTests` へ 9 件追記。
  - `ui.Tests/PresetSaveMessageTests.cs` (28 件): `Build` の **4 通り全数** (overwrite × hasThumb)・
    上書きとそうでない文言が**常に区別できる**こと (課題 #19 の核心・退化検知)・サムネ有無の区別・
    名前の trim と「」囲み (語中の空白は保持・日本語/記号はそのまま)・空/null でも落ちない・
    形式の不変条件 (「」が 1 組・「保存しました」で終わる)・カルチャ非依存・決定性。
    加えて呼び出し側 `TagPresetField.razor` のテキスト検査 1 件
    (保存バーの条件が `Tags.Count > 0` / 文言が純クラス経由 / 同名判定が `Presets.All(Kind)` 読みだけ)。
  - `AppCssTokenTests` 追記 (9 件): 追加先の左縁が **inset の box-shadow で border 幅を動かさない**こと・
    フォーカスリングとの**合成形** (`var(--edge), 0 0 0 2px var(--focus-ring)`)・`.chip-x` での
    `--edge` 打ち消し・`IsTarget` が**親から降りている**こと (子が自前判定を持たない)・
    パレットの favorites 面表示・生成中グローが **`.tm-bar` 側** (`overflow: hidden` クリップの回帰止め)・
    デバイス色の使用・「生成中」pill が accent 塗りで**テレメトリの `Generating` 駆動**であること。
- **P1-6 / P3-1 / P3-3 (実装済 2026-08-08 / P3 バッチ D / 依存追加ゼロ・bUnit 不使用)** —
  2 ファイル追加 (`AppCssTokenTests` の partial として同じ CSS/razor パーサを共有する)。
  **リファクタ前に凍結 → 実装時に検査を反転させる**運用なので、件数が定数で固定してある箇所は
  「黙って 1 件増やすと赤くなる」= 増減にレビューが入る。
  - `ui.Tests/AppCssScaleBaselineTests.cs` (241 → 374 = **133 件**をステップ 1 で追加。うち
    P1-6 のトークン検査 + 余白表で **+73 件** (→ 447)、P3-1 の張り替え全数で **+19 件** (→ 466)):
    **(A)** `font-size` / `border-radius` の「セレクタ → 実効 px」表 (凍結時 67 件 → P3-1 で
    ボタンの radius 2 宣言が畳まれ 65 件)。`var()` は `:root` 解決後の値で比較するので、
    P1-6 の置換は表に対して**透明**。表に無い宣言が増えた / 表の行が消えた場合も落ちる
    **(A2)** `padding` / `margin` / `gap` の同型の表 (65 件) + 「全成分が 4px グリッドの宣言は
    必ず `var(--sp-*)` へ畳まれていること」の取りこぼし検査。余白に寄せの許可リストは無い
    **(A3)** 寸法トークンが §4.2 の **12 段ちょうど**であること (`--fs-xxs` 等を勝手に増やすと赤)・
    単調増加・`--sp-N` = N × 4px・参照が段から外れていないこと・ペイン見出しがセクションヘッダ型
    **(A4)** 寄せの許可リストが**レビュー済みの 10 件ちょうど**で、宛先が表に実在し `From` が
    表の値と一致し、§4.2 が認めた形 (13→12/14・15→14/16・8→10) しか含まないこと
    **(B)** ボタン 8 群 × 4 状態 (base / :hover / :disabled / :focus-visible) の宣言集合を
    後勝ち合成で突合 (32 件)。意図的差分の許可リストは **2 件ちょうど・上限 6**
    **(C)** 旧クラス 7 系統が app.css からも razor からも**完全に消えた**こと (クラストークンとして解析)・
    `.btn` 一族が**この 4 クラス・この規則だけ**を持つこと (`.btn-lg` 等を足すと赤)
    **(C2)** 張り替え漏れの全数検査: `.btn` を持つ要素は**必ずバリアント 1 つ** (0 個 = 素の button に
    落ちる / 2 個 = 後勝ちで不定)・9 要素それぞれの宛先バリアント・`<a download>` のリング受け皿・
    「`:disabled` 規則が無かった 3 群に disabled 属性が付いていない」こと・**同詳細度 2 組の記述順**・
    `.btn` が `--edge` 合成に割り込まないこと
    **(D)** 抽出器そのものの自己検査 (合成 CSS で `var()` 畳みが透明・1px 差を検出・後勝ち合成・
    接頭辞の誤検出・razor のラムダ `=>` と入れ子引用符をまたぐタグ解析・変異を入れると本当に赤くなること)。
  - `ui.Tests/TelemetryRoleTests.cs` (**20 件** / 466 → 486): P3-3 の 3 性質を機械保証する。
    ① **`Role` が title 属性以外の DOM に出ている**こと (razor から開始/終了タグと razor コメントを
    落とした「描画テキスト」に `@d.Role` が現れる。title も狭幅の受け皿として残っていること)
    ② **表示制御が CSS だけで完結**していること (クラスは静的 1 語・`@if` で包まれていない・
    `@code` に `Role` / `tm-role` が 1 文字も無い = razor に状態を増やしていない)
    ③ **レイアウトが動かない**こと (メディアクエリ**外**の `.tm-role` は `display: none` 1 件のみ・
    露出は **`min-width` のメディアクエリ 1 つだけ**でしきい値 ≥ 1200px・`.tm-role` は
    `font-size`/`padding`/`margin`/`width`/`position` 等の**寸法を 1 つも持たない**・
    (A)(A2) のベースライン表に `.tm-role` が 1 行も現れない)。
    加えて P2-10 の回帰止め (既定グラデ `linear-gradient(90deg, var(--accent), var(--dev-npu))`・
    `.tm-bar` の `overflow: hidden` と 64px・生成中規則がラベルへ伸びていない・`--edge` に触らない)、
    **`:root` のトークンが 36 個ちょうど**であること (新トークン追加は PL 裁定事項なので
    「増やさない」向きの検査を配色側にも用意した)、`@media` を条件と中身に分けるスキャナの
    自己検査、そして**変異検査 7 ケース** (常時表示 / メディアクエリ無し / `max-width` で露出 /
    低すぎるブレークポイント / 出ないメディアクエリ / 寸法を持った / 規則ごと消えた) が
    **全部赤くなる**こと = 検査が空振りでないことの証明。
- **P3-4 / P3-2 (実装済 2026-08-08 / P3 バッチ E / 依存追加ゼロ・bUnit 不使用)** —
  1 ファイル追加 (`AppCssTokenTests` の partial として同じ CSS パーサを共有)。
  - `ui.Tests/AppCssLayoutTests.cs` (**43 件** / 486 → 529): レイアウトの 3 性質を機械保証する。
    ① **デスクトップ非退行 (最重要)** — `order` も `position: sticky` も**縦積みメディアクエリの
    外に 1 つも無い**こと (トップレベル + 他のメディアクエリを全走査する専用スキャナで判定)・
    縦積みブロックが触るセレクタは **5 つちょうど** (`.main`/`.gen`/`.canvas`/`.sidebar`/`.gen-actions`)・
    トップレベルの `.gen-actions` は `display: flex` + `gap` だけで位置も地も枠も持たない・
    `.gen`/`.sidebar` はデスクトップでは `overflow-y: auto` (自前スクロール) のまま
    ② **縦積みの導線** — `order` が **中央 < 右 < 左**の**大小関係**であること (値の一致では
    見ない = 番号を詰め直しても壊れない)・sticky なのは `.gen-actions` 1 件だけで `bottom: 0`
    (上端貼り付きでない)・**地 (`--panel`) と上境界を持つ** (透明だと裏の要素と重なって読めない)・
    **`.gen` の `overflow-y` が縦積みで解除されている** (これが無いと sticky が永久に発動しない罠)
    ③ **P3-4 のトラックと右ペイン** — 3 トラック構成のまま左 320px / 中央 minmax の**下限 340px 据え置き・
    上限 460px**・右は fr・縦積みは 1 カラム・プレビュー枠の頭打ちは `min-width` クエリ 1 つだけで
    しきい値が縦積みの境界 + 1 (隙間なく接する)・トップレベルの `.preview` は素の伸び
    (`flex: 1` / `width: 100%`) を保つ。
    加えて **(A)(A2) ベースライン表の防衛** (`.gen-actions`/`.main`/`.gen`/`.canvas`/`.sidebar`/`.preview` の
    余白・寸法宣言の件数を直接固定 = 新規則が表を 1 行も動かしていないことの明示)、
    そして**変異検査 25 ケース** — 合成 CSS を 1 箇所ずつ壊す **19 件** + **本物の `app.css` に
    1 箇所だけ変異を入れる 6 件** (sticky を外へ / order を外へ / 上限を 400px へ戻す /
    スクロール容器を解除しない / 上境界を消す / 頭打ちを消す) が**全部赤くなる**こと。
- **P3-5 (実装済 2026-08-08 / P3 バッチ F / 依存追加ゼロ・bUnit 不使用)** — 3 ファイル追加。
  **純ロジック 2 本を先に固めてから結線を検査する**構成 (razor に残るのは器と代入の位置だけ)。
  - `ui.Tests/PreviewHistoryTests.cs` (**57 件** / 529 → …): `PreviewHistory` の全域。
    容量 (投入 n 件 → 長さ min(n, 4)・5 件目で最古が落ちて `Find` でも引けない・上限超過の入力も切り詰め)、
    **非 Mutate** (入力の要素と順序が参照ごと不変・常に新インスタンスを返す = 同一参照だと
    Blazor の差分描画から「変化なし」に見えて画面が更新されない)、`Find` / `IsSelected`
    (存在しない Id・`int.MinValue/MaxValue`・null 要素・Id 衝突時は先頭 = 決定的・
    未選択の番兵 0/-1 で誰も選択中にならない・選択中は高々 1 枚)、
    **並びが `CreatedAt` に依存しない**こと (時計の巻き戻し・同一秒でも投入順のまま)。
    加えて **breadth の錠前**: public メンバ集合が `Capacity`/`Add`/`Find`/`IsSelected` の
    **完全一致** (`Clear` を 1 本足した写しで抽出が 5 件になり食い違うことまで常設で示す)・
    入れ子型ゼロ・`PreviewItem` は 5 プロパティで **`byte[]` を持たない** (型が int/string/
    DateTimeOffset の 3 つだけ = 生バイト常駐の入口を塞ぐ)。
  - `ui.Tests/PreviewLabelTests.cs` (**32 件** / … → 618): `PreviewLabel.Build` (モードバッジ文言)。
    **期待値表 13 行は移設前の `Generate.ModeBadge` を反射で呼んで採取した実測値**で、
    移設前に緑を確認してから純クラスへ移した (後から書くと「移した実装のコピー」になる)。
    読める size × draft の全組合せ・退化入力 (null/空/空白のみ → モード名だけ・末尾スペース無し)・
    読めない size の trim・**下書きと本番が常に区別できる**こと・公開 API が `Build` 1 本
    (モード名の定数を public にすると「語だけ借りて自前で組む」呼び出し側が生まれる)・
    **カルチャ非依存** (負値でしか観測できない差を含む 6 ロケール)・決定性・
    旧実装が razor に残っていないこと。
  - `ui.Tests/PreviewHistoryRazorTests.cs` (**69 件** / 618 → **687**): 結線 (F-1〜F-8)。
    `AppCssTokenTests` の partial として既存パーサを共有し、razor の `@code` を
    **文字列・文字・コメントをマスクしてから**波括弧で切り出す C# スキャナを足した
    (代入 `=`/`??=`/`+=` と比較 `==`/`!=` を区別して数える)。
    **F-1** `_imageData`/`_imageBytes`/`_lastMode`/`_downloadName` への代入は
    **private メソッド `ShowResult` の中に各 1 箇所** (markup からの代入も 0)・
    `Convert.FromBase64String` も 1 箇所だけ・`ShowResult` の呼び出し元は 2 つ
    **F-2** `PreviewHistory.Add` は `try` の成功経路に 1 箇所だけ (catch/finally に無い)
    **F-3** `_history` の宣言型が `IReadOnlyList<PreviewItem>` で `.Add(`/`.Insert(`/`.RemoveAt(`/
    `.Remove(`/`.Clear(` が 0 件・代入の右辺は `Array.Empty<` か `PreviewHistory.Add(` だけ
    **F-4** クリックハンドラ冒頭が `if (_busy) { return; }` + markup 側も `disabled`
    **F-5** 履歴経路 (markup / `ShowResult` / `SelectHistory` / `PreviewHistory.cs`) と
    razor 全体に `File.`/`Directory.`/`PresetStore`/`JSRuntime`/`localStorage`/`sessionStorage` が 0 件
    **F-6** 履歴 markup に `draggable`/`download`/`btn-icon`/`Delete`/`Remove`/`Pin`/`Take(`/`Skip(` が 0 件
    **F-7** サムネ列が (A)(A2) のベースライン表に **1 行も現れない**こと + 表の件数
    (font-size 36 / border-radius 29 / 余白 65) を直接固定 + `.canvas` に増えたのが
    `flex-direction` だけであること
    **F-8** 頭打ちが **`N == 104 (E の基準) + .thumb の height`** であること (= 176) と、
    その足し算の前提 `box-sizing: border-box`。
    加えて配置 (サムネ列は `.canvas` の中・`.preview` の**閉じタグより後** = 枠に重ならない・
    `position` を持たない)・空なら `@if` ごと出ない・選択中は `var(--accent)` のみ
    (新トークンゼロ)・`all: unset` で UA 既定を落としていること。
    **変異検査は 2 段構え**: 合成 razor / CSS を 1 箇所ずつ壊す **24 ケース** +
    **本物の `Generate.razor` に 1 箇所だけ変異を入れる 7 ケース** が全部赤くなること。
- **P2-2 のタイマー**は自動テスト対象外 (razor + レンダラが要る)。停止 3 経路
  (完了 / エラー / 離脱) は制御フローだけを抜き出した使い捨てハーネスで確認する。
- CSS/razor の見た目はテスト対象外 — ビルド緑 + 手動確認で担保。
- **件数の推移** (ui.Tests 全体): P1 まで 91 → P2-A 135 → P2-B 204 → P2-C 241 →
  (バッチ D ステップ 1 = ベースライン凍結) 374 → P1-6 447 → P3-1 466 → P3-3 486 →
  P3-4/P3-2 529 → (バッチ F 前半 = 純クラス 2 本) 618 → P3-5 結線 **687**。

### 手動 (実施台帳)

ブラウザ挙動・IME・見た目はエージェントで検証できないので**ユーザーが実施する**。
散文のチェックリストだと「何が未実施か」を追えないので、**この表を唯一の台帳**とする
(2026-08-08 / P3 バッチ D で改稿。旧 §7 の散文 1〜7 に散っていた項目はすべてここへ移送済み)。

**やり方**: `dotnet run --project ui` → ブラウザで開く。**表の上から順に 1 パス**で消化できる並びに
してある。**M-01〜M-13 は C++ サーバーを起動せずに**確認でき (先に未接続状態でしか見られない M-02 を
片付ける)、**M-14 以降は `dollama --http` を起動してから** — 生成ボタンの理由テキストは
「busy > 未接続 > プロンプト空」の優先順なので、**未接続のままだとプロンプト空の理由も
enabled 判定も観測できない**ため。所要は一巡 **15 分**程度。
状態は **未 / OK / NG** で埋め、**NG は実装で粘らず PL へ上げる**。

| 項目 ID | 対象バッチ | 期待結果 | 優先度 | 状態 | 確認日 | ブラウザ |
|---|---|---|---|---|---|---|
| M-01 | P2-A (P2-5) | **IME**: プロンプト欄で「少女」を IME 変換し **Enter で確定 → タグ化されない** (未変換ローマ字も入らない)。確定後にもう一度 Enter で初めてチップ化 | **高** | 未 | — | — |
| M-02 | P2-A (P2-9) | (C++ 未起動のまま) 未接続 → 生成 2 ボタンが disabled +「C++ サーバーに接続していません」+「再接続」ボタンが出る。押すと再判定が 1 回だけ走る | 中 | 未 | — | — |
| M-03 | P2-C (P2-6) | プロンプト欄にフォーカス → その `.chips` 左に 3px の accent 縁。**フォーカスリングも同時に出る** (片方が消えたら `--edge` 合成が壊れている) | 低 | 未 | — | — |
| M-04 | P2-C (P2-6) | お気に入り入力欄にフォーカス → 左ペインのパレット**全体**がうっすら青い面になる | 低 | 未 | — | — |
| M-05 | P2-C (P2-7) | タグ 0 個 → 保存バー (プリセット名 + 保存) ごと消える。タグを 1 つ足すと戻る | 低 | 未 | — | — |
| M-06 | P2-C (P2-7) | 既存プリセットと同名で保存 → 「〜を**上書き**保存しました」になる (新規時と文言が違う) | 低 | 未 | — | — |
| M-07 | **D** (P3-1) | **ボタン 7 種を一巡**して見た目・hover・disabled が崩れていない: 生成 (accent 塗り) / 下書き (枠) / プリセット保存 / お気に入り + / 言語トグル 2 連 / プリセット削除 × (hover が赤系) / PNG 保存。**素の灰色ボタンが 1 つも無い**こと | 中 | 未 | — | — |
| M-08 | **D** (P3-1) | **Tab 一巡で全ボタンにリング**が出る (張り替えた 9 要素すべて。とくに `<a download>` の PNG 保存 = `button:focus-visible` に当たらない要素) | 中 | 未 | — | — |
| M-09 | P1 (P1-2) | **フォーカス一巡**: Tab だけで トップバー → パレット → プロンプト → ネガティブ → サイズ/ステップ → LoRA → 生成ボタン を回り、全停留点でリングが見える | 中 | 未 | — | — |
| M-10 | **D** (P3-3) | **役割ラベル**: ウィンドウ幅 ≥1500px でテレメトリ各バーの右に役割 ("Qwen2 LLM" / "CLIP enc" / "VAE enc" / "SDXL UNet") が出る。幅を縮めると消え、**バーの位置・幅は動かない**。狭い幅でもホバーの `title` には出る | 低 | 未 | — | — |
| M-11 | P1 (P1-5) | **テレメトリ**: スタブ波形で 4 色 (青/紫/ティール/オレンジ) のバーが**個別に**動く | 低 | 未 | — | — |
| M-12 | P1 (P1-1) | **コントラスト実測**: DevTools のカラーピッカー (コントラスト表示) で §3 の想定値を数値で記録 (muted ≥ 4.5:1 / focus リングが視認できる / accent ボタン ≥ 4.5:1) | 低 | 未 | — | — |
| M-13 | P1 / P3 | **ブレークポイント**: 1440px (3 ペイン) / 1100px 境界 / 900px (縦積み) / 768px の 4 点で (a) テレメトリの折返し・省略 (b) 追加先トグル 3 ラベルの非折返し をスクショで残す。※ (c) 縦積み時の生成→プレビュー導線は **M-19 へ分離** (P3-2 実装済・2026-08-08) | 低 | 未 | — | — |
| M-19 | **E** (P3-4/P3-2) | **レイアウト 4 点スクショ**: M-13 と同じ 1440 / 1100 / 900 / 768px で (a) **3 ペイン時に中央列が 460px まで伸び**、長いタグ列 (15 個超) の折返しが減っている (b) 右ペインのプレビュー枠が**画像より極端に横長にならず**中央に居る (c) **縦積みは 生成 → 結果 → パレット**の順 (1100px 境界の前後で並びが切り替わる)。**3 ペイン側で行の貼り付き (sticky) が起きていない**ことも同時に見る | 中 | 未 | — | — |
| M-20 | **E** (P3-2) | **sticky 導線**: 900px 幅で中央列をスクロール → **生成 2 ボタンが画面下端に残り、押せる** (地が不透明で裏のチップ列が透けない・上境界の 1px が見える)。押した後 **少しスクロールするだけでプレビューが見える**。※ 貼り付き中はボタン直下の「押せない理由」が画面外に出る = 仕様 (実装メモ参照) | 中 | 未 | — | — |
| M-14 | P2-A (#2) | (要 C++・以降同じ) タグ 0 かつ draft 空 → 生成 2 ボタンが disabled + 直下に「プロンプトにタグを 1 つ以上追加してください」 | 中 | 未 | — | — |
| M-15 | P2-A (#1/#2) | **draft に文字あり・タグ 0 → ボタン enabled** (罠の核心。ここが disabled だと確定の機会が永久に来ない) | 中 | 未 | — | — |
| M-16 | P2-A (#1/P2-8) | ① 「1girl」と打って **Enter せず**生成ボタン → 確定されてチップ化され生成される ② チップ入力にフォーカスがある状態で **Ctrl+Enter → 生成が走る** | 中 | 未 | — | — |
| M-17 | P2-C (P2-10) | 生成中 (`Activity.Enter` 中) → テレメトリの 4 バーが**デバイス色に光り**、ブランド横に「生成中」pill が出る。**終了で消える** | 低 | 未 | — | — |
| M-21 | **F** (P3-5) | **並び**: 未生成では右ペイン下に**何も出ない**。3 回生成 → サムネが **3 枚**並び、**最新が先頭 (左端)**。選択中 (= 今表示中) の 1 枚だけ枠が accent | 中 | 未 | — | — |
| M-22 | **F** (P3-5) | **容量**: さらに生成して**計 5 回** → サムネは **4 枚のまま**で、**最古が消える** (5 枚目が並ばない) | 中 | 未 | — | — |
| M-23 | **F** (P3-5) | **★ funnel の実地確認 (ずれたら即 NG)**: サイズか下書き/本番を変えて 2 回以上生成 → **古いサムネをクリック**すると ① 画像 ② 左上のモードバッジ ③ 「PNG 保存」で落ちるファイル名 の**3 つが同時にその生成のものへ戻る**。1 つでも前の生成のままなら F-1 の想定事故 (機械検査は緑でも実地で割れる形) なので即報告 | **高** | 未 | — | — |
| M-24 | **F** (P3-5) | **サムネ元の一貫**: 履歴から再表示した状態でプリセットを保存 → **その (今見えている) 画像**がプリセットのサムネになる。併せて **生成中はサムネ列が押せない** (減光してクリックが無視される) ことも見る | 中 | 未 | — | — |
| M-18 | 回帰 | プリセット保存/ロード/削除・お気に入り追加/削除・LoRA 選択 → 生成リクエストの `loras` キー有無 (未選択で送らない)・下書き/本番のサイズ切替 が**従来どおり** | 中 | 未 | — | — |

- **M-21〜M-24 は C++ サーバーが要る** (実際に絵が返らないと履歴が積まれないため)。
  表の並び上は M-17 と M-18 の間 = **「要 C++」区間の末尾**に置いてあるので、
  M-14〜M-17 を消化した流れでそのまま続けられる。
- **M-01 と M-23 が優先度「高」**。
- **M-23 の理由**: F-1 (funnel) が防いでいる事故は「**絵だけ**正しく戻ってバッジと保存名が
  前の生成のまま」という、**目視でも見落としやすいのに実害が大きい**もの
  (保存した PNG のファイル名が別の生成を指す)。機械検査は「代入が 1 箇所か」を見るだけで、
  **`PreviewItem` に詰める値そのものが取り違っていれば緑のまま通る**ので、
  3 つが同時に切り替わることは人間が 1 度見るしかない。
- **M-01 だけ優先度「高」**の理由: doc に**残存リスクが明記されている唯一の項目**だから。
  P2-5 は JS interop を使わず `key == "Process"` の無視だけで防いでいるので、
  **composition 終了と同一 keydown で `Enter` が来るブラウザでは 1 回だけ確定が漏れうる**
  (P2 バッチ A 実装メモ参照)。**再現したら実装で粘らず PL へ上げる** (JS を勝手に足さない)。
- **件数**: 既存 **15** (旧 §7 の散文 1〜7 を全部移送 = 1→M-12 / 2→M-09 / 3①〜⑥→M-16①・M-14・
  M-15・M-02・M-01・M-16② / 4→M-13 / 5→M-11 と M-17 / 6①〜⑤→M-03・M-04・M-17・M-05・M-06 /
  7→M-18。旧 5 後半と旧 6③ は**同じ確認**だったので M-17 に統合した) +
  **バッチ D の新規 3 件** (M-07 / M-08 / M-10・PL 上限どおり) = 18 件 +
  **バッチ E の新規 2 件** (M-19 / M-20・PL 上限どおり) = 20 件 +
  **バッチ F の新規 4 件** (M-21 / M-22 / M-23 / M-24・PL 上限どおり) = **24 件**。
  **これで全 22 実装項目に対する手動確認の割り当ては完了**し、以後この台帳の消化だけが残る。
  M-13 に保留で書いてあった「(c) 縦積み時の生成→プレビュー導線は P3-2 実装後に追加」は
  **M-19 (c) と M-20 に分離して解消**した (M-13 は 1 パスで撮る 4 点スクショ、M-19 は同じ 4 点で
  レイアウトの中身、M-20 はスクロール操作を伴う導線 — 操作の有無で分けてある)。
