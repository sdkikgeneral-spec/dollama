# R-1 記録是正の監査指摘 (★未是正・次セッションの入力)

> ★**2026-09-20 追記 (見出しは当時のまま残す)**: 本文冒頭が言う「次セッション」は 2 週間来なかったが、
> **g14k 合流セッション (2026-09-20) で是正した**。**指摘 1 件ごとの対応は本ファイル末尾の
> 「是正結果」節**にある。**指摘本文は 1 文字も書き換えていない**ので、末尾と突き合わせて読むこと。
> 末尾の表には**未解決 / 未修正として残した項 (7 件)** も明記してある
> (G-14k のユーザー決裁 / G-8k S4 の生ログ / `E:\Develop\logs\g10k-t2c\` の現存 / `bench_pair` の B=2 /
> VAE mid のスコープ / `cuda-kernel-dev.md` の CUDA Graphs 記述 / `fast-mode-plan.md:23` の `#4 ✅`)。
> 加えて**行番号参照の未検算分**が `docs/g10k-plan.md` §15-2 に一覧してある。

**監査**: 2026-09-05 / `record-auditor` による敵対的監査
**対象**: `a0b938b` (R-1 記録是正 4 件) / `76c672d` (G-10k T2a/T2b/T2c 完了 + 保留の記録)
**判定**: 条件付き PASS (BLOCK 相当 2 / 要検討 7)

★**本ファイルは指摘の転記であって是正ではない。** 是正は次セッションで `record-writer` が行い、
`record-auditor` が再監査する (CLAUDE.md 実装作業のルール 4 = 書き手と検査者を分ける)。
転記を親セッションが行ったのは、エージェントの出力がセッションと共に消えるためで、
**判断を含まない機械的転記に限る**。

監査が確認した健全な点: `git diff 1c85dd9..HEAD -- src/` = **0 行** / 履歴保存型 (打消線・引用ブロック) は
全箇所で守られており **削られた記録は無い** / 分母混同 (65.50% vs 64.32%) は docs に持ち込まれていない /
(a)(b)(c)(d) の実質はすべて一次証拠で裏取り済み。

---

## BLOCK (必ず直すべき)

### B-1. `docs/g10k-plan.md` の `fast-mode-plan.md:NNN` 参照が 4 件だけ旧番号のまま

書き手は 20 箇所以上を更新したが、同じ文・隣の行にある 4 件を取りこぼした。
`docs/g10k-plan.md:826-827` の索引には旧番号として明記済みなので、**本文と索引が自己矛盾**している。

| 箇所 | 残っている参照 | 現物 (HEAD) の中身 | 正しい番号 |
|---|---|---|---|
| `g10k-plan.md:293` | `:411-413` | `:411` = G-3k の attention 実測 | **`:445-447`** |
| `g10k-plan.md:381` | `:345-347` | `:344-347` = prof_unet_fast_warm の resnet バケット表 | **`:379-381`** |
| `g10k-plan.md:564` | `:463-466` | `:464` = 「## スコープ外」見出し | **`:502-505`** |
| `g10k-plan.md:565` | `:690` | `:690-693` = G-8k F3 reserve 不足の stderr 化 | **`:752`** |

- ★`:565` は **T8 への作業指示**。再開者が `fast-mode-plan.md:690` を開いて conv2d の行番号注記を書き込むと、
  G-8k の F3 記述に無関係な注記が入る。
- ★`:381` はさらに悪く、飛び先が **「resnet 秒の実表」という実在するもっともらしい表**なので、
  読者が「間違った場所に着いた」ことに気づきにくい。
- `:293` の直前行 `:292` は `:413`→`:447` に更新済みで、**次の行だけ落ちている**。

### B-2. `docs/g10k-plan.md:903` — §13-1 の seed 出典が T2b 前の行番号

```
| seed | harness 既定 1234 (`src/tests/test_diffusion_batch2.cu:237`) |
```
- 現物: seed は **`:252`** (`const uint64_t seed = 1234ULL;`)。`:237` は無関係なコメント行。
- 原因: **この commit 群自身の `de34ec6` (GATE5 番兵) がファイルを +15 行ずらした**。
- 同じ §13-1 の表で warmup の出典 `:584` / `:586` は T2b 後の正しい番号を使っており、
  **同一表の中で T2b 前と後の行番号が混在**している。

---

## 要検討

### Y-1. `docs/fast-mode-plan.md:546` —「−34% はラウンド B 側の実数」は一次証拠で決まらない

監査が `docs/logs/g8k-s4b/*.log` の `[S4] image=N sec=` 全 12 値から再計算した値:

| ラウンド | 基準 POOL=0 | 既定 | min↔min | mean↔mean |
|---|---|---|---|---|
| A | 16.6602 / 16.2348 / 16.3341 | 10.9382 / 10.8357 / 10.8111 | **−33.41%** | **−33.81%** |
| B | 17.1355 / 16.9907 / 17.0408 | 11.2686 / 11.2632 / 11.2204 | **−33.96%** | **−34.04%** |

転記された 12 値と `−33.4%` / `−34.0%` は生ログと完全一致 (ここは問題なし)。
問題は**出自の断定**: `−34%` は **ラウンド A の平均 (−33.81%)** とも整合するため、
旧記述の `−34%` が B 由来だとは一次証拠では決められない。
→ 「どのペアから採られたかは特定できない (A の平均 −33.8% とも B の min −34.0% とも整合)。
A の min ペアは −33.4%」と書き換えるのが安全。放置すると次の是正で秒の側を B に「直される」経路がある。

### Y-2. `docs/g10k-plan.md:1020` — 出典行の中身と引用値が一致しない

`t2c_db2bench.log:472` / `:480` は **warmup ブロック** (9.730 / 0.853)。
引用値 9.729 / 0.854 は **`:496` / `:504`** (本走ブロック) にある。結論 (8.8%) は不変。
同じ §13-1 の表は warmup と本走を正しく書き分けているので、§13-4 側だけの取り違え。

### Y-3. `docs/g10k-plan.md:1005` — 207,460,184 bytes は「DLL 21 本」ではなくディレクトリ全量

監査が `t2c_stage_exe.log` の listing から DLL 21 本を合計 = **204,670,296 bytes**。
`207,460,184` は **DLL 21 本 + exe 2 本 (1,401,344 + 1,388,544)**。
放置すると再配置時に「DLL 合計が 204.67MB にしかならない → 2 本足りない」と誤判定する経路。

### Y-4. ★launch 谷が「生成 #2 限定」だと冒頭で明示されず、生成 #1 の値がどこにも無い

- `g10k-plan.md:27` (保留の理由): 「launch 谷は wall の 1.79% (GPU busy 98.21%) しかない」— **どの生成か書いていない**。
- §13-4 の表は「生成 #2 (重み転送の尾を含まない側)」と正しく限定している。
- **生成 #1 の実測値が本書のどこにも無い**: `t2c_nsys_gen_split.log` 生成#1 = **3.54%** (busy 96.46%) /
  `t2c_nsys2_gen_split.log` 生成#1 = **3.43%** (busy 96.57%) = 生成#2 の約 2 倍。
- ★`fast-mode-plan.md:398` の G-1k 行は「非侵襲 profile で launch 谷を直接観測し、
  **残らなければ不着手クローズ可**」と書いている。**1.79% は G-1k を不着手クローズさせうる
  load-bearing な数値**であり、1 枚目が 2 倍あることを知らずに判定される経路が開いている。

### Y-5. `docs/hw-accel-plan.md:154` — 陳腐化注記のスコープが「下表」なので P-4 行に届いていない

`:31` の注記は「下表の分母」に限定。一方 `:154` の P-4 行は「現分母 warm 20.93s vs diffusers 3.8s」=
**将来の出荷判定 (G-6k) の分母**として 20.93s を指名しており、注記の射程外。`:162` も同様。
→ 注記が禁じている「この分母を新しい見積り・合否の分母にしない」を、同じ doc の別行が指示している状態。

### Y-6. `docs/g10k-plan.md:690` — T7 の副次条件が pre-G-8k の x1.33 帯のまま

§8 の副次条件は「x1.33 帯から +0.02 以上」(出典 = CLAUDE.md の x1.33439 = pre-G-8k)。
一方 §13-1 は post-G-8k の実測 **x1.30399** (profile ON) を記録しているのに、**相互参照が無い**。
合否なしなので影響は限定的だが、T7 再開者が x1.33 を現行帯として使う経路がある。

### Y-7. §11 の「計装が無い」が現ツリーで偽 / CLAUDE.md に語義解消のポインタが無い

- `g10k-plan.md:806`「`generate_txt2img` は batched で呼ぶが計装が無い」— **T2b がまさに入れた**。
  `diffusion.cu` も 981 行 → **1042 行**。§11 の見出しは日付限定 (2026-08-24) で冒頭に追記もあるが、
  **§11 だけを引く読者は現況と読む**。
- ★`CLAUDE.md:232` の G-8k 行が「秒は characterization のみ (G-8k は秒数レバーではない)」のままで、
  今回の語義解消 (「実測で秒が動かなかったという意味ではない」) の**ポインタが無い**。
  fast-mode-plan は自 doc に射程を限定し、hw-accel-plan にはポインタが入ったが、
  **毎セッション文脈に入る CLAUDE.md だけが取り残されている** —
  `docs/measurements-log.md:275` の「中4」に過去の同型事故として記録されているパターン。
- `docs/measurements-log.md:433` の「84.07s → 11.30s」だけ採取条件注記が無い (日付併記ありで軽微)。

---

## 監査が未検証と申告したもの

- G-14k 先行の**ユーザー決裁そのもの**は repo 内の一次証拠では裏取り不能 (記述に出所は明示されている)。
- **G-8k S4 の生ログは現存しない**ため、`fast-mode-plan.md:553-560` の S4 実行形態はハーネス構造からの演繹のまま
  (doc 自身が格下げを明記済み)。
- `E:\Develop\logs\g10k-t2c\` (研究機側の exe / nsys 保管) の**現存**は開発機からは未確認。
  §13-5 が「T3 に入る前に確認せよ」としているので記録上は塞がっている。

---

## 併せて処理すべき、G-14k 計画書起草時の発見

詳細は `docs/g14k-plan.md` の §2-2 / §3-1 / §3-2 / §12 に記録済み。とくに次の 2 件は
**PL 決裁の前提に関わる**ので、S1 着手前に確認が要る:

1. ★**`bench_pair` の既存呼出は 3 本すべて B=1・self は H=1** (`test_attention_fast.cu:408-410`)。
   PL が hard に指定した速度ゲート「SDXL self 実形状・B=2」を**現状の計器は通らない**。
   かつ `run_once` の分岐は `use_fast` の 2 択 (`:321-328`) で「現行 fast 対 新カーネル」の 3 経路目が無い。
2. ★**VAE mid attention が G-14k のスコープかは未決**。`fast-mode-plan` #4 節は「G-14k のスコープ」と書くが、
   PL 決裁 (段構成・ゲート・形状) には現れない。計画書は勝手に入れも落としもせず未決として明示した。

その他 (触っていない・報告のみ):
- `.claude/agents/cuda-kernel-dev.md:101` が `--fast` を「ループ GPU 常駐 + CUDA Graphs / …」と記述しているが、
  **CUDA Graphs は未実装** (`cudaGraph` は `src/` に 0 件)。波及先の変更はファイルごとに決裁が要るため未修正。
- `docs/fast-mode-plan.md:23` の 3 段モード表が `#4 FlashAttn級 ✅` のままで、
  同ファイル `:117-143` の #4 節進捗表 (項目 3 は ❌ 未実装) と整合しない。監査中につき未修正。

---
---

# 是正結果 (2026-09-20 / `record-writer` / g14k 合流セッション)

★**上の指摘本文は 1 文字も書き換えていない。** 本節は追記のみ。
★**監査の主張も鵜呑みにせず一次証拠で検算した。** 検算の結果、監査側の記述を**訂正した項が 2 件ある**
(B-1 の「正しい番号」・併記 1 の「self は H=1」)。

**前提の変化**: 指摘は 2026-09-05 時点。その後 2 週間で main が G-10k 陰性クローズ → 2-6d → E-0/E-1 と進み、
**指摘が名指しした行番号は「監査が正しいと言った番号」も含めて再度ずれている**。
よって**すべて現在の main 合流後のツリーで検算し直した**。

## BLOCK

| # | 是正内容 | 根拠にした一次証拠 |
|---|---|---|
| **B-1** | **4 件とも是正した。ただし監査が示した「正しい番号」も 2 週間で腐っていたので、そのまま書き写していない** — 行番号を **grep 文字列 + 2026-09-20 検算の現物番号**の併記へ置き換えた。実際の現物: `:411-413`→**`:457-458`** (監査の想定 `:445-447`) / `:345-347`→**`:390-391`** (同 `:379-381`) / `:463-466`→**`:513-516`** (同 `:502-505`) / `:690`→**`:763-764`** (同 `:752`)。ついでに同じ文の中で腐っていた `:447` / `:752` / `:753` / `:274` も直した。方針と未検算分の一覧は `docs/g10k-plan.md` **§15** に新設 | `docs/fast-mode-plan.md` の当該行を直接表示して確認 (`:457`,`:458`,`:390`,`:391`,`:513`-`:516`,`:763`,`:764`,`:765`,`:281`)。一致していて据え置いた番号も確認済 (`:233`,`:236`,`:242`,`:245`,`:246`) |
| **B-2** | **解消。ただし「番号を直す」ではなく「誤った記述ごと落とす」形**。`:237` を書いていたのは g14k 側 §13-1 ドラフトのみで、main 側 §13 は行番号を書かず「harness ハードコード」と記している。合流で main 側を採ったため誤参照が消えた。**経緯は消さず** `docs/g10k-plan.md` §14-3 の表に「ドラフト側の `:237` は誤り・現物は `:252`」と明記 | `src/tests/test_diffusion_batch2.cu:252` = `const uint64_t seed  = 1234ULL;` (2026-09-20 に現物確認)。`grep -rn "test_diffusion_batch2.cu:237" docs/` の残存は本監査ファイル自身のみ |

## 要検討

| # | 是正したか | 内容と一次証拠 |
|---|---|---|
| **Y-1** | ✅ 是正 | `docs/fast-mode-plan.md` の当該引用ブロックから「**−34% はラウンド B 側の実数**」という**出自の断定を削除**し、「A の平均 −33.81% とも B の min −34.0% / 平均 −34.04% とも整合するため一次証拠では特定できない」に書き換えた。**旧記述の −34% は削っていない**。監査の再計算表を**書き写さず自分で再計算**して一致を確認: `docs/logs/g8k-s4b/{s4b_roundA_1_pool0,s4b_roundA_2_default,s4b_roundB_1_pool0,s4b_roundB_2_default}.log` の `[S4] image=N sec=` 全 12 値 |
| **Y-2** | ✅ 是正 | 8.8% の出典を **`t2c_db2bench.log:496` / `:504` (本走)** に直し、**`:472` / `:480` は warmup (9.730 / 0.853) なので引くな**と明記 (`docs/g10k-plan.md` §14-2)。4 行すべて現物で確認 |
| **Y-3** | ✅ 是正 | 誤った `207,460,184 = DLL 21 本` を**持ち込まなかった**うえで、`docs/g10k-plan.md` §14-3 に「`t2c_stage_exe.log:39-40` の `total_bytes` は **exe 2 本込みのディレクトリ全量**・DLL のみなら **204,670,296**」と明記。同ログの listing で `prof_arena_e2e.exe 1388544` / `test_diffusion_batch2.exe 1401344` を確認し差引で検算 |
| **Y-4** | ✅ 是正 (指摘の中で最重要だったもの) | `docs/g10k-plan.md` **§14-1 に生成 #1 / #2 の 4 値すべてを表で収載**し、「**1.79% だけを見て G-1k を不着手クローズするな・1 枚目は約 2 倍 (3.43〜3.54%)**」「単発生成の体感は生成 #1 側」「どちらの帯を採るかは PL 決裁」と明記。出典 `t2c_0904_nsys_gen_split.log:36-40` (3.54% / busy 96.46%) / `t2c_0904_nsys2_gen_split.log:36-40` (3.43% / busy 96.57%)・生成 #2 は同 `:11-15` |
| **Y-5** | ✅ 是正 | `docs/hw-accel-plan.md` の陳腐化注記に**射程の明示**を追記 (「下表だけでなく 20.93s/15.88s を分母に使う全箇所に掛かる・とくに P-4」) し、**P-4 行自体にも**「この値のまま出荷判定の分母にしないこと」を入れた。あわせて同 doc §1.1 の表と注記にあった「**再測は G-10k T7c・未実施**」が**現況と食い違う**ことを是正 (T7c はスキップ・G-10k 陰性クローズ = **再測は未起票**) |
| **Y-6** | ✅ 是正 | §8 の副次条件に現況注記を追加: **x1.33439 は pre-G-8k で現行帯ではない** / post-G-8k の実測は **x1.30399** (`docs/logs/g10k-baseline/t2c_db2bench.log:514` で確認) / ただし**計装 ON なのでこの副次条件の判定には使えない** / **計装 OFF の正典倍率は T7c スキップで未採取** / 合否なしなので G-10k の結論には影響しない |
| **Y-7** | ✅ 是正 (3 点すべて) | ①§11 の「`generate_txt2img` は計装が無い」行に**現況注記**を追加 (**T2b が埋めた**・`diffusion.cu` は 981→**1071 行**・`generate_txt2img` `:761`・dump `:1000` の `// --- G-10k T2b: 内訳 dump ---` を現物確認)。②**CLAUDE.md の G-8k 行に語義解消のポインタを追加** (「秒数レバーではない」は起票の目的の話で実測の話ではない → `docs/fast-mode-plan.md`「用語の解消」節)。あわせて「本命は G-10k」も陰性クローズ済である旨を併記。③`docs/measurements-log.md` の 84.07s→11.30s は**既に採取条件注記が入っていた** (同 `:69`・2026-09-04 追記分) ので**何もしていない = 是正不要** |

## 監査が未検証と申告したもの (再評価)

| 項 | 現時点の扱い |
|---|---|
| G-14k 先行の**ユーザー決裁**そのもの | ★**未解決のまま**。repo 内に一次証拠は無い (エージェントは中継された決裁を根拠にできない)。なお本合流 (2026-09-20) についてはユーザーの合流承認が別途あるが、**それは「合流してよい」であって「G-14k を先行させる」の決裁ではない**。混同しないこと |
| G-8k S4 の生ログ | ★**未解決のまま (復旧不能)**。2026-09-20 に `docs/logs/` 直下を確認したが **`g8k-s4b` はあるが `g8k-s4` は無い**。よって当該記述は引き続き**ハーネス構造からの演繹**であり、doc 側の格下げ明記をそのまま維持した |
| `E:\Develop\logs\g10k-t2c\` の現存 | ★**開発機からは依然未確認**。ただし**依存度は下がった**: T2c の生ログ・stats・gen_split は 2026-09-09 に repo へ回収済で (`docs/logs/g10k-baseline/t2c_0904_*`)、§14 の主張はすべて repo 内ファイルで裏が取れる。repo 外に残るのは exe / DLL / `.nsys-rep` (再走用) のみ |

## 併せて処理すべきとされた G-14k 起草時の発見

| 項 | 扱い |
|---|---|
| 1. `bench_pair` が B=2 を通らない | ★**未解決 (S1 着手前の PL 決裁が要る)**。2026-09-20 に現物再確認し、**監査の記述を一部訂正**: 呼出 3 本が**すべて B=1** なのは正しいが、「**self は H=1**」は不正確 — `src/tests/test_attention_fast.cu:408` が `(1,1,4096,4096,64)` で H=1、**`:409` は `(1,8,1024,1024,64)` で H=8** = self でも H=8 の本がある。`run_once` が `use_fast` の 2 択で 3 経路目が無い点 (`:318` のラムダ) は指摘のとおり |
| 2. VAE mid attention が G-14k のスコープか | ★**未解決 (PL 決裁待ち)**。`docs/g14k-plan.md` が未決として明示している状態を維持した。参考値として §14-2 に **VAE mid = 1 launch 514ms・`grid=(1024,1) block=32` = 1 block 1 warp・VAE Σkernel の 51.69%** を収載したので、決裁の材料は揃っている |
| その他 `cuda-kernel-dev.md` の CUDA Graphs 記述 | ★**未修正 (エージェント定義の変更は要決裁)**。ただし**主張は再確認した**: 2026-09-20 に `git grep -i cudagraph -- src` = **0 件**。残債として `docs/g10k-plan.md` §14-3 に起票 |
| その他 `fast-mode-plan.md:23` の `#4 FlashAttn級 ✅` | ★**未解決・未修正**。本合流のスコープ外 (3 段モード表の改訂は G-14k 本体の決裁と一緒に扱うべき)。**無かったことにはしない**ため、ここに残す |
