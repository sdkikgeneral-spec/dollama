# G-10k: conv2d 真の batch2 — 研究機セッション向け引き継ぎ書

**ステータス**: 🔲 **未着手** (T3 以降はすべて**予定**)。本書は着手前に開発機で起草したもので、
**実走した数値を 1 つも含まない**。「確認した」と読める記述があれば、それは本書起草時に
**ソースを開いて確認した事実**であって、GPU 実走の結果ではない。

**作業ブランチ**: `feat/g10k-conv-true-batch2` (開発機で checkout 済。研究機でも同名で作業する)
**起草**: 2026-08-24 / 開発機 (CUDA なし) / `record-writer`
**改訂**: 2026-08-24 (5 巡目) — PL 決裁 (T6②廃止 / T7・T7b・T7c 再編 / **F3 は①採用 = T2b 新設** /
`%` 列は B 案 = 出さない / **アンカー 3 は帯 + 参考対照へ降格し、秒の hard 担保は T4 の構造保存要件へ移設**) +
敵対的監査 3 ラウンド (BLOCK 2 → BLOCK 1 → BLOCK 1) を反映。
★**決裁は全件クローズ (2026-08-24)。未決は無い。**
**T2b から T9 まで着手条件なしで直列に進めてよい。**
★**ここまでで `src/` の変更は 1 行も発生していない** (計器設計は 3 回差し替わり、
そのうち **2 件は PL 決裁の欠陥**だった。§2 の計器規則末尾)。
**設計の正本**: `docs/fast-mode-plan.md` の「G-10k: conv2d 真の batch2」節 (`:688-693`)

---

## 0. この文書の位置づけ

- **開発機には CUDA Toolkit が無い**: 本セッションで `nvcc` 不在・`CUDA_PATH` 未設定を確認した。
  `meson.build:130` の `-arch=sm_120` は `add_languages('cuda', required: true, native: false)`
  が成功したときだけ付くので、開発機では `.cu` を 1 行もコンパイルできない。
  → **承認計画にあった「S1〜S3 を開発機でビルド可否確認」工程は成立しないため廃した**
  (承認計画からの意図的な逸脱。判断根拠は上記の実測)。
- したがって **実装・実走はすべて研究機 `KIK-WIN-RTX58` (`E:\Develop\Projects\dollama`) で行う**。
- 研究機セッションと本セッションは通信できない。**本書が唯一の転送路**であり、研究機側は冷えた
  コンテキストからこれを拾って実行する。前例は G-8k S6 (`docs/g8k-review-fix-plan.md:268`
  「実装・実走とも研究機 `KIK-WIN-RTX58` (SAC OFF)」) と同型。
- 本書は正本 (`docs/fast-mode-plan.md` G-10k 節) を**実行手順・ゲート・不変条件へ落としたもの**。
  正本と重複する設計説明は繰り返さずポインタで済ませる。ただし §2 の N1/N2 のとおり、
  **正本には未検証の実装案が含まれ、行番号も現物とずれている**ので、正本を無条件の権威として読まないこと。

---

## 1. ★本書に「書いていない」こと (非交渉)

**cuBLAS strided batched GEMM の stride 写像は本書に書かない。**
`cublasGemmStridedBatchedEx` の strideA / strideB / strideC をどう渡すか、A と B のどちらが
第一行列側に来るか、転置後の位置がどこか — 一切書かない。コード断片も疑似コードも書かない。

> **stride 写像は研究機側で `src/kernels/gemm.cu` の既存 `gemm_cublas` (`:405-` / cuBLAS 呼出は `:423`) と
> `launch_gemm_fp16` (`:516-`) を読んで自分で導出し、S1 (T3) の GEMM 単体ゲートで実測検証すること。
> 本書に答えは書かない。**

**理由**: 未検証の設計を文書化すると、実装者はそれを検証済みの権威として扱い、自分で導出し直さない。
G-8k S5 で「是正が新しい誤りを持ち込む」形が 5 ラウンド続いたのと同根 (doc の 1 行から推測して断定する型)。
とくに stride 写像は**間違っても「動くが数値が違う」形で通ってしまう**ため、doc 由来の権威が
最も危険な箇所である。

★**注意 (N1)**: 正本 `docs/fast-mode-plan.md:691` には **stride 写像の案が既に書かれている**
(起草時の提案で、**実測検証を経ていない**)。本書はそれを再掲しない。実装者は正本のあの 1 行を
**「未検証の提案」として扱い、鵜呑みにせず `gemm.cu` から導出したものと突き合わせる**こと。
一致しても、T3 のゲートを通すまでは正しさの根拠にしない。
(正本側へ「起草時の未検証提案」注記を入れるのは **T8 のスコープ**として決裁済み = §6 T8 / 決裁 4。)

---

## 2. 計器とゲートの是正 (起草時に発見 → **2026-08-24 PL 決裁済み**)

**F1 / F2 / F3 / N2 / N3 はすべて決裁済み (2026-08-24)。未決は無い。**
いずれも起草時または監査ラウンド中にソースを開いて見つけた食い違いで、**PL が独立に裏取りしたうえで
決裁済み**。決裁内容は本書の該当節 (§3 分母 / §5 数値方針 / §6 タスク表 / §7 ゲート / §8 判定帯) に
**すべて反映済み**。**T2b〜T9 は着手条件なしで直列に進めてよい。**
以下は「なぜそう決まったか」の履歴 + 各件の一次証拠である。

★**この 3 件は同じ型の誤り**である: **計器が被験変数を見ていない** (F1 = B=1 の harness /
F3 = B=1 経路にしかない dump)。**G-4k S3 の「測っている変数が epilogue ではない」の計器版**であり、
**3 回連続で、いずれも「doc の 1 行を信じて計器を選んだ」ときに起きている**。
計器を指定するときは、**その計器が被験変数を通る経路で印字されることをソースで確認する**こと。

### F1: `prof_unet_fast_warm` は B=1 経路しか叩かない → **G-10k の観測点から外す (決裁済み)**

一次証拠 (起草時に開いたもの):

- `src/tests/prof_unet_fast_warm.cu:116, 128` は `launch_unet(handle, d_latent, ...)` を呼ぶ
  (`d_latent` は `latent_n = 4*128*128` = **1 サンプル分**・同 `:82`)。
- `src/infer/unet.cu:1462` — その `launch_unet(handle, ...)` は `launch_unet_impl(w, 1, ...)` =
  **B=1 固定**。B を渡せるのは別関数 `launch_unet_batched` (`src/infer/unet.cuh:137` /
  `src/infer/unet.cu:1475-1489`) の方で、prof harness はこれを呼んでいない。
- G-10k が触るのは `launch_conv2d` の **N>1 枝** (`src/kernels/conv2d.cu:487-507`) のみ。
- → **B=1 では新経路を 1 度も通らない** ので `[RESNET-BUCKET]`
  (`prof_unet_fast_warm.cu:154-156`) は G-10k の前後で変わらないのが期待値。
- さらに、分母 **1.212s** / baseline **1.225s** / S3 の 1.28564–1.41299s は
  **すべて B=1 の「1 forward/step × 20step 換算」**である
  (出典: `docs/fast-mode-plan.md:306-316` の「参考: prof_unet_fast_warm」表 /
  換算式は `prof_unet_fast_warm.cu:148` の `resnet_ms * gen_steps / 1000`)。

**PL の認定 (射程は起草時の指摘より広い)**: `docs/fast-mode-plan.md:212-215` / `:318-324` の
「resnet ≤0.95s の合否を G-10k 後の再 profile へ再割当」という **G-4k の決裁自体が、
被験変数を通らない計器に紐づいていた**。G-4k S3 で学んだ「測っている変数が epilogue ではない」と
同型の誤りが、今回は比較条件ではなく**計器側**で起きていた。
→ **これは数値の更新ではなく「ゲート設計の誤りの是正」として T8 の記録スコープに含める** (§6 T8)。

**決裁**: T6 を全面差し替え (§6 T6)。`prof_unet_fast_warm` は G-10k では**使わない**。
分母 1.212s は **G-10k の合否分母として退役** (§3)。判定は**削減率**へ移す (§8 ケース B)。
harness の B=2 化 (起草時の選択肢 3) は**却下** — 新規計測コードの検証工数がかかる上、
**どちらにせよ過去の 1.212s とは繋がらない** (分母は取り直しになる)。
B=2 の UNet 単体プロファイルが要るのは G-12k (transformer 脇役の内訳分離) なので **§12 残債へ**。

### F2: 既存 9b はハード BIT-EXACT ゲートではない → **tol の議論を廃止し memcmp 不変量を hard 化 (決裁済み)**

一次証拠 (`src/tests/test_conv2d.cu`):

- `:513` `const bool bit_exact = (max_abs == 0.0f);` — この値は `:514-517` で**表示だけ**される。
- `:521` `return compare(got, ref, K, name);` = **合否は `compare()` の tol 判定** (`:87-123`)。
- `:448-449` のコメント自身が「ビット一致 (MAE=0) を**第一目標に、届かねば tol 内 (compare) を許容する**」
  と書いている。
- → 現状の 9b は、per-n 直列経路の bit 一致が壊れても **tol 内なら緑のまま通る**。

**PL の裏取りで判明した tol の実効幅** (`test_conv2d.cu:76-84`):
`rtol_for(K) = 5e-3 + 1e-5*K` / `atol_for(K) = 3e-3 + 2e-5*K`。UNet の 3x3 conv は
K = Cin·3·3 で、Cin=320 なら **K=2880 → rtol ≈ 3.4% 相対 / atol ≈ 0.061 絶対**。
**この幅を「ゲート」と呼べない。**

**決裁**: 起草時に PL 自身が出した「既存 `compare` をそのまま使え」を**撤回**。
tol の数字を議論するのをやめ、**tol に依存しない構造不変量を hard ゲートにする** (§7 の
[G1] / [G2a] / [G2b])。既存 `compare` は **floor (超えたら緩めず BLOCK)** として残す。
これで「緩和ではなく分岐」が実際に担保され、**tol を実測後に決める必要が消える**
(ゲートの自己成就の回避も同時に達成)。

### F3 (★決裁済み 2026-08-24): batch2 構成で resnet バケットを印字する実行物が存在しなかった → **①で解消**

**起草時に私 (record-writer) が書いた「`diffusion.cu:701-702` の `cat_resnet_sec` が N>1 枝を通る唯一の
既存計器」は誤りだった。** F1 と同型 (計器が被験変数を見ていない) の誤りを、私が計器表側で作り込んでいた。
一次証拠 (2026-08-24 に全数確認):

- `src/infer/diffusion.cu` は **981 行**で、`DiffusionPipeline` の**メソッド定義は 8 個**:
  `:288` (ctor) / `:384` (`destroy_resources`) / `:430` (dtor) / `:438` (`apply_lora_file`) /
  `:461` (`clear_loras`) / `:521` (`generate` の 3 引数ラッパ) / `:533` (`generate`) /
  `:732` (`generate_txt2img`)。★このうち**画像を生成するメソッドは `:533` と `:732` の 2 つだけ**
  (`:521` は `:533` へ guidance=1.0 で委譲するラッパ・`:548` は throw 文字列で定義ではない)。
- **内訳テーブルの dump は 1 箇所** (`:686` ヘッダ / **`:701-702` resnet 行** / `:714` 終端)、
  **counters の reset も 1 箇所** (`:554`)。**どちらも `:533` の `generate` の中**にある
  (`:533 < 554 < 686 < 714 < 732`)。
- その `generate` は **CFG を拒否し** (`:545-549`「guidance_scale != 1.0 is not supported
  (use generate_txt2img for CFG)」)、UNet を **`launch_unet(...)` = B=1** で呼ぶ (`:616-624`)。
- **N>1 枝を通る e2e 経路は `generate_txt2img`** (`:872` で `launch_unet_batched(..., 2, ...)`) だが、
  `:732-981` に **reset も dump も無い** (grep 0 件)。
- `test_diffusion_batch2` は parity (`:249`) も DB2_BENCH (`:460`) も **`generate_txt2img` しか呼ばない**。
  → **`DB2_BENCH=1 DOLLAMA_PROFILE=1` でも構成ごとの `cat_resnet_sec` は出ない。**
  (`DOLLAMA_PROFILE=1` で得られるのは `[ALLOC]` 行 = `src/infer/unet.cu:1352-1370` の方だけ。
  代わりに `ScopedSyncTimer` (`unet.cu:542`) の同期が入り **e2e 秒は膨らむ**。)

**帰結 (起票時点)**: **batch2 構成の resnet バケット秒は、当時のどの実行物からも取得できなかった。**

### ★F3 の決裁 (2026-08-24): **選択肢 ① を採用** — `generate_txt2img` に reset + dump を追加する

**決め手になった追加事実 (本セッションで一次証拠を確認)**: **カウンタは既に溜まっている。
欠けているのは reset と dump だけ。**

- `cat_resnet_sec` への加算は **`src/infer/unet.cu:542` の 1 箇所のみ** (`src/` 全体を grep)。
  それは `resnet_block()` の**先頭** (`:537-543`) = ブロック本体側。
- `launch_unet_impl` (`src/infer/unet.cu:902-1309`) の呼出元は **`:1326`** (SafeTensors 版・B=1) /
  **`:1462`** (ハンドル版・B=1) / **`:1487`** (`launch_unet_batched` 経由・**B=B**) の 3 つ。
  → **`launch_unet_batched` 経由でも同じ `resnet_block` を通る**。
- 段グループ/カテゴリ系のカウンタも `launch_unet_impl` の内側で加算される
  (`unet_steps` = `:925` / `unet_total_sec` = `:1298` / `unet_embed|down|mid|up|convout_sec`) =
  **経路非依存**。
- ⇒ **`generate_txt2img` + `DOLLAMA_PROFILE=1` の走行では、B=2 の resnet 秒は既に累算されており、
  `ScopedSyncTimer` の同期摂動も既に発生している。**
  ①は**新規計装ではなく、既に取れている数字を印字する host 側の printf + reset** =
  **数値不変・同期増加ゼロ** (コストが printf 相当まで下がるのが採用理由)。

**却下した 2 案と理由**: ② (合否を conv 単体に置く) は「conv が速くなったか」しか答えられず、
**G-4k から継承した問い (resnet バケット秒)** に答えられない。③ (合否を e2e 倍率に置く) は
**§8 の設計と矛盾** (「e2e はノイズが大きいので合否にしない」)。
加えて **N>1 経路に内訳計装が無いのは G-10k 限りの欠損ではなく G-9k / G-12k でも同じ壁**なので、
ここで埋めるのが筋 (PL 判断)。

→ 実装は **T2b** (`cuda-kernel-dev` / 研究機 / 単独コミット)、初採取は **T2c**。§6 のタスク表を参照。
**F3 は本決裁で解消**したので残債からは外した (実装台帳 T2b/T2c へ移動)。

### ★計器を指定する決裁の規則 (同型 3 連の再発防止・2026-08-24 PL 規則化)

> **計器を指定する決裁は、次の 3 点を `file:line` の一次証拠で示さない限り出さない・採らない。**
> ① **どの関数が呼ばれるか** (ハーネスの呼び出し行)
> ② **その関数が被験コード行に到達するか** (呼び出し連鎖)
> ③ **被験構成で実際にその枝を通るか** (B や env の値)
>
> **fast-mode で計器名を書くときは必ず `(B=1)` / `(B=2)` を併記する。**
> **3 回の事故はすべて B の見落ちだった。**

★**この規則の違反者に PL 自身が含まれる。** PL の弁: 「**T6② は record-writer の起草行を一次証拠なしに
採った私の決裁ミス**であり、『PL の決裁だから裏取り不要』という運用にしないことが、この規則の実効性
そのものである」。
★**実装着手前に監査が 3 回止めたのは想定どおりの機能**であり、**コストは実装後に発見するより桁で安い**。

★★**この Pkg の実績として記録する (T8 でも残す)**:
**監査 3 巡で計器設計が 3 回差し替わった (T6② → 廃止 → T7/T7b/T7c 再編 → T2b の新設) が、
`src/` の変更は 1 行も発生していない。** 一次証拠の補正も 4 件出た
(① `prof_unet_fast_warm` の B=1 / ② 9b が tol 合否 / ③ dump が `generate` 側のみ /
④ `launch_unet_impl` の呼出元は 2 本ではなく **3 本** = `unet.cu:1326` / `:1462` / `:1487`) が、
**4 件すべて実装着手前**である。
**次に同じ状況に来た人が「監査が重い」ではなく「実装前に潰すのが正しい」と読めるように**、
この事実を数字つきで残す。

★★**さらに: Fable (record-auditor) が PL の決裁の欠陥を 2 巡連続で捕まえた** —
**① T6② の計器誤り** (record-writer の起草行を一次証拠なしに採った決裁) →
**② アンカー 3 の「帯なし・別セッション跨ぎ比較」** (本書自身の原則を、PL 自身が作ったアンカーで破っていた)。
加えて **③ T2b DoD ②「連続 2 回の generate」** (実在する別メソッド名との衝突) も PL 文言由来である。
**これは 書き手 (record-writer) / 査読者 (record-auditor) / 決裁者 (PL) の三分割が効いている証拠**である。
★**PL 決裁も査読対象であり、実際に差し替わった。** 次に同じ体制を組む人が
**PL の決裁を検査対象外にしないため**に記録する。

### N2 (注意・残置): 正本の行番号が現物とずれている

`docs/fast-mode-plan.md:690` は N>1 枝を `src/kernels/conv2d.cu:483-503` と書くが、**現物は `:487-507`**
(本セッションで `grep -n "N > 1 && use_gemm_path"` → 487 を確認)。
**決裁 4**: 行番号ドリフトを追いかけて直す方針は採らないが、G-10k 節は**現役参照**なので例外として
「現物 `:487-507` (2026-08-24 確認)」を**併記**する (当時の記述は消さない = `fast-mode-plan.md:463-466`
で確立した「行番号ごと当時の記述を残すのは経緯の履歴としてであって、再度撲滅しに行く対象ではない」型)。
実施は **T8 のスコープ**。

### N3 (注意): キルスイッチの A/B は同一プロセス内で切り替えられない → **A→B→A の 3 プロセス (決裁済み)**

- `src/tests/test_diffusion_batch2.cu:483-486` — DB2_BENCH は **1 プロセス内で 3 構成**を回す:
  `default` / `attn+batch2` (**CLI 到達不能な計測専用の合成構成**・同 `:481-482` に明記) /
  `fast+epilogue` (= 出荷 `--fast`)。
- 一方 `DOLLAMA_CONV_BATCH` は §9-4 のとおり getenv キャッシュ型で**プロセス単位に固定**される。
  → **CONV_BATCH の A/B は必ず別プロセスになる。**
- **決裁 3**: 「隣接時刻帯の別プロセス × 2」では**足りない**。差分がドリフトか実効かを判別できない
  (G-4k S3 でドリフト +9.9% を捕まえた唯一の防御は「同一構成の再測を挟んだこと」=
  1.28564 → 1.41299s)。→ **`CONV_BATCH=1 → =0 → =1` の 3 プロセス連続採取**に確定 (§6 T7)。
- **同一プロセス内で構成を切り替えたと記録に書かない。** G-8k で S5e→S5f と 2 度の是正が入った型
  (出典: `docs/measurements-log.md:75` の該当注記 =「S4 も 1 走行 1 プロセスで、構成は起動時 env で固定」)。

---

## 3. スコープと狙い

`src/kernels/conv2d.cu` の **N>1 枝 (`:487-507`)** を置換する。現物は per-n オフセットで N==1 GEMM
ヘルパ (`launch_conv2d_1x1_gemm` `:313-331` / `launch_conv2d_im2col_gemm` `:340-429`) を
**直列 N 回**呼ぶ実装で、G-2k S1 のビット一致優先設計である (同 `:479-486` のコメントに設計意図が明記)。
これを **im2col の n 次元対応 + strided batched GEMM による真の batch2** へ置き換える。

- fast-mode に残る**唯一の秒数レバー**: `docs/fast-mode-plan.md:413` 「**秒数の本命は G-10k 単独**である」。
  G-8k は秒数レバーではない (同 `:411-413`)。
- 回収見込みは **resnet ~0.5-1.0s (resnet 1.3-1.7x 相当)** = 正本 `docs/fast-mode-plan.md:692` の
  **見込み値であり未実測**。同行も「正確な倍率は要実測」と書いている。本書はこの数値を合否に使わない。
- G-4k で保留された **resnet ゲートの合否をここで確定する**のが本 Pkg の対外的な意味
  (`docs/fast-mode-plan.md:212-215` / `:318-324`)。

### ★分母の決裁 (2026-08-24)

- **B=1 由来の 1.212s は G-10k の合否分母として退役。据え置かない。**
  絶対秒の据え置きはドリフト ±9.9% (G-4k S3 実績) に耐えないことが既に分かっている。
- 代わりに **毎回、そのセットの `--fast` + `DOLLAMA_CONV_BATCH=0` を分母にする** (§6 T7 のセット内)。
  ★**A/B は 2 層構造で、層を混ぜないこと**:
  **`DOLLAMA_CONV_BATCH` の A/B は必ずプロセスを跨ぐ** (getenv キャッシュ型 = プロセス単位固定・
  `gemm.cu:376-386` / `device_arena.cu:210-232` の型) 一方、**`default` / `attn+batch2` / `fast+epilogue` の
  構成差は同一プロセス内**で切り替わる (`test_diffusion_batch2.cu:483-486`)。
  したがって分母は「**プロセス B (`CONV_BATCH=0`) の `fast+epilogue` 構成**」、被験は
  「**プロセス A / C (既定) の `fast+epilogue` 構成**」= **同一構成・別プロセス**。
  ★**「同一走行で A/B を取った」と書いてはいけない** — この repo の正典用法では **走行 = プロセス**
  (`docs/measurements-log.md:75`「S4 も **1 走行 1 プロセス**で、構成は起動時 env で固定される」)。
- 判定は**削減率**で行い、G-4k が「≤0.95s vs 1.212s」で意図していた **−22%** を継承する
  (削減率なら分母が変わっても意味が保たれる)。帯は §8 ケース B の表。

### 分母に使ってはいけない数値 (既存記録の明示禁止事項をそのまま引き継ぐ)

- **B=1 の 1.212s / 1.225s / 1.28564–1.41299s** — 上記のとおり退役 (§2 F1)。
  出典: 1.212s = `docs/fast-mode-plan.md:203` (S2 の表) / `:206` (ゲート未達の判定) / `:216` (分母の採用) 、
  1.225s の baseline と `≤0.95s` のゲート値は **ハーネスの出力文字列に埋め込まれている**
  (`src/tests/prof_unet_fast_warm.cu:155-156`)、1.28564–1.41299s = 同 `docs/fast-mode-plan.md:310-313` の表。
  ★**`prof_unet_fast_warm` を走らせると退役後も `(gate: <=0.95s / stretch <=0.85s / baseline 1.225s)` が
  そのまま印字される** (src 変更は本 Pkg のスコープ外 = §12-4)。**出力の文字列を分母として読まないこと。**
- G-8k S4 / S4b の e2e 秒 (11s / 16s / 10.8s 等) — `docs/measurements-log.md:74` に
  「熱・常駐が揃った基準線として **G-9k/G-10k の効果見積りに流用しないこと**」と明記されている。
- CLAUDE.md 計測表の `decode 7.96s` / `1step ~9.2s` — **陳腐化・要再測**で、
  `docs/measurements-log.md:192-198` が「正典表を分母にして G-9k / G-10k の効果見積りや合否設計を
  組むと、桁違いの分母で合否が決まる」と警告している。

---

## 4. 不変条件 4 本 (破ったら不合格)

1. **default (B=1) は 1 バイトも変えない。**
   `launch_conv2d` の N==1 分岐 (`conv2d.cu:464-468` の 1x1・`:471-477` の im2col+GEMM) と
   `launch_conv2d_direct` は**無改変**、**N>1 枝 (`:487-507`) のみ差し替え**。これにより
   `test_unet_fast` の `default vs golden SSIM 0.999996` と `fast vs default bit-exact` が自動維持される。
2. **既存 9b (現行の合否は tol) を緩めない — かつ hard 化する。**
   9b (`run_case_batch_vs_persample` `:451-522` / 呼出 `test_conv_batch_gemm` `:527-546`) は
   **キルスイッチ `DOLLAMA_CONV_BATCH=0` (per-n 直列) 経路への検査として残す**。新経路には
   **別ゲートを新設**する = **緩和ではなく分岐**。
   ★**「既存 BIT-EXACT ゲート」という言い方はしない** — 現行 9b の合否は tol であって
   bit 一致は print のみ (§2 F2)。決裁 2 により、キルスイッチ経路の bit 一致は
   [G1] で **memcmp 合否化**される (§7)。**計測表へ「既存 BIT-EXACT ゲート維持」と転記しないこと。**
3. **G-8k の VRAM 予算を壊さない。** reserve 本体は `src/infer/diffusion.cu` の
   `reserve_arenas()` (`:264-283`)、live peak 定数は `:203-210`
   (`kArenaLivePeakUnetMiB = 5914` + `kArenaHeadroomUnetMiB = 128` → 64MiB 境界切り上げで **6080MiB**・
   計算は `arena_reserve_unet_mb()` `:218-245`)。
   よって `IM2COL_TILE_BYTES` (`conv2d.cu:56` = 256MB) は「**N 込みの合計上限**」として扱い、
   `rows_cap` (`conv2d.cu:354`) を N で割って **live peak を据え置く**。
   **上限緩和で効果を伸ばす案は本 Pkg のスコープ外。**
   観測点: `DOLLAMA_PROFILE=1` の終了時サマリ `[ALLOC] arena=... cap=... reserved=... live_peak=...`
   (`src/infer/unet.cu:1360-1368`) の **live_peak が 5914MiB を超えていないこと**。
4. **epilogue の前提は不変。** `conv2d_uses_gemm_bias_path` (`conv2d.cu:521-546`) は最終的に
   `use_gemm_path(1, ...)` に帰着する (同 `:544-545`) ので、「GEMM 経路 = bias 2 段丸め」の性質は変わらない。
   `src/infer/unet.cu:574` (`fuse1`) / `:625` (`fuse2`) のガードは**無改修**。

---

## 5. 数値方針

- strided batched はタイル選択が変わるため **bit 一致は狙わない** (G-2k S2 と同種の ~1 ULP)。
  **ただし「タイル選択差が入らないはずの場所」では bit 一致を要求する** — それが決裁 2 の
  [G2a] / [G2b] (§7)。
- **tol を主ゲートにしない (決裁 2)。** `test_conv2d.cu:76-84` の
  `rtol_for(K) = 5e-3 + 1e-5*K` / `atol_for(K) = 3e-3 + 2e-5*K` は UNet の K=2880 で
  **rtol ≈ 3.4% / atol ≈ 0.061** という幅になり、これを合否の主軸にはできない。
  → **tol に依存しない memcmp 構造不変量を hard ゲートに置く** ([G1] / [G2a] / [G2b])。
  既存 `compare(..., K, ...)` は **floor** として残す ([G2])。
- **[G2] の tol は新設しない・実測後に決めない。** 既存 `compare` をそのまま floor に使う。
  「初回実測を見て tol を決める」は**ゲートの自己成就なので禁止**。
  **既存 tol (floor) を超えたら緩めずに BLOCK して PL へ上げる** — タイル選択差では説明できない量であり、
  写像バグ・帯分割バグ・bias index バグのいずれかの証拠である。
- 実測は **MAE / max_abs / max_rel / exact 率**を必ず print する
  (9b が既に `:514-517` で MAE/max_abs/exact を、`compare` が `:120-121` で max_rel を出している形式を踏襲)。
  characterization として「G-2k S2 の per-sample MAE **6.4e-05** と同オーダーか」を併記する
  (**合否には使わない**。6.4e-05 は CLAUDE.md 計測表「UNet バッチ (G-2k S2)」行の既存記録の引用)。
- e2e の合否は既存 `test_diffusion_batch2` の **GATE2 = batch2 vs off @g=1.0 SSIM ≥ 0.999**
  (`src/tests/test_diffusion_batch2.cu:360-372`) を流用する。
- **g>1 で FP16 微差をゲートしない。** 出典は `docs/fast-mode-plan.md:244`
  (「**教訓 (非交渉)**: CFG 増幅下 (g>1) で FP16 の微差をゲートしてはいけない。被験変数は g=1.0 で分離する。」)
  と `:345-347` (増幅の機序と G-4k S3 初版の実害 0.996533 < 0.999)。

### ★resnet 代表形状 (本書で「代表形状」と呼ぶのはこの 3 つだけ)

**320ch/128² ・ 640ch/64² ・ 1280ch/32²**。

- 出典: `src/tests/test_conv2d.cu:562`「G-4k S2 と同じ 6 形状 (B1/B2 × 320ch/128² 640ch/64² 1280ch/32²)」・
  実ケースは同 `:696-701` (`b1_320_128` / `b2_320_128` / `b1_640_64` / `b2_640_64` / `b1_1280_32` / `b2_1280_32`)。
- **SDXL UNet の解像度レベルは 128 / 64 / 32 の 3 段しかなく、16² は存在しない**
  (`src/infer/unet.cu:1076` "128->64" / `:1094` "64->32" / `:1119` "32x32" / `:1144` mid "1280, 32x32" /
  `:1167` "32->64" / `:1204` "64->128" / `:1241` "128x128"。チャネルは `:939` `C0=320, C1=640, C2=1280`)。
  → **`1280/16²` は実在しない形状なので、どの節にも書かない。**
- `320/64²` / `640/32²` は「downsample 直後の先頭 resnet の Cin」としてなら実在するが、
  **その読みで書くときは「代表形状」の語を外す** (混同すると最大 Ncol=16384 の 320/128² 帯が
  検査から抜け、128² 固有の不具合を素通しする)。
- **帯分割が発動する形状は本書で断定しない。** `rows_cap` を N で割った後にどの形状で発動するかは
  **実装後の実値に依存する**。実装者が `rows_cap` の実値から 1 つ選ぶ。
  選定の足がかりとして、N=1 における形状ごとの col サイズと 256MB 上限の関係は
  `test_conv2d.cu:421-428` のコメントに既に記録されている (例: `:425` は
  「up_block_2 単帯 (帯分割なし): Cin=640 Cout=320 128²」・`:426` は
  「up_block_2 concat (帯分割発動): Cin=960 Cout=320 128²」)。**これは N=1 の値であって N=2 の値ではない。**

---

## 6. タスク表 (T2b〜T9・完全直列)

**実行順**: **T2b → T2c → T3 → T4 → T5 → T6 → T7 → T7b → [T7c] → T8 → T9**。
**GPU 実走の並列は禁止** (同一 GPU を 2 プロセスで叩いた秒は意味を失う)。
**T7b は実走なしなので GPU を占有しない** (両機で実施可)。
**着手条件付きのタスクは無い** (§2 の F1 / F2 / F3 すべて決裁済み)。
★**T2b → T2c の順序は強制** (T2c は `conv2d` 未変更のツリーで取る = 第 3 のアンカー・下記の理由節)。
★**T6 が赤なら T7 以降を走らせず T4 へ差し戻す** (先行必須ゲート)。

| # | 段 | 担当 | 触るファイル | DoD |
|---|---|---|---|---|
| **T2b** | S0a | cuda-kernel-dev | `src/infer/diffusion.cu` (**`generate_txt2img` = `:732-981` のみ**) | **F3 の解消 = reset + 内訳 dump の追加** (`generate` 側の `:554` reset / `:686-714` dump と同種)。**`conv2d` には触らない。単独コミット。** DoD: ①`DOLLAMA_PROFILE=1` で `generate_txt2img` が内訳を印字する ②**同一プロセスで `generate_txt2img` を連続 2 回呼び、2 回目の `cat_resnet_sec` が 1 回目の ~2 倍にならないこと** (= reset が効いている) を検査する。置き場所は **`test_diffusion_batch2` の parity 節 (steps=4 = `src/tests/test_diffusion_batch2.cu:236`) 内**。★**語を間違えないこと**: `generate` (`src/infer/diffusion.cu:533`) は**実在する別メソッドで既に reset を持つ** (`:554`) ので、「連続 2 回の `generate`」と書いて字義どおり実装すると**被験 (txt2img 側に新設する reset) を一度も通らずに緑になる** (F1 / T6② / F3 に続く **4 回目の同型**。この文言ミスは PL 自身が作りかけ、Fable 監査が実装前に捕まえた)。置き場所の根拠 (現物確認): parity 節は `gen` ラッパ (同 `:247-253`) 経由で 1 プロセス内から `generate_txt2img` を呼ぶ。`run_config` の呼出は **4 箇所** (`:284` off / `:289` batch2 単独 / `:294` fst / `:300` epi = fast+epi)。**`g1_again` 付きで呼ばれるのは off と epi の 2 構成**で、その 2 つが **1 構成あたり 4 回** — guidance 3 本 (`:267-270` の `guids[]` ループ) + **g=1.0 の再走** (`:273-276` の `g1_again`・同一構成/同一 seed/同一 guidance)。on / fst は `nullptr` = 3 回。**同一構成・同一 guidance の 2 連走がすでに存在する** (GATE1 / GATE3 がそれを使う) ので、そこに ② を載せられる。★★**負のコントロールを必須にする**: **T2b 着手前のツリー (= 研究機セッションの開始状態) で ② が赤になることを先に確認し、報告に含める。** これが「検査が被験を通っている」ことの一次証拠。T2b は研究機セッションの最初のタスクなので着手前ツリーがそのまま手元にあり、**追加コストはほぼゼロ**。★**負のコントロールの出力はファイルへ落として `docs\logs\g10k-baseline\` へ退避する** — 前例 (G-8k S6 **F1** の `test_http` 直列化ゲート) では負のコントロールが「**空撃ちでないことを支える唯一の証拠**」だったのに**走行出力がコンソールのみでファイルに落ちておらず**、セッション ID 付き temp ごと消える状態だった (出典 `docs/measurements-log.md:223-230`)。(★PL は「G-8k S6 の **F5**」を空撃ち疑義の前例に挙げたが、**F5 は「既存 exe 内にゲートを増やす / 新規 meson test 0 本」の前例** (`docs/g8k-review-fix-plan.md:161`, `:285`) であって、負のコントロールで空撃ちを潰した前例は **F1** である。出典を訂正した。)③**profile OFF 時の出力バイト列が変わらない**: 比較対象は **T2b コミット前後の同 seed 生成物**、手段は **sha256 突合** (G-8k S6 で確立した型・CLAUDE.md G-8k 行「出力 sha256 全一致」)。★**「同一ツリーの二連走 bit 一致」で代用してはいけない — それは determinism 検査であって無改変証明ではない**。この罠は `src/tests/test_diffusion_batch2.cu:377-380` の GATE3 注記が明文化している (「二連走は同一バイナリ内の再実行ゆえ、epilogue を壊しても壊れた同じコードが 2 回走って bit 一致する」)。④`meson test` 全緑。★**新規 meson test ターゲットは作らなくてよい** (既存 exe 内のゲートで足りる = G-8k S6 F5 と同じ型)。★**印字してよい欄 / いけない欄は下記「T2b の禁止事項」を必読** |
| **T2c** | S0b | gpu-benchmarker | 実走のみ | **T2b 適用直後・`conv2d` 未変更のツリー**で DB2_BENCH (profile ON) を **1 プロセス**走らせ、**batch2 の resnet バケットを史上初採取**。★**env を T7 と完全一致で pin する**: `DB2_BENCH=1 DB2_BENCH_STEPS=20 DB2_BENCH_ITERS=1 DOLLAMA_PROFILE=1`・`DB2_BENCH_G` は**既定 7.5 のまま**・**seed 同一**・**1 プロセス**。**既定値に依存させない** — 条件が 1 つでも違えば per-generation 値の比較が成立せず、**アンカー 3 の偽陽性を自作することになる**。★★**ビルド成果物 (exe) を保存する**: アンカー 3 が帯を外れたときの手順 2 段目 (T7 のセッション内で隣接再走して**同一セッション比較へ変換**する) は、**T2c 時の exe が残っていないと実行不能**で、**帯超過が永久に未解決のまま記録に残る**。保存先は生ログと同じ `docs\logs\g10k-baseline\` の隣で可 (repo に入れない大きさなら**研究機の永続パス**に置いて所在をログに明記する)。★**セッション ID 付き temp は不可** — T2b 行で引用した F1 の教訓 (`docs/measurements-log.md:223-230` = 唯一の証拠がセッション ID 付き temp ごと消える状態だった) は **exe にも適用される**。temp に置くと T2c → T7 が別セッションになった時点で 3 段手順の 2 段目 (T2c exe の隣接再走) が実行不能になり、**帯超過が永久に未解決のまま記録に残る**。**判定なし・採取と提出のみ。** 生ログを `docs\logs\g10k-baseline\` へ (`2>&1`)。★**位置づけ = 第 3 のアンカー**: 下記「T2c を T4 に混ぜない理由」を参照 |
| T3 | S1 | cuda-kernel-dev | `src/kernels/gemm.cu` / `src/kernels/gemm.cuh` / `src/tests/test_gemm.cu` | batched GEMM ラッパを**純追加** (既存関数は無改変) + **`test_gemm.cu` に「batched 1 発 vs 既存 `launch_gemm_fp16` を N 回直列」の突合ゲートを新設して緑**。ケースには **conv の実使用形態 (全 n が同一の重みを参照する形) を代表するもの**を必ず含める + `DOLLAMA_GEMM=wmma` フォールバック経路も 1 ケース。既存 `test_gemm` の全ケース (`test_identity` `:154` / `test_small_square` `:176` / `test_rectangular` `:192` / `test_transB` `:208` / `test_alpha_beta` `:224`) が**無改変で緑** |
| T4 | S2+S3 | cuda-kernel-dev | `src/kernels/conv2d.cu` / `src/tests/test_conv2d.cu` | conv2d の N>1 枝差し替え + キルスイッチ `DOLLAMA_CONV_BATCH=0` + **7 ゲート [G1] / [G2a] / [G2b] / [G2] / [G3] / [G4] / [G5] (§7) を追加して緑**。**加えて T6① で使う `bench_batch_vs_persample` の呼出を代表形状 3 つ (§5) へ拡充する** — 現物の呼出は `test_conv2d.cu:899` の 320ch/64² **1 本だけ**なので、実走担当 (T6) には追加できない (T6 は実走のみ)。★★**構造保存の要件 (2026-08-24 決裁・レビュー可能な形で満たすこと)**: **`DOLLAMA_CONV_BATCH=0` のとき、現行 `src/kernels/conv2d.cu:487-507` の per-n ループが構造的にそのまま実行される**こと。**新設の N 対応関数を経由しない・`rows_cap` の N 分割 (`:354` / 不変条件 3) を通らない。****差分レビューで「キルスイッチ経路が旧コードへ分岐している」ことが読み取れること。**理由: 不変条件 3 の「`rows_cap` を N で割る」設計が**キルスイッチ経路にも漏れる**と、`CONV_BATCH=0` は **bit 一致 ([G1] 緑) のまま帯分割回数だけ増えて秒が悪化する** — **これを秒で検出しようとしたのがアンカー 3 の設計の誤りで、構造で封じるのが正しい** |
| T5 | S4a | gpu-benchmarker | 実走のみ | ① `meson test -C build` 全緑 (**現状 53 本** + 追加分。53 は `src/meson.build` の `test(` 定義数を本セッションで実数カウント) ② `test_diffusion_batch2` GATE1-4 (**主 = GATE2** batch2 vs off @g=1.0 SSIM≥0.999) ③ `prof_arena_e2e` で G-8k 回帰 = step ループ内 **実 cudaMalloc 0 / cudaFree 0 / chunk_alloc 0** (`d_cudaMalloc` / `d_cudaFree` / `d_chunkAlloc` は `prof_arena_e2e.cu:135-137` が同じ行で出す) / peak delta は**同一セッションの `DOLLAMA_POOL=0` 比** / **`[ALLOC] reserve shortage:` が出ていないことの明示確認** ④ **生ログを `docs\logs\g10k-*\` へ退避** (§10 規律) |
| T6 | S4b | perf-profiler | 実走のみ | **先行必須ゲート (合否あり)・これ 1 本に縮小** (2026-08-24 決裁で旧 ② = e2e resnet の独立走行は廃止)。`bench_batch_vs_persample` (`test_conv2d.cu:725-795`) で **batched median / seq median ≤ 0.95**。形状は §5 の代表形状 3 つ + [G4] の帯分割ケース。**合否集約 = 全形状で ≤ 0.95** (1 形状でも超えたら赤。形状ごとの実数値を必ず併記)。**赤なら T7 を走らせず T4 へ差し戻す** (conv 単体で速くなっていない = 実装側の問題)。**この順序は強制**。★per-call ms なので **e2e 秒には翻訳できない** — 秒の主張には使わない。★**`prof_unet_fast_warm` は使わない** (§2 F1)。**使わなかったことと理由を報告に明記する**。★**生ログを `docs\logs\g10k-resnet\` へ** (`2>&1`)。 |
| T7 | S4c | gpu-benchmarker | 実走のみ | **数値採取と生ログ提出のみ・判定しない**。`DB2_BENCH=1 DB2_BENCH_STEPS=20 DB2_BENCH_ITERS=1` を **`CONV_BATCH` 既定 → `=0` → 既定 の 3 プロセス連続**。各プロセスは 1 プロセス内で 3 構成 (default / attn+batch2 合成 / fast+epilogue = 出荷 `--fast`) を回す (`test_diffusion_batch2.cu:483-486`)。★**構成ごとに `generate_txt2img` が warmup 1 + iters 回**呼ばれる (同 `:465-477`) = ITERS=1 なら **1 構成あたり 2 回**。warmup 側は lazy init を含むので**判定に使わない(記録には残す)**。★**`DOLLAMA_PROFILE=1` で走らせる** (T2b 適用後は構成ごとに resnet バケットが印字される = §2 F3)。同期摂動は A/B 両側に等しく乗るので**比は保たれる**が、**絶対秒は非 profile の wall ではない** (正典 e2e 倍率は T7c で別に採る)。★**アンカー 3 の窓**: 本走行の `CONV_BATCH=0` の resnet バケット絶対秒が **T2c (conv2d 変更前) の値の帯内か**を**記録する** (帯 = ±max(アンカー 1 の実測ドリフト幅, 10%))。★**帯外でも結論を書かない** — 下記「帯を外れたときの 3 段」に従い、まず T2c の exe を**このセッション内で隣接再走**する。**採取中は間に他の GPU 負荷を挟まない**。**全 stdout+stderr を `docs\logs\g10k-e2e\` へ** (`2>&1`)。 |
| T7b | S4d | perf-profiler | **書き込みなし・実走なし (両機可)** | **T7 の生ログを読んで判定する**。① `default` 構成の**プロセス間変動 = ドリフト幅の直接測定値** (default は B=1 × 2 forward/step で `DOLLAMA_CONV_BATCH` の影響を**原理的に受けない**ので、動いた分はドリフト) ② `fast+epilogue` 構成の **プロセス A/C (既定) vs プロセス B (`=0`)** の差 ③ ②が①を超えているか ④動かなかった場合の律速診断 (im2col 帯域 / bias パス / launch 数)。★**合否は resnet バケットの削減率 (§8 ケース B の 3 帯)** で出す。分母は同一セットの **プロセス B (`CONV_BATCH=0`) の `fast+epilogue`**、被験は **プロセス A / C (既定) の `fast+epilogue`**。⑤ **アンカー 3 (参考対照・hard ではない)**: `CONV_BATCH=0` の絶対秒が **T2c の帯内か** (帯 = ±max(アンカー 1 の実測幅, 10%))。**帯外なら断定せず 3 段手順へ** — 復元性の hard 担保は **[G1] の memcmp** と **T4 の構造保存要件**が持つ。★**判定は絶対秒と削減率のみ。`%` は使わない (そもそも印字されない = T2b 禁止事項 1 の決裁)。**★**resnet バケットの比較は同一構成・プロセス間のみ** (`fast+epilogue` の `CONV_BATCH` 既定 vs `=0`)。**構成をまたいだ resnet の直接比較は禁止** — default は 1 step あたり 2 forward なので桁が合わない(危険 2)。**`default` 行の役割はプロセス間不変性 = ドリフト対照に限る。**★**`weight_upload=0` と `n/a` 欄は引用しない** (T2b 禁止事項 2・3)。**T7b が実走から独立していること自体が成果物**: 生ログが repo に入るので**後から誰でも追試できる** = G-8k 残債⑤ (`docs/measurements-log.md:202`「S3・S3b・S4・S6 (T2) の生ログは依然として無し」) の**再発防止形として G-10k の既定にする**。 |
| T7c | S4e | gpu-benchmarker | 実走のみ | **T7b が合格を出したときのみ実施**。profile OFF (`DOLLAMA_PROFILE` 未設定・`DB2_BENCH_ITERS` 既定) で A→B→A を 1 セット追加し、**正典 e2e 倍率**を採る。**合否なし** (CLAUDE.md 計測表用の正典値)。生ログは同じく `docs\logs\g10k-e2e\` へ。 |
| T8 | S5 | record-writer | `docs/measurements-log.md` (G-10k 節新設) / `docs/fast-mode-plan.md` / `CLAUDE.md` 計測表 1 行 | **実測値を持つ研究機セッション内で走らせる** (数値転記事故の削減)。**未発火の異常系を「実走で確認した」と書かない**。内容は下記「T8 の記録スコープ」を参照 |
| T9 | — | record-auditor | 書き込みなし | T8 の敵対的監査。BLOCK 収束まで T8↔T9 を回す (書き手と検査者を分ける = CLAUDE.md 実装作業のルール 4) |

### T2b の禁止事項 4 件 (守らないと記録が捏造になる)

1. ★**その経路で実測していない項目を印字しない。** `generate` の dump は **`generate` ローカルの計時で
   埋まる欄**を持つ。**`generate_txt2img` では埋まらない欄を 0.000s で印字してはいけない** —
   T7b / T8 がそれを実測値として拾う。**印字しないか `n/a` と明示する**。
   本セッションで `src/` を全数 grep して同定した内訳 (これをそのまま使ってよい):

   | dump の欄 | 加算箇所 | `generate_txt2img` で埋まるか |
   |---|---|---|
   | `steps=` (`unet_steps`) | `unet.cu:925` (`launch_unet_impl` 内) | ✅ ただし★**構成で意味が変わる** (batch2 は 1 step = 1 呼び出し / default は 1 step = 2 呼び出し) |
   | `weight upload+malloc` (`weight_upload_sec/_bytes/_count`) | `unet.cu:343-345` (重みアップロード時) | ⚠ **warm では計測窓の外** (下記 2) |
   | `UNet step total` (`unet_total_sec`) | `unet.cu:1298` (`launch_unet_impl` 内) | ✅ |
   | `-> UNet pure kernels (total-upl)` | 派生 (`unet - upl`) | ⚠ upl が窓外 0 なので **`UNet step total` と同値**になり「pure」の意味を失う |
   | `embed / down / mid / up / conv_out` | `unet.cu` (`launch_unet_impl` 内) | ✅ |
   | **`resnet` / `transformer` / `attention only`** | `unet.cu:542` 他 (`resnet_block` / transformer 内) | ✅ **← T2b の目的はこの 3 行** |
   | `VAE decode` (`vae_sec`) | **`diffusion.cu:655` = `generate` のみ**。`generate_txt2img` の decode は **`:944` で無計時** | ❌ **埋まらない** |
   | `host roundtrip` (`host_roundtrip_sec`) | **`diffusion.cu:613`, `:642` = `generate` のみ**。`generate_txt2img` は `:854-862` 等で同等処理をするが**無計時** | ❌ **埋まらない** |
   | `TOTAL` (`total_sec`) | **`diffusion.cu:676` = dump ブロック内で `generate` ローカルの `prof_t_total` から算出** | ❌ 起点が無いと埋まらない |
   | **`%` 列すべて** | `pct()` = 分母が `total_sec` (`:678`, `:683`) | ❌ **`total_sec` が無いと表の % 列が全部無意味になる** |

   → ★**決裁 (2026-08-24・全件クローズ): (B) を採用 = `%` 列は出さない。
   `total_sec` / `host_roundtrip_sec` はこの経路で埋めない。**
   **理由は「最小だから」ではなく「印字した数値は必ず誰かに引用されるから」**:
   `total_sec` を入れると **同一の `generate_txt2img` 呼び出しに対して 2 つの wall 秒が並存する** —
   ① 新設の `total_sec` (profile 計装 ON・同期込み・dump 側の窓) ② `test_diffusion_batch2.cu:470-475` の
   `steady_clock` による窓 (min は `:476`・min-of-iters)。**窓も条件も違うので必ず食い違い、
   記録側は毎回それを注記して回る羽目になる**。これは**今回 3 回続いた事故と同じ構造
   (数値と、その数値が何を測っているかの乖離) を、新しい数値でもう 1 本作る行為**である。
   G-10k に要るのは **resnet バケットの絶対秒と削減率だけ**なので、要らない数値を出す理由がない。
   ★**第三案 (`unet_total_sec` を分母に % を出す) も却下**: 「**同じ表形式で 2 種類の % が流通する**」のが
   最悪で、しかも **`unet_total_sec` は VAE を含まない** (`vae_sec` は別欄・`diffusion.cu:655`) ため、
   同じ「resnet 30%」が一方では**対 UNet**・他方では**対全体**を意味する。**誤引用が確定する形は採らない。**
   ★**0 を入れて % を印字する形も当然禁止** (0 除算回避で `1e-9` が分母に入り (`:678`)、% が発散値として残る)。
2. ★**`weight_upload` を warm の証拠として使わせない。** warm 経路では重み転送は
   **ハンドル生成 (ctor) で完了しており計測窓の外**なので、この dump の `weight_upload=0` は**循環論法**。
   印字するなら「**窓外・0 は warm の証拠ではない**」と併記し、**T7b / T8 では引用禁止**。
3. ★**reset のスコープ**: 計測窓外で溜まった値 (上記 1・2) を黙って 0 にして、
   それを「**実測 0**」として記録に流さない。
4. **新規 meson test ターゲットは作らなくてよい** (既存 exe 内のゲートで足りる = G-8k S6 F5 と同じ型)。

**T2b への追加指示 3 件 (2026-08-24 決裁)**

5. ★**表を `generate` 側の表と見分けがつく形にする。** 同一レイアウトのまま `%` 列だけ抜くと、
   切り貼りで並べられて「**% が欠けた同じ表**」と読まれる。**ヘッダを別文言にし、
   「絶対秒のみ」「`total_sec` をこの経路では測っていない」ことを表題に含める。**
   埋まらない欄は 0 を出さず、**行を落とすか `n/a`**。
6. ★**危険 2 (`unet_steps` の意味が構成依存) の警告をログ本文にも埋める。**
   **batch2 は 1 step = 1 batched forward / default は 1 step = 2 forwards** であることを、
   **docs だけでなく印字するヘッダにも 1 行**入れる。理由: **docs 側の注記は切り貼りで剥がれるが、
   ログに埋めた警告は数値と一緒に運ばれる**。
   ★**この型の実在する前例 (本セッションで現物確認・出典を訂正)**: PL は
   `src/kernels/conv2d.cu:31-40` を挙げたが、**そこは printf ではなくソースコメント**
   (「帯分割の正当性に関する重要な注意」) なので「**ログに埋めた警告**」の前例としては当たらない。
   **印字物に注記を埋めている実在の前例**は
   `src/kernels/device_arena.cu:350-358` (`[ALLOC] reserve shortage: ... (falling back to chunk growth;
   reserve is undersized -- see reserve_arenas() in src/infer/diffusion.cu)` = 警告 + 直し方のポインタを
   ログ行そのものに埋める。文面を arena 非依存にした理由が `:341-346` に明記) /
   `src/infer/diffusion.cu:279-280` (`(set DOLLAMA_ARENA_RESERVE_MB=0 to disable)`) /
   **`src/tests/test_diffusion_batch2.cu:504`, `:514`** (`(characterization・合否なし / ハード判定は
   parity 節 GATE4 @g=1.0)` = **数値の隣に「何に使ってよい数値か」を書く型** — T2b が真似るべきはこれ) /
   `src/tests/prof_unet_fast_warm.cu:153` (`(must be ~0 when warm)`)。
   ★**同時に反例も見ておくこと**: 同 `prof_unet_fast_warm.cu:155-156` の
   `(gate: <=0.95s / stretch <=0.85s / baseline 1.225s)` は**埋め込んだ後に陳腐化した**
   (§12-4 の残債)。**ログに埋める文は「陳腐化しない事実」に限る** —
   構成依存の意味 (step の数え方) は不変なので適格、**閾値や分母は不適格**。
7. ★**親切心で `total_sec` を足さないこと。** 「% が無いと表として不自然だから」で埋めるのを**明示的に禁止**。
   埋めるのは**別 Pkg の決裁事項** (§12-6 の宿題)。

### アンカー 3 本と、アンカー 3 の位置づけ (2026-08-24 再決裁)

- アンカー 1 = **プロセス 1 と 3 の同一構成再測** (ドリフト幅)
- アンカー 2 = **`default` 行のプロセス間不変性** (`DOLLAMA_CONV_BATCH` の影響を原理的に受けない)
- アンカー 3 = **T2c (`conv2d` 変更前) vs T7 の `CONV_BATCH=0` (変更後)** = **キルスイッチ復元性の
  「秒レベルの参考対照」** (★**hard 検査ではない**。下記)

**T2c を T4 に混ぜない理由**: `conv2d` 変更後のツリーでは「変更前の `=0`」を作れないので、
**混ぜると取れない**。だから T2b → T2c → T3 → T4 の順序は強制する。

#### ★アンカー 3 は「復元性の hard 検査」ではない (初版の設計誤りを是正)

**初版はアンカー 3 を「キルスイッチが旧経路を本当に復元しているかを数値で押さえる」検査として書いた。
これは本書自身の 2 つの原則に違反していた**:
① §3「★分母の決裁」= **絶対秒の据え置きはドリフトに耐えない**
② §8 ケース B「マージンを対照の代替にしない」= **別セッション跨ぎの差を対照なしで読めない**
T2c と T7 は**別セッション**なので、アンカー 1 (同一セッション内の幅) をそのまま当てられない。
★**PL の弁**: 「アンカー 3 を良い設計と評価したのは私の誤り。文書自身の原則を、自分で作ったアンカーで破っていた」。

**再定義 (3 点)**:
1. **「一致」は帯で定義する** — **帯 = ±max(アンカー 1 で実測したドリフト幅, 10%)**。
   ★**幅の正規化 = |P1 − P3| / P1 (先行プロセス P1 比)**。理由: 床の根拠である下記 **+9.9%**
   自体が前値比 ((1.41299 − 1.28564) / 1.28564 = 0.099) で算出されており、`max(実測幅, 10%)`
   の両辺を同一の正規化で比較するため (平均比だと同じ実測が 9.4% になり床との対応がずれる)。
   下限 10% の根拠は G-4k S3 の実測ドリフト **+9.9%** (`docs/fast-mode-plan.md:310-313` の
   同一走行内 default 再測 **1.28564s → 1.41299s**)。
   ★**`max()` を採る理由**: アンカー 1 は**同一セッション内**の幅なので、**別セッション跨ぎの
   アンカー 3 に対しては下限としてしか使えない**。跨ぎの方が狭いという主張には根拠がないため、
   **実測幅が 10% を下回っても帯は 10% で床を張る**。
   比較する量は「**同一構成 (`fast+epilogue`) の resnet バケット絶対秒**」に限る。
2. **帯超過時は断定しない**。手順は下記「帯を外れたときの 3 段」。
3. ★**数値的な復元性の hard 検査は [G1] の memcmp が担う** (§7)。
   **秒側の hard 担保は T4 の「構造保存の要件」**(キルスイッチ経路が旧コードへ分岐していることを
   差分レビューで確認する) が担う。**アンカー 3 はその 2 枚の上に載る「確認の窓」に降格する。**

#### なぜ降格してよいのか (この論理が成立して初めて降格できる)

アンカー 3 が本当に捕まえたい失敗形は、**承認済み設計から自然に生えるもの** — すなわち
**不変条件 3 の「`rows_cap` を N で割る」がキルスイッチ経路にも漏れる**ケースである。
そのとき `CONV_BATCH=0` は **bit 一致 ([G1] 緑) のまま帯分割回数だけ増えて秒が悪化する**。
**この失敗形を秒で検出しようとしたのが誤りで、構造で封じるのが正しい** (T4 の DoD に追加済)。
→ 復元性は **[G1] (bit) + 構造保存 (秒の原因側)** の 2 枚で担保される。
**2 枚が揃った時点で、アンカー 3 は「万一を見る窓」に落として差し支えない。**
逆に言えば、**T4 の構造保存要件を満たさないままアンカー 3 を窓に降格させてはいけない。**

#### 帯を外れたときの 3 段 (事前に固定・実走後に決めない)

1. ★**結論を書かない。「非復元の証拠」という語を使わない。**
2. **T2c 時に保存した exe を、T7 のセッション内で隣接再走**して**同一セッション比較に変換**する。
   これで初めてアンカー 1 の幅を正当に適用できる。
   - **T2c の DoD に「exe / build 成果物を保存する」が入っているのはこのため。**
     保存していないとこの手順が実行不能で、**帯超過が永久に未解決のまま記録に残る。**
   - 再走は **SAC 状態を変えないまま同一セッション内で行う**。
     ★**「既走の exe は allow-list 済みだから大丈夫」とは書かないこと** (裏取りしていない主張)。
3. 隣接再走でも帯外なら **「非復元の疑い」として PL へ**。
   **そのとき初めて**構造レビュー ([G1] と T4 の構造保存要件) に戻る。

### T8 の記録スコープ (決裁で確定した分)

1. **ゲート設計の誤りの是正** (数値更新ではない): `docs/fast-mode-plan.md:212-215` / `:318-324` の
   「resnet ≤0.95s を G-10k 後の再 profile へ再割当」という **G-4k の決裁が、被験変数を通らない計器
   (`prof_unet_fast_warm` = B=1) に紐づいていた**こと。G-4k S3 の「測っている変数が epilogue ではない」と
   同型の誤りが**計器側**で起きた、という認定を書く。
2. **分母の退役**: B=1 の 1.212s は G-10k の合否分母から退役し、判定は**削減率**へ移った (§3 / §8)。
3. **`docs/fast-mode-plan.md:691`** の stride 写像案に「**起草時の未検証提案**」注記を付す
   (**削除しない**。G-8k 節で確立した「当時の記述は履歴として残し現況を別に書く」型 = `:463-466`)。
4. **同 `:690`** の行番号に「現物 `src/kernels/conv2d.cu:487-507` (2026-08-24 確認)」を**併記**
   (当時の記述は消さない)。
5. **`DOLLAMA_PROFILE=1` の絶対秒を正典 e2e 値として引用しない。** `ScopedSyncTimer`
   (`src/infer/unet.cu:542`) がバケットごとに同期を入れるため計装 ON の秒は膨らむ。T7 の e2e 秒は
   「**計装 ON 条件下の参考値**」、正典は **T7c** (条件付き実施) の値。
   ★**削減率のような「計装 ON 同士の比」は同期の影響を受けない** — 合否を計装 ON 側に置ける理由はこれ
   (ただし現状 resnet バケット自体が取れない = §2 F3)。
6. **`default` 行を分母にしない。** default は **B=1 × 2 forward/step** で batch2 構成とは
   仕事の分割が違う。分母は**同一 `fast+epilogue` 構成の `CONV_BATCH=0`** (§3 の 2 層構造)。
   **`default` 行はドリフト対照専用。**
7. **`DB2_BENCH_ITERS=1` では 1 構成あたり `generate_txt2img` が 2 回**呼ばれる
   (warmup 1 + 計測 1・`src/tests/test_diffusion_batch2.cu:465-477`)。**warmup 側は lazy init を含むので
   判定に使わない (記録には残す)。**
8. **`prof_unet_fast_warm` は G-10k では使っていない。** かつ
   `src/tests/prof_unet_fast_warm.cu:155-156` に `(gate: <=0.95s / stretch <=0.85s / baseline 1.225s)` の
   **退役済み絶対値ゲートが出力文字列として残っている**事実を**残債として記載**する
   (コード修正は G-10k のスコープ外・別タスクの宿題 = §12-4)。
9. **マージンは対照の代替にならない**という却下理由を明文化する (§8 ケース B の該当ブロック)。
   次に「−15% のマージンで足りるのでは」と考える人のために残す。
10. **★§2 F3 とその決裁 (①採用 = T2b で計装) を記録する。** F1 と同型の誤りが
   **本引き継ぎ書の計器指定にも入っていた**こと、**PL の決裁もその誤りを一次証拠なしに採っていた**こと、
   および §2 の「計器を指定する決裁の規則」を記録に残す。
11. ★**batch2 resnet バケットは「G-10k 時点で初めて測れた絶対値」として記録し、退役した B=1 の
   1.212s / 1.225s とは別系列であることを明記する。** **2 系列を同じ表・同じ文で比較しない**
   (混ぜると 4 回目の同型事故)。削減率 (−22% / −15%) の定義は維持。
12. **T2b で `n/a` にした欄・`weight_upload=0`・warm 判定は引用しない** (T2b 禁止事項 1〜3)。
13. **アンカー 3 (T2c vs T7 の `CONV_BATCH=0`) は「帯内か / 帯外か」で記録する。**
   帯 = ±max(アンカー 1 の実測ドリフト幅, 10%)。★**帯外でも「非復元の証拠」と書かない・結論を書かない。**
   **帯外だった場合は「隣接再走を行ったか」「その結果」までを書き、未解決なら未解決と書く。**
   ★**復元性の hard 担保は [G1] の memcmp と T4 の構造保存要件であり、アンカー 3 は参考対照**である旨を
   明記する (アンカー 3 を hard 検査として記録すると、本書が是正した設計誤りが記録側で復活する)。
14. ★**`%` 列は存在しないことを記録する** (T2b で (B) 採用)。**`%` を引用した記述を作らない。**
   却下理由 (同一呼び出しに 2 つの wall 秒が並存する / `unet_total_sec` 分母の % は VAE を含まず
   誤引用が確定する) も 1 行残す — 次に「% を足そう」と考える人のため。
15. ★**「監査 3 巡で計器設計が 3 回差し替わったが `src/` の変更は 1 行も発生していない」を記録する。**
   一次証拠の補正 4 件もすべて実装着手前だった。**「監査が重い」ではなく「実装前に潰すのが正しい」と
   読める形で書く** (§2 の計器規則末尾に根拠を置いた)。
   ★**併せて「監査が PL 決裁の欠陥を 2 巡連続で捕まえた (T6② の計器誤り / アンカー 3 の帯なし跨ぎ比較)」
   ことも記録する** = 書き手 / 査読者 / 決裁者の**三分割が効いている証拠**であり、
   **PL の決裁を検査対象外にしないため**の記録。
16. ★**`.claude\agents\perf-profiler.md` は T8 のスコープ外** — 2026-08-24 のユーザー決裁で
   **別途更新済み**である (§12-2)。**T8 で二重に直さないこと。** 計測表・docs 側へ
   「エージェント定義にも退役分母が残っている」と書き足すのも不要 (もう残っていない)。

**T3 を T4 から分離したのが要**: stride 写像の正誤を **GEMM 単体で確定させてから** conv に載せる。
混ぜると「conv の出力が違う」ときに ①写像バグ ②im2col の n 対応バグ ③帯分割バグ の切り分けができず、
G-2k S3c と同じノイズ床切り分けを最初からやり直すことになる。

---

## 7. T4 の 7 ゲート

**方針 (決裁 2)**: tol に依存しない **memcmp 構造不変量を hard**、既存 `compare` は **floor**。
実装方法は指定しない — **検査すべき性質**だけを書く。

| ゲート | 検査すべき性質 | 強度 |
|---|---|---|
| **[G1]** (昇格) | `DOLLAMA_CONV_BATCH=0` (per-n 直列) の N=2 出力が、N==1 per-sample 参照と **memcmp 一致**すること。既存 9b の 5 ケース (`batch2_1x1` / `batch2_1x1_bias` / `batch2_3x3_same` / `batch2_3x3_s2` / `batch2_unet_c320_64` = `test_conv2d.cu:531-544`) に対して行う | **hard bit-exact**。CLAUDE.md 計測表が「conv2d S1 は per-n ループで **BIT-EXACT**」と既に**観測済みの性質**なので、新たな要求ではなく**既存資産の固定** |
| **[G2a]** (新設) | 新経路で **sample0 と sample1 に同一データ**を入れたとき、出力の前半と後半が **memcmp 一致**すること | **hard bit-exact**。両サンプルは同一 stride を通るのでタイル選択差が入らない。**stride / オフセット誤り・サンプル間の漏れを tol 無しで捕まえる** |
| **[G2b]** (新設) | 新経路で (X,Y) を入れた出力と (Y,X) を入れた出力が、**入れ替えで memcmp 一致**すること | **hard bit-exact**。**非対称バグ (2 本目だけ壊れる形) を捕まえる** |
| **[G2]** (既存維持) | batched vs per-sample 参照を既存 `compare(..., K, ...)` (`test_conv2d.cu:87-123`) で判定。形状は §5 の**代表形状 3 つ**を含める | **floor** (超えたら緩めず **BLOCK**)。MAE / max_abs / **max_rel** / exact 率を print し、G-2k S2 の per-sample MAE 6.4e-5 と同オーダーかを **characterization** として記録 |
| **[G3]** | 新経路で**同一設定 3 runs が memcmp 一致**すること。方式は G-8k S1b の `test_g8k_arena_bitexact` (`test_conv2d.cu:692-719`・設計コメントは `:548-564`) と同型 | **hard**。「たまたま同じ」の排除 |
| **[G4]** | `rows_cap` (`conv2d.cu:354`) を N で割った結果**帯分割が発動する形状**を 1 ケース。**具体形状は実装者が `rows_cap` の実値から選ぶ** (§5 末尾)。帯分割 (`banded` = `conv2d.cu:366`) + batched + scatter (`scatter_band_to_out`・設計は `conv2d.cu:31-40` / `:162-170`) の組み合わせが未検査で残るのを防ぐ | **hard** ([G2a]/[G2b]/[G3] と同じ性質をこの形状でも通す) |
| **[G5]** | GEMM 下限割れの N=2 形状 = 既存 5. `test_conv_batch_multi` (`test_conv2d.cu:384-387`・N=2 / Cin=5 / 14×18 / Cout=7 / 3x3) が **direct 経路のまま**通ること。(`use_gemm_path` の下限は `Cout < 16 \|\| Ncol < 16 \|\| K < 16` で false = `conv2d.cu:299-302`。この形状は Cout=7 で下限割れ) | **hard** |

---

## 8. 不合格時の分岐 (事前に固定・実走後に決めない)

### ケース A: GATE2 (@g=1.0 SSIM≥0.999) 落ち → **陰性クローズ不可。実装バグとして切り分ける。**

conv 単体ゲート ([G2a]/[G2b]/[G2]) が緑で GATE2 が赤なら、conv 単体では通るのに e2e で発散 =
特定形状・特定経路の写像ミスか step 累積。手順:

1. `DOLLAMA_CONV_BATCH=0` で GATE2 が回復するか。回復すれば**発散源が新経路に確定**
   (G-2k S3c のノイズ床切り分けと同型)。
2. conv 単体ゲートに **§5 の代表形状 3 つ (320ch/128² ・ 640ch/64² ・ 1280ch/32²)** と
   **1x1 経路**、および **帯分割が発動する形状 1 つ (実装者が `rows_cap` の実値から選ぶ)** を足して、
   差が出る形状を特定する。
   ★**`1280/16²` は実在しない** ので書かない。`320/64²` 等の移行形状を足すのは可だが
   **「代表形状」と呼ばない** (§5)。**最大 Ncol=16384 の 320/128² を必ず入れる** —
   ここを落とすと 128² 帯固有の不具合 (タイル境界・Ncol 依存) を素通しして発散源の特定を誤る。
3. 不明なら PL へ上げる。

**tol 緩和と g>1 での再判定は禁止** (§5)。

### ケース A': [G2a] / [G2b] が赤 → **緩めず PL へ。tol へ逃がさない。**

[G2a] / [G2b] は「タイル選択差が入らない**はず**の場所」を突く hard ゲート
(その前提自体は cuBLAS の batched call 内の item 間一様性に関する**未検証の想定**である = §5 / §7)。
したがって **~1 ULP 級の差で赤になる可能性はゼロではない**。そのとき取る道は 1 つだけ:

- **memcmp を tol に置き換えない。閾値も入れない。** まず [G3] (3 runs 自己一致) を見て
  非決定性か系統差かを分け、`DOLLAMA_GEMM=wmma` で cuBLAS を外した経路でも同じか見る
  (`gemm.cu:376-386`)。そのうえで **PL へ上げる**。
- 前提 (item 間一様) が破れていたと確認できた場合に限り、ゲートの設計自体を PL が再定義する。
  **実装者や実走担当が現場で緩めてはいけない** (ゲートの自己成就の禁止・§5)。

### ケース B: 全ゲート緑・秒が動かない → **陰性クローズを許容。**

判定は **T7b が T7 の生ログから出す削減率**。分母は同一セットの **プロセス B (`CONV_BATCH=0`) の
`fast+epilogue` 構成** (§3 の 2 層構造)。**絶対秒での判定はしない** (B=1 の 1.212s は退役済み)。

★**3 帯の削減率定義 (−22% / −15%) は維持。ただし絶対秒は T2c と T7 で初めて確定する。**
batch2 構成の resnet バケットは **G-10k で初めて測れる量**であり (§2 F3 = T2b で計装)、
**退役した B=1 の 1.212s / 1.225s とは別系列**である。
★**この 2 系列を同じ表・同じ文の中で比較しないこと** — 混ぜると**4 回目の同型事故**になる。
T8 では「**G-10k 時点で初めて測れた batch2 resnet バケットの絶対値**」として記録し、
**別系列であることを明記**する (T8 記録スコープ参照)。
なお **e2e 倍率を合否へ昇格させないこと** (下の副次条件のとおり e2e はノイズが大きい = 元々合否に使わない設計)。

★**マージンを対照の代替にしない (決裁 2026-08-24・却下理由の明文化)**: 「−15% のマージンを取れば
同一構成の再測は要らない」は**誤り**。マージンは**差の大きさしか見ない**ので、
**ドリフトが有利方向に振れた場合 (偽の合格) を検出できない**。G-4k S3 で観測されたのは +9.9% の
減速方向だったが (`docs/fast-mode-plan.md:287`)、**方向が保証されている根拠はどこにも無い**。
対照 (プロセス 1 と 3 の同一構成再測 + `default` 行の不変性) は**方向を含めて**検出する。

| ② resnet バケットの削減率 (同一時刻帯 A/B) | 判定 | 既定 |
|---|---|---|
| **−22% 以上** | 目標達成 (G-4k 継承ゲート合格) | 新経路 **既定 ON** |
| **−15% 〜 −22%** | 部分的成功 (残りは G-9k / G-12k へ持ち越し) | 新経路 **既定 ON** |
| **−15% 未満** (ドリフト床の **~1.5 倍のマージン**を抜けない) | **秒中立 → 陰性クローズ** | **`DOLLAMA_CONV_BATCH=0` を既定**にし新経路を opt-in へ |

- **副次条件 (合否なし・裏付け)**: T7 の e2e `--fast` 相対倍率が **x1.33 帯から +0.02 以上**動くこと。
  e2e はノイズが大きいので**合否は ② で取る**。x1.33 帯の出典は既存記録
  (`docs/fast-mode-plan.md:288` 「主指標は相対倍率 x1.33」・CLAUDE.md 計測表 G-4k S3 行の x1.33439)。
  比較する構成は **fast+epilogue (= 出荷 `--fast`)** の列に固定する (T7 の 3 構成のうちどれで見るかを揺らさない)。
- ドリフト床 **±9.9%** は既存記録の引用: G-4k S3 の同一走行内で default 自身の resnet バケットが
  1.28564s → 1.41299s と揺れた実測 (`docs/fast-mode-plan.md:287`, `:310-313`)。本セッションで
  再測はしていない。**−15% は「床 ~1.5 倍のマージンを取った値」**であって「床と等しい」わけではない。
- 秒中立なら、**bit 一致という既存資産** (per-n 直列は N==1 と完全同一の蓄積順 =
  `conv2d.cu:483` のコメントが設計意図として明記・[G1] で memcmp 固定) を
  tol 内の数値差と引き換えに手放す理由がない。
- ただし**陰性でも revert はしない**。opt-in として残し、「conv が batch されても秒が動かなかった律速は
  どこか (im2col 帯域 / bias パス / launch 数)」の診断を記録に残す。G-4k S2/S3 と同じ扱いで、
  次の Pkg (G-9k / G-12k) の入力にする。

---

## 9. 実装者への危険箇所 (現物確認済み・答えは書かない)

1. **`conv_bias_add_rows` を「N 倍で 1 発」にする短絡は壊れる。**
   現物 `conv2d.cu:149-160` は形状引数が `int Cout, long Ncols` の 2 つだけで、
   `const int co = static_cast<int>(idx / Ncols);` = **`row = idx / Ncols` の 1 段分解**を前提にしている。
   `Ncols` に `N*HW` を渡すと bias index が全滅する ([N, Cout, HW] レイアウトでは別の分解が要る)。
   引数追加か別カーネルが必要 — **答えは実装者が導いて単体ゲートで確認する**。
   ([G2a] は同一データを 2 本入れるので bias の**サンプル間の食い違い**は捕まえるが、
   **全サンプルで一様にずれる bias バグは捕まえない**。★受け皿は **[G2] floor と e2e GATE2** で、
   **検出下限は tol 幅** (rtol≈3.4% / atol≈0.061 = §5 が「ゲートと呼べない」と認定した幅)。
   **[G1] は数えない** — [G1] は `DOLLAMA_CONV_BATCH=0` = **per-n 直列経路**の検査であり (§7)、
   参照も per-sample N=1 呼びなので、**新経路 (batched) の一様バグには何も寄与しない**。)
2. **`[ALLOC] reserve:` の行が出ただけを reserve 成功と読むのは既知の誤読。**
   `DOLLAMA_POOL=0` でも出る (出典 `docs/fast-mode-plan.md:664-675`。機序は
   `reserve_arenas()` 自身に pool 判定が無く、printf が `device_arena_reserve()` 呼出の**後**にあり、
   no-op 判定は callee 側の早期 return で行われるため)。
   **判定は終了時サマリの `cap` / `reserved` / `chunks`** (`src/infer/unet.cu:1360-1368`)。
   reserve 不足は別行 `[ALLOC] reserve shortage:` (`src/kernels/device_arena.cu:350-358`) で
   **DOLLAMA_PROFILE 非依存・stderr へ無条件 1 回**出る。
   ★**採取時は必ず `2>&1`** — `[ALLOC]` のストリームは 2 系統に割れている (`docs/testing.md:26-30`)。
3. **帯分割は 1x1 経路には存在しない。** `conv2d.cu:306-312` のコメント (とくに `:310-311`) が
   「N=HW は単一 GEMM の C にそのまま全体テンソルへ書く (ldc=N=HW=全体行 stride) ので
   **帯分割問題は起きない**」と明記している。N 対応で 1x1 と im2col を同じ形に均そうとして、
   **1x1 に不要な band 経路を持ち込まないこと。**
4. **キルスイッチの getenv はキャッシュ型で書く。** 既存の型は `cublas_disabled()`
   (`src/kernels/gemm.cu:376-386`) と `device_arena_pool_enabled()`
   (`src/kernels/device_arena.cu:210-232`)。**プロセス単位で固定される**ため、T6/T7 の A/B は
   **構成ごとに別プロセス**になる (T7 は A→B→A の 3 プロセス = §2 N3 / §6 T7)。
   **同一プロセス内で切り替えたと記録に書かない。**
5. **`.cu` の文字列リテラルは ASCII 必須** (nvcc が CP932 解釈)。**日本語コメントは可・リテラルは不可。**

---

## 10. 規律

- ブランチ `feat/g10k-conv-true-batch2`。**`git add -A` 禁止** — 対象ファイルのみパスを明示して stage。
- 研究機は**コミットのみ** (HTTPS+GCM で push 不可)。push は開発機が研究機から fetch して行う。
- **`meson test` の前に SAC OFF をユーザーへ依頼する** (黙って走らせない)。今回の実走は SAC OFF が前提。
- ★**生ログは必ず repo 配下 `docs\logs\g10k-*\` へ退避する** (T5 / T6 = `g10k-resnet` /
  T7 = `g10k-e2e`。T5 分も同じ命名規則に従う)。前例は `docs\logs\g8k-s4b\` (README 付き)。
  **temp に置いたままにしない** — temp はセッション ID 付きで消え、消えた時点で全緑が
  検証不能な自己申告に降格する (G-8k S5c→S5d で実際に起きた・出典 `docs/measurements-log.md:75` 末尾)。
- **段 (T3 / T4 / T5〜T7 / T8) の完了ごとに Fable モデルのレビュアーを立てて検査させる**
  (ユーザー指示 2026-08-24)。**実装担当と同一主体に検査させない。**
  指摘が出たら次段に進まず、その段で是正する。
- コメント・記録はすべて日本語 (CLAUDE.md コーディング規約)。C++ は Allman ブレース・
  `switch` の `case` は `switch` と同じタブ位置。

---

## 11. 本書が根拠にした一次証拠 (2026-08-24 開発機セッションで実際に開いたもの)

すべて「ソースまたは物理状態」を開いて確認した。**docs の記述は引用元としてのみ使い、根拠にはしていない**
(docs を引くときは出典として明記した)。

| 主張 | 一次証拠 |
|---|---|
| 開発機で `.cu` をコンパイルできない | `which nvcc` = not found / `CUDA_PATH` 空 / `meson.build:130` は `add_languages('cuda')` 成功時のみ |
| N>1 枝は `:487-507` の per-n 直列 | `src/kernels/conv2d.cu:479-507` |
| N==1 分岐と direct は別枝 | `src/kernels/conv2d.cu:464-468`, `:471-477`, `:509-512` |
| GEMM 下限は Cout/Ncol/K ≥ 16 | `src/kernels/conv2d.cu:290-304` |
| 1x1 は帯分割なし | `src/kernels/conv2d.cu:306-312` |
| bias カーネルの index 分解 | `src/kernels/conv2d.cu:149-160` |
| `IM2COL_TILE_BYTES` = 256MB / `rows_cap` / `banded` | `src/kernels/conv2d.cu:54-56`, `:350-366` |
| epilogue ガードは `use_gemm_path(1,...)` に帰着 | `src/kernels/conv2d.cu:521-546` / 呼出 `src/infer/unet.cu:574`, `:625` |
| 9b の合否は tol・bit 一致は print のみ | `src/tests/test_conv2d.cu:448-449`, `:513-517`, `:521` |
| **tol の実効幅** (rtol 5e-3+1e-5K / atol 3e-3+2e-5K → K=2880 で rtol≈3.4% / atol≈0.061) | `src/tests/test_conv2d.cu:76-84`, `:87-123` (max_rel の print は `:120-121`) |
| 9b の 5 ケース名 | `src/tests/test_conv2d.cu:531-544` |
| **代表形状は 320/128² ・ 640/64² ・ 1280/32²** | `src/tests/test_conv2d.cu:562`, `:696-701` |
| **SDXL UNet は 128/64/32 の 3 段のみ (16² は不在)** | `src/infer/unet.cu:939` (`C0=320,C1=640,C2=1280`), `:1072-1074`, `:1076`, `:1090`, `:1094`, `:1115`, `:1119`, `:1144`, `:1167`, `:1204`, `:1241` |
| N=1 の形状別 col サイズと 256MB 上限の関係 | `src/tests/test_conv2d.cu:421-428` (`:424` down_block_0 / `:425` 単帯 / `:426` 帯分割発動 / `:427` VAE) |
| [G5] の形状が direct に落ちる | `src/tests/test_conv2d.cu:384-387` + `conv2d.cu:299-302` |
| 3runs memcmp 方式の前例 | `src/tests/test_conv2d.cu:548-564`, `:692-719` |
| **conv 単体 batch ベンチは同一ループ内で交互採取** (ドリフト耐性の根拠) | `src/tests/test_conv2d.cu:725-795`、とくに `:779` (warmup) と **`:782`** (`tb.push_back(time_ms(true)); ts.push_back(time_ms(false));`)・出力は `:785-787` |
| 同ベンチの呼出は 1 本だけ (320ch/64²) | `src/tests/test_conv2d.cu:888-900`、とくに `:899` |
| `test_gemm` に batched ケースが無い | `src/tests/test_gemm.cu:123`, `:154-239`, `:318-` (関数一覧) |
| `prof_unet_fast_warm` が B=1 | `src/tests/prof_unet_fast_warm.cu:82`, `:116`, `:128`, `:148`, `:154-156` + `src/infer/unet.cu:1462` |
| B を渡せるのは別 API | `src/infer/unet.cuh:137` / `src/infer/unet.cu:1475-1489` |
| resnet バケットの計時点 | `src/infer/unet.cu:537-543` (`cat_resnet_sec`) / e2e 出力は `src/infer/diffusion.cu:701-702` |
| reserve 定数と `reserve_arenas()` | `src/infer/diffusion.cu:203-210`, `:218-245`, `:264-283` |
| アリーナ終了時サマリ | `src/infer/unet.cu:1352-1370` |
| **`prof_arena_e2e` は malloc / free / chunkAlloc を同じ行で出す** | `src/tests/prof_arena_e2e.cu:134-142` (`:135` malloc / `:136` free / `:137` chunkAlloc) |
| reserve 不足警告は stderr 無条件 | `src/kernels/device_arena.cu:347-360` |
| pool キルスイッチは getenv キャッシュ | `src/kernels/device_arena.cu:210-232` |
| cuBLAS キルスイッチは getenv キャッシュ | `src/kernels/gemm.cu:376-386` |
| GEMM 実装の読み先 (写像の導出元) | `src/kernels/gemm.cu:405-`, `:423`, `:516-` |
| DB2_BENCH は 1 プロセス 3 構成 / env 名 | `src/tests/test_diffusion_batch2.cu:436-443`, `:481-486` |
| GATE2 の閾値 0.999 | `src/tests/test_diffusion_batch2.cu:360-372` |
| `prof_arena_e2e` は meson test 未登録 | `src/meson.build:886-899` |
| 登録テスト数 = 53 | `src/meson.build` の `test(` 定義数を実数カウント |
| `DOLLAMA_CONV_BATCH` は新規名 (衝突なし) | `grep -rn DOLLAMA_CONV_BATCH src/ docs/` = 0 件 |
| 「当時の記述を履歴として残す」型の前例 | `docs/fast-mode-plan.md:463-466` |
| **`diffusion.cu` の総行数と `DiffusionPipeline::` メソッド境界** | `src/infer/diffusion.cu` = **981 行** / `:384` `:438` `:461` `:521` `:533`(`generate`) `:732`(`generate_txt2img`) |
| **reset は 1 箇所・dump は 1 箇所・どちらも `generate` 内** | reset `src/infer/diffusion.cu:554` / dump `:670-716` (ヘッダ `:686`・resnet `:701-702`・終端 `:714`) |
| `generate` は CFG を拒否し B=1 で呼ぶ | `src/infer/diffusion.cu:545-549`, `:616-624` |
| `generate_txt2img` は batched で呼ぶが計装が無い | `src/infer/diffusion.cu:872` (`launch_unet_batched(...,2,...)`) / `:732-981` に reset・dump とも grep 0 件 |
| **`cat_resnet_sec` の加算は 1 箇所のみ** (経路非依存) | `src/infer/unet.cu:542` (`resnet_block` `:537-543` の先頭)・`src/` 全体 grep |
| **`launch_unet_impl` の呼出元 3 つ** (B=1 が 2 本・B=B が 1 本) | `src/infer/unet.cu:1326` / `:1462` / **`:1487`** (定義は `:902-1309`) |
| 段/カテゴリ系カウンタも `launch_unet_impl` 内 = 経路非依存 | `src/infer/unet.cu:925` (`unet_steps`) / `:1298` (`unet_total_sec`) |
| **`generate` ローカルでしか埋まらない欄** | `vae_sec` = `src/infer/diffusion.cu:655` (txt2img 側の decode は `:944` で無計時) / `host_roundtrip_sec` = `:613`, `:642` / `total_sec` = `:676` / `%` の分母 = `:678`, `:683` |
| `weight_upload_*` は重みアップロード時のみ加算 (warm では窓外) | `src/infer/unet.cu:343-345` |
| `test_diffusion_batch2` は `generate_txt2img` しか呼ばない | `src/tests/test_diffusion_batch2.cu:249` (parity) / `:460` (bench) |
| SDXL UNet のチャネル定数 | `src/infer/unet.cu:939` (`C0=320, C1=640, C2=1280`) |
| **DB2_BENCH の wall 窓 (`total_sec` を却下した根拠)** | `src/tests/test_diffusion_batch2.cu:470-475` (`steady_clock` の t0/t1・前後に `cudaDeviceSynchronize` `:469`,`:472`) / min-of-iters は `:476` |
| **印字物に注記を埋める型の実在前例** | `src/kernels/device_arena.cu:350-358` (警告 + 直し方のポインタ・文面を arena 非依存にした理由は `:341-346`) / `src/infer/diffusion.cu:279-280` (操作指示) / **`src/tests/test_diffusion_batch2.cu:504`, `:514`** (数値の隣に「何に使ってよい数値か」= T2b が真似る型) / `src/tests/prof_unet_fast_warm.cu:153` (`must be ~0 when warm`) |
| **同じ型が陳腐化した反例** | `src/tests/prof_unet_fast_warm.cu:155-156` (`gate: <=0.95s / stretch <=0.85s / baseline 1.225s` = 埋め込んだ後に退役) |
| **parity 節が 1 プロセスで `generate_txt2img` を呼ぶ回数 = `g1_again` 付きの off / epi 2 構成が各 4 回 (guids 3 + 再走 1)・on / fst は `nullptr` = 各 3 回** (T2b DoD ② の置き場所根拠) | `src/tests/test_diffusion_batch2.cu:236` (steps=4) / `:247-253` (`gen` ラッパ) / `:267-270` (guidance 3 本) / **`:273-276`** (g=1.0 の再走 `g1_again`) / `:284`, `:289`, `:294`, `:300` (`run_config` 呼出 4 箇所・`g1_again` 付きは `:284` off と `:300` epi のみ) |
| **同一ツリー二連走は determinism 検査であって無改変証明ではない** (WARN 4 の出典) | `src/tests/test_diffusion_batch2.cu:377-380` (GATE3 注記) |
| **負のコントロールで空撃ちを潰した前例は F1** (F5 ではない) / **その出力がファイルに残っていなかった教訓** | `docs/measurements-log.md:223-230` (`test_http` 直列化ゲート) / F5 は「既存 exe 内にゲート増設・新規 meson test 0 本」の前例 = `docs/g8k-review-fix-plan.md:161`, `:285` |
| **帯の下限 10% の根拠 (実測ドリフト +9.9%)** | `docs/fast-mode-plan.md:310-313` (同一走行内 default 再測 1.28564s → 1.41299s) |
| ★**PL が挙げた `conv2d.cu:31-40` は printf ではなくソースコメント** (「帯分割の正当性に関する重要な注意」) = 「ログに埋めた警告」の前例としては当たらない (出典を訂正して上記に差し替えた) | `src/kernels/conv2d.cu:31-40` |

**docs からの引用 (出典として明記した二次記述)**: `docs/fast-mode-plan.md:206`, `:212-215`, `:216`, `:244`,
`:287`, `:306-316`, `:310-313`, `:318-324`, `:345-347`, `:411-413`, `:463-466`, `:664-675`, `:688-693` /
`docs/measurements-log.md:74`, `:75`, `:192-198` / `docs/g8k-review-fix-plan.md:268` /
`docs/testing.md:26-30` / CLAUDE.md 計測表 (G-2k S2 行の MAE 6.4e-05 と「conv2d S1 は per-n ループで
BIT-EXACT」・G-4k S2 行の 1.212s)。

---

## 12. 残債 (本 Pkg のスコープ外)

1. **UNet 単体プロファイルの B=2 化** — 起草時に選択肢として挙げ、**2026-08-24 に却下された**
   (新規計測コードの検証工数 + どちらにせよ過去の 1.212s と繋がらない)。
   B=2 の UNet 単体プロファイルが本当に要るのは **G-12k (transformer 脇役の内訳分離)** なので、
   **G-12k 起票時の宿題**として持つ。
2. **(完了・履歴として残す) `.claude\agents\perf-profiler.md` の計器誘導を是正済み (2026-08-24・ユーザー決裁)**。
   PL の当初判断は「T8 では触らない・ユーザー決裁枠へ上げる」だったが、**ユーザー本人の決裁で今回の作業に含めた**。
   決裁理由 = 「**エージェント定義は起動時に文脈へ自動注入されるので、記録だけ直しても計器の誘導が残る**」。
   - **実際に残っていたのは「退役した数値」ではなく「計器の誤誘導」だった**: `1.212` / `1.225` / `≤0.95s` は
     `.claude\agents\` 配下のどのファイルにも**書かれていなかった** (grep 0 件)。危険だったのは、
     計器表の行が `[RESNET-BUCKET]` を resnet の計器として提示しつつ **B=1 固定であることを書いていなかった**点。
   - 是正 4 箇所 (いずれも**分母・計器の記述に限定**・役割定義やツール権限は無改変): ①計器表の
     `prof_unet_fast_warm` 行に **B=1 固定 + 「B>1 が被験変数のときは使えない」** を追記
     (★「使うな」ではない — **B=1 の UNet 単体プロファイラとしては現役**) ②カウンタ表の
     `cat_resnet_sec` 行に「e2e 出力で読めば N>1 枝を通る = conv batch 化の合否計器」を追記
     (★**この追記内容は誤りだった** — dump は B=1 経路 (`generate`) にしかなく、この記述は F3 として
     Fable 監査で BLOCK。**当該行は全面改稿済み** (現況 = 「印字箇所は B=1 経路にしかない」+
     T2b 適用後の条件分け + `%` 列を出さない決裁)。**この文を「agent ファイルにはこう書いてある」と
     引用しないこと** — §12 は履歴節なので、ここだけを読むと F3 の誤りが復活する)
     ③攻め筋の G-10k 項に **1.212s / 1.225s の退役 + 削減率判定 + 分母は毎回「その回の
     (同一時刻帯・別プロセス)」から取る**を追記 (★初版は「同一走行から取る」と書いてしまい
     Fable 監査で BLOCK。**走行 = プロセス**が正典用法 (`docs/measurements-log.md:75`) で、
     getenv キャッシュ型のキルスイッチは**プロセス単位固定**ゆえ「同一走行内に既定と `=0` の
     両方がある」ことは物理的にあり得ない。2026-08-24 に両ファイルとも是正済)
     ④「batch2 が 2× にならない」行に**正しい観測点 2 つ**を追記。
   - ★**教訓 (残す)**: **エージェント定義は起動時に文脈へ自動注入されるため、docs の腐りより実害が大きい**
     (docs で塞いだ誤りがエージェント経路から復活する)。記録を直したら**同じ基準で
     `.claude\agents\` も grep する**。ただし**波及先の変更は対象ファイルごとに決裁を取る** —
     今回の決裁は `perf-profiler.md` に対してのみ出たものであり、他ファイルへは及ばない。
   - **他ファイルは未変更 (報告のみ)**: `cuda-kernel-dev.md:33` (`拡散ループ (CFG・batch2)` = 単なる
     ファイル対応表)・同 `:126` (「G-4k の resnet ゲートは不合格で正しく閉じた」= **記述は正確**で、
     計器も分母も指定していない) の 2 件のみが関連ヒット。`gpu-benchmarker.md` / `docs/agent-common.md` は
     該当 0 件。**いずれも今回は触っていない。**
3. **`docs/hw-accel-plan.md:118` / `:132`** (「GPU 側で次に残っているのは G-9k / G-10k → G-5k → G-6k」)
   は G-10k クローズで追随が必要になる。追随の要否は PL 決裁 (T8 のファイル指定に入っていない)。
4. **`src/tests/prof_unet_fast_warm.cu:155-156` に退役済みゲートが出力文字列として残っている**:
   `(gate: <=0.95s / stretch <=0.85s / baseline 1.225s)`。この値は **B=1 換算に紐づいた絶対値ゲート**で
   G-10k の分母としては退役済み (§3) だが、**harness を走らせると今も印字される**。
   `src/` は本 Pkg で不可侵ゆえ**修正しない** = **別タスクの宿題**。
   ★**走行ログにこの行が出ても分母として読まないこと** (T8 の記録スコープ 8 で明文化させる)。
   ★**宿題の優先度 = 「次に fast-mode を触る段の先頭で処理」**。G-10k では同ファイルを使わないので
   実害は「読んだ人が誤解する」だけだが、**放置すると 4 回目の同型事故の入口**になる。
   (**§2 F3 は本決裁で解消したので残債から外し、実装台帳 T2b / T2c へ移した。**)
5. **txt2img 経路の `%` 列 (`total_sec`) は G-9k / G-12k が必要になった時点で追加する。**
   G-10k では **(B) 採用 = 出さない**と決裁済み (T2b 禁止事項 1)。**(A) を将来入れるコストは低いまま
   据え置かれる** (計時 3 行相当)。★**追加するときは、DB2_BENCH の wall
   (`src/tests/test_diffusion_batch2.cu:470-475` の `steady_clock` 窓・min は `:476` の min-of-iters) とは
   **窓が違う**ことを必ず併記する — 併記しないなら追加しない方がよい。
