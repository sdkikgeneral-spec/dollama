# セキュリティポリシー

**[English](SECURITY.md)** | **日本語**

## ステータス

dollama は **実験的な研究プロジェクト**であり、現在も開発途上で
**製品用途を想定していません**。[LICENSE](LICENSE) のとおり「現状のまま」提供され、
迅速な修正やバックポートは保証されません。

## サポート対象バージョン

最新の `main` ブランチのみをサポートします。リリース版・バージョン付き成果物は
まだありません。

## 脆弱性の報告

セキュリティ上の問題は**非公開で**報告してください。公開 Issue は立てないでください。

- 推奨: 本リポジトリの GitHub **「Report a vulnerability」**（Security → Advisories）。
- 記載内容: 影響を受けるファイル/コンポーネント、再現手順、影響範囲。

個人研究プロジェクトのため、対応はベストエフォート（返信までの時間は前後します）。

## 対象範囲

対象 — 本リポジトリの自作コード:

- C++ コア、自作 CUDA カーネル、推論グルー（`src/`）
- 実装後の HTTP サーバ（`src/server/`、Phase 3）

対象外:

- サードパーティの**モデル重み**（SDXL / CLIP-L / WD14 / Qwen2 等）とそのライセンス
- サードパーティのランタイム/ライブラリ（OpenVINO / CUDA / cpp-httplib / nlohmann/json 等）
- 既に利用者が完全に掌握しているローカル環境を悪意的に改変して初めて成立する問題

## 運用上の注意（実行前に必ず確認）

- 予定している HTTP サーバは**認証なし・未ハードニング**です。**信頼できないネットワークに
  公開しない**でください。ローカル実験では localhost にバインドしてください。
- 重み/モデルローダー（safetensors 等）は**セキュリティ境界ではありません**。
  **信頼できるモデルファイルのみ読み込んで**ください。不正な/信頼できないファイルは危険な場合があります。
- 生成物の取り扱いとモデルライセンスの遵守は利用者の責任です。
